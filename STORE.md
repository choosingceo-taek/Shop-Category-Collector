# Chrome 웹스토어 배포 가이드 (Unlisted)

> 이 팀은 Google Workspace를 쓰지 않는다(Microsoft 기반) → **Private 불가,
> Unlisted로 간다.** 근거와 대안(Intune 강제 설치)은 4장.

## 왜 스토어인가 — 자동 업데이트가 되는 두 가지 길

크롬에서 확장이 자동 업데이트되는 경로는 둘뿐이다. 나머지(개발자 모드 +
공유폴더, .crx 직접 배포)는 수동 리로드가 남거나 설치 자체가 차단된다.

**① 웹스토어 Unlisted / Private (이 문서의 방법, 권장)**
- Unlisted: 검색 노출 없음, 링크 아는 사람만 설치. 판매 아님(무료).
- Private: 지정한 Google 그룹에 든 계정만 설치 가능 — 완전 사내 한정.
- 비용 = 등록비 $5 1회 + 심사 1~3일. 개인 크롬 환경에서 확실히 동작.

**② 기업 정책 + 자체 호스팅 CRX (스토어 없이, IT 개입 필요)**
- `.crx`를 개인 키로 서명해 사내 서버에 올리고 `update.xml`을 함께 둔다.
  크롬이 주기적으로 XML을 확인해 자동 업데이트한다.
- 단, Windows/Mac 크롬은 **관리되는 브라우저에서만** 허용:
  Google Admin Console(Chrome Browser Cloud Management, 무료) 또는
  Windows GPO로 `ExtensionInstallForcelist`에
  `<확장ID>;https://사내서버/update.xml` 을 배포해야 한다.
  개인 크롬에 .crx를 직접 주면 설치가 차단된다(우회 불가).
- 판단법: 팀원 크롬에서 `chrome://management` 를 열었을 때 "관리되는
  브라우저"가 아니면 이 방법은 쓸 수 없다 → ①로 간다.


팀 배포는 **Unlisted(목록에 없음)** 게시로 간다. 링크를 아는 사람만 설치할 수
있고, 검색에는 안 나오며, 무엇보다 **자동 업데이트**가 된다 — ZIP 덮어쓰기 →
리로드 → F5를 팀원 60명에게 매번 시키지 않아도 된다.

## 0. 준비물 (한 번만)

1. Google 계정으로 https://chrome.google.com/webstore/devconsole 접속
2. 개발자 등록비 **US$5** 결제 (1회, 카드 필요)
3. 계정 확인 이메일 인증

## 1. 제출 ZIP 만들기

```
sh pack.sh
```
→ `dist/market-lens-<버전>.zip` 이 생긴다. **이 파일을 올린다**
(저장소 전체 ZIP이 아니라 — 개발용 파일이 섞이면 심사만 느려진다).

## 2. 대시보드에서 새 항목 만들기

"새 항목" → ZIP 업로드 → 아래를 채운다.

### 스토어 등록정보 (복사해 쓰기)

- **이름**: Market Lens
- **요약** (132자 이내):
  > 참고할 브랜드·카테고리 URL을 리스트로 모으고, 스캔해서 상품명·썸네일·원단·혼용률을 Excel로 뽑는 디자인팀용 마켓 리서치 도구.
- **설명**:
  > 디자인팀의 주간 마켓 리서치를 위한 도구입니다.
  >
  > · 브라우징 중 참고하고 싶은 브랜드 카테고리 페이지를 리스트에 담고
  > · ▶ Run all 한 번으로 리스트 전체를 스캔해
  > · 브랜드·카테고리·상품명·썸네일·URL·원단·혼용률 7컬럼 Excel 한 파일로 내보냅니다.
  >
  > 스캔이 쌓이면 LAB에서 주차별 신상 피드, 브랜드별 어소트먼트,
  > 키워드·원단·색상 점유율의 주차별 변화를 확인할 수 있습니다.
  > 모든 데이터는 사용자 브라우저 안에만 저장되며 어떤 서버로도 전송되지 않습니다.
- **카테고리**: 생산성 (Productivity) 또는 쇼핑
- **언어**: 한국어

### 그래픽 (필수)

- 스크린샷 **1280×800** 최소 1장 (권장 3~5장: 사이드 패널 / 신상 피드 / LAB /
  Excel 결과). 실제 화면을 캡처해서 쓰면 된다.
- 스토어 아이콘 128×128: `icons/icon128.png` 그대로 사용 가능.

## 3. 개인정보 보호 탭 (심사 통과의 핵심)

- **단일 목적 설명**:
  > 사용자가 지정한 쇼핑몰 카테고리 페이지의 공개 상품 정보를 수집해
  > 로컬에 저장하고 Excel로 내보내는 마켓 리서치.
- **개인정보처리방침 URL**: 저장소의 PRIVACY.md 주소
  (예: `https://github.com/<owner>/<repo>/blob/<branch>/PRIVACY.md`)
- **데이터 사용 신고**: "개인 식별 정보를 수집하지 않음"에 해당.
  원격 코드 사용 안 함(모든 스크립트 동봉). 데이터 판매/공유 없음.

### 권한 정당화 (요구하는 문구, 복사해 쓰기)

| 권한 | 정당화 |
|---|---|
| `storage` | 사용자가 만든 URL 리스트와 설정을 브라우저에 저장 |
| `downloads` | 스캔 결과 Excel/JSON 파일을 저장. 일부 쇼핑몰(Target 등)이 페이지 내 앵커 다운로드를 차단해 서비스워커 경유 저장이 필요 |
| `sidePanel` | 리스트 관리·스캔 진행을 보여주는 주 UI |
| `contextMenus` | 우클릭 "상품 담기" 메뉴 |
| `scripting` + `activeTab` | 우클릭/버튼 클릭 시점에만, 사용자가 보고 있는 탭에서 상품 추출 스크립트 실행. 사전 호스트 권한 없이 동작 |
| `host_permissions` (쇼핑몰 목록) | 지원 쇼핑몰의 상품 상세·이미지를 스캔 중 가져오기 위함. 나열된 리테일러 도메인으로 한정 |
| `optional_host_permissions` (`*://*/*`) | 목록에 없는 쇼핑몰에서 "상품 담기"를 누른 경우, **클릭 시점에 그 사이트 하나만** 요청. 설치 시점에는 어떤 호스트 권한도 추가로 갖지 않음 |

## 4. 공개 범위 — 왜 Unlisted인가 (Private이 아니라)

스토어의 공개 설정은 같은 항목에 붙는 **라디오 버튼 하나**다. 무엇을 고르든
등록비·심사·자동 업데이트는 동일하다. 다른 것은 "누가 설치할 수 있는가" 뿐.

| 설정 | 설치 가능한 사람 | 조건 |
|---|---|---|
| 공개 | 누구나 (검색됨) | — |
| **Unlisted** | **링크를 아는 사람** | **없음 ← 우리** |
| Private | 지정한 Google 그룹 구성원 | **개발자 계정이 Google Workspace 조직 소속이어야 함** |

**이 회사는 Microsoft 기반이라 Workspace가 없다 → Private은 선택 불가.**
Unlisted가 남는 유일한 스토어 옵션이고, 실질적으로도 충분하다:

- 스토어 검색·카테고리에 노출되지 않는다.
- 설치하려면 32자 확장 ID가 든 정확한 URL을 알아야 한다(추측 불가).
- 실제 노출 위험은 "팀원이 링크를 외부에 전달하는 것" 하나뿐이고, 이 도구는
  공개된 쇼핑몰 페이지를 읽어 엑셀을 만드는 물건이라 그 위험의 크기가 작다.

게시 후 나오는 스토어 링크를 팀에 공유하면 끝. 이후 업데이트는 7장의 자동
배포가 처리한다(버전 번호가 이전보다 커야 한다 — manifest.json의 version).

### 스토어를 아예 안 쓰는 길 — Intune/GPO 강제 설치

회사가 Microsoft로 기기를 관리한다면(Intune·Active Directory GPO) IT가 크롬
정책 `ExtensionInstallForcelist`를 밀어 넣는 방법이 있다. 이 경우 두 갈래다:

- **스토어 항목 + 강제 설치**: Unlisted로 올린 뒤 그 확장 ID를 정책에 넣는다.
  팀원이 설치 버튼을 누를 필요조차 없고, 삭제도 막힌다.
- **자체 호스팅 CRX + update.xml**: 스토어를 완전히 건너뛴다. $5도 심사도
  없다. 대신 CRX 서명 키를 우리가 관리하고, 파일을 사내(또는 Azure)에 호스팅
  해야 하며, **IT가 정책을 배포해 줘야 한다**.

판단 기준은 하나다 — **디자인팀 도구 하나 때문에 IT에 정책 요청을 넣을 수
있는가.** 넣을 수 있으면 강제 설치가 더 깔끔하고, 그렇지 않으면 Unlisted가
IT를 거치지 않는 유일한 자동 업데이트 경로다. 지금 권장은 **Unlisted**.

## 5. 심사 예상

- 보통 며칠(경험상 1~3일, 첫 제출은 더 걸릴 수 있음).
- 심사 지연 요인은 대부분 권한이다. 위 정당화 문구를 그대로 붙이면 되고,
  `optional_host_permissions`의 `*://*/*`가 질문을 받으면: "설치 시 부여되지
  않으며, 사용자가 특정 사이트에서 기능을 쓸 때 그 오리진 하나에 대해서만
  런타임 요청" 이라고 답하면 된다.
- 리젝되면 사유가 메일로 오고, 수정 후 재제출하면 된다.

## 6. 업데이트 루틴 (게시 이후)

수동으로 하면:

1. `manifest.json` 버전 올리기 (예: 1.65.1 → 1.66.0)
2. `sh pack.sh`
3. 대시보드 → 패키지 → 새 ZIP 업로드 → 제출
4. 심사 통과 후 몇 시간 내 팀 전체 크롬이 자동 업데이트

수동 ZIP 배포(개발자 모드 로드)는 이때부터 개발/테스트용으로만 쓴다.

## 7. 배포 자동화 — 커밋하면 알아서 올라가게

`.github/workflows/release.yml`이 이미 들어 있다. 브랜치에 푸시가 들어오면
ZIP을 만들어 웹스토어에 올리고 게시까지 한다. **위 3번(사람이 대시보드에
올리는 단계)이 없어진다.** 남는 일은 버전 올리기 하나뿐이다.

비밀값 네 개를 저장소에 넣기 전까지는 **ZIP만 만들고 조용히 넘어간다** —
설정 전이라고 빨간 실패가 뜨지 않는다.

### 한 번만 하는 준비

**① 확장 ID 얻기** — 5장까지 따라 해서 **첫 ZIP은 손으로 한 번 올린다**.
그 업로드가 확장 ID를 만든다(대시보드 주소의 `.../detail/<32자>` 부분).

**② API 자격 증명 만들기** (Google Cloud Console, 무료)
1. 프로젝트 하나 만들기
2. **API 및 서비스 → 라이브러리 → "Chrome Web Store API" 사용 설정**
3. **사용자 인증 정보 → OAuth 클라이언트 ID → 데스크톱 앱** → 클라이언트 ID·시크릿 기록
4. OAuth 동의 화면은 **외부/테스트** 로 두고 본인 계정을 테스트 사용자로 추가
5. 리프레시 토큰 발급 — 브라우저에서 아래 주소를 열고 코드를 받는다:
   ```
   https://accounts.google.com/o/oauth2/auth?response_type=code
     &scope=https://www.googleapis.com/auth/chromewebstore
     &client_id=<클라이언트ID>
     &redirect_uri=urn:ietf:wg:oauth:2.0:oob
     &access_type=offline&prompt=consent
   ```
   받은 코드를 토큰으로 교환:
   ```
   curl -d "client_id=<ID>" -d "client_secret=<시크릿>" \
        -d "code=<코드>" -d "grant_type=authorization_code" \
        -d "redirect_uri=urn:ietf:wg:oauth:2.0:oob" \
        https://oauth2.googleapis.com/token
   ```
   응답의 `refresh_token`이 필요한 값이다(한 번만 나오므로 바로 저장).

**③ 저장소 비밀값 넣기** — GitHub 저장소 → Settings → Secrets and variables
→ Actions → New repository secret, 네 개:

| 이름 | 값 |
|---|---|
| `CWS_EXTENSION_ID` | ①에서 얻은 32자 ID |
| `CWS_CLIENT_ID` | ②-3의 클라이언트 ID |
| `CWS_CLIENT_SECRET` | ②-3의 시크릿 |
| `CWS_REFRESH_TOKEN` | ②-5의 refresh_token |

### 이후의 흐름

```
manifest.json 버전 ↑  →  git push  →  (자동) ZIP 빌드 → 업로드 → 게시
                                        → 심사 → 팀 크롬이 자동 업데이트
```

- **버전을 올리지 않으면 실패한다.** 스토어는 버전당 빌드 하나만 받는다.
  실패 로그에 "버전을 올리라"고 명시된다 — 조용히 옛 빌드에 머무는 것보다 낫다.
- 문서(`*.md`)만 고친 푸시는 워크플로가 돌지 않는다.
- Actions 탭에서 **Run workflow**로 수동 실행도 되고, 이때 `trustedTesters`를
  고르면 지정한 테스터에게만 먼저 나간다.
- 심사가 걸려 있어도 그 실행의 **Artifacts**에 ZIP이 남는다 — 급하면 그걸로
  수동 배포하면 된다.

### 왜 서드파티 액션을 안 쓰나

게시 자격 증명은 **팀 전원의 브라우저에 코드를 밀어 넣을 수 있는 권한**이다.
남의 액션을 체인에 넣으면 그 액션의 공급망이 우리 팀의 공급망이 된다.
그래서 GitHub 공식 checkout/artifact와 `curl`만 쓴다.
