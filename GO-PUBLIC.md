# 저장소를 public으로 바꾸고 자동 업데이트 켜기

코드는 전부 준비돼 있습니다. **남은 것은 GitHub 설정 한 번**이고, 그건 계정
소유자만 할 수 있습니다.

## 왜 public이어야 하는가 (실측)

| | private (지금) | public |
| --- | --- | --- |
| `raw.githubusercontent.com`에서 버전 확인 | **404** | **200** |
| 새 버전 배지 · 알림 | 뜰 수 없음 | 뜸 |
| `⚡ Update now` (ZIP 바이트 읽기) | 받을 수 없음 | 받음 |

확장이 보낼 수 있는 요청에는 자격 증명이 없습니다 — `chrome-extension://`
오리진이라 쿠키가 실리지 않고, raw는 애초에 쿠키로 인증하지 않습니다. 그래서
private인 동안은 `check()`가 **항상 `{ok:false}`**입니다.

채팅으로 받은 ZIP 링크가 되는 이유는 그건 **github.com**이 로그인 세션으로
내주기 때문입니다. 그래서 지금까지 아무도 이상을 느끼지 못했습니다.

## 바꾸기 전에 (한 번만)

1. **`PRIVACY.md`의 연락처를 회사 대표 주소로.** 지금은 개인 지메일이고,
   public이 되면 스팸 봇이 긁어갑니다.
2. **브랜드 리스트를 어떻게 할지 정하기.** `starter-list-miro.txt`(898줄)에
   팀이 추적하는 브랜드·카테고리 주소가 들어 있습니다. 공개해도 무방하다고
   판단하셨으므로 그대로 두면 되고, 나중에 생각이 바뀌면 파일을 빼는 것만으로는
   부족합니다 — **커밋 이력에서도 지워야** 합니다.
3. **감사 결과 (v3.14.0에서 확인)**: 전 이력 53개 커밋에 자격증명 0건,
   릴리스 워크플로에 `pull_request` 트리거 없음(포크가 시크릿을 못 가져감),
   서드파티 액션 0개, 수집 데이터 0건.

## 바꾸기

1. `https://github.com/choosingceo-taek/Shop-Category-Collector/settings`
2. 맨 아래 **Danger Zone → Change repository visibility → Make public**
3. 저장소 이름을 입력해 확인

## 바꾼 직후 (권장)

**Settings → Branches → Add branch protection rule**, 브랜치
`claude/main-session-cudnkx`에:

- *Require a pull request before merging* 끄기(혼자 푸시하므로), 대신
- *Restrict who can push* 를 본인으로

public 저장소는 누구나 PR을 올릴 수 있고, **그 PR이 병합되면 팀원 전원의
브라우저에 그 코드가 들어갑니다.** 외부 PR을 실수로 병합하는 경로를 막는 것이
목적입니다.

## 확인

바꾼 뒤 팀원 크롬 하나에서:

1. 사이드패널을 엽니다 → 헤더 버전 칩이 `v3.17.0` 처럼 **숫자만** 보이면 확인
   성공입니다. `v3.17.0 ?` (점선)이면 아직 못 읽고 있는 것입니다.
2. 다음 릴리스가 나오면 칩이 `v3.17.0 → 3.18.0`으로 바뀌고, 툴바 아이콘에
   **NEW** 배지가 붙습니다.
3. 업데이트 상자에서 **📂 Choose my Market Lens folder** 를 한 번 눌러
   크롬이 로드한 그 폴더를 지정합니다 (팀원당 한 번).
4. 그 뒤로는 **사이드패널을 여는 것만으로** 최신 버전이 설치되고 재시작됩니다.

## "자동"이 정확히 어디까지인가

- **알림**: 완전 자동. 패널이 닫혀 있어도 크롬이 알려줍니다(버전당 한 번).
- **설치**: **패널을 열면** 스스로 내려받아 폴더에 쓰고 재시작합니다. 누를 것이
  없습니다.
- **패널을 한 번도 안 열면**: 설치는 일어나지 않습니다. 크롬은 폴더로 로드한
  확장을 스스로 갱신하지 않고, 저장된 폴더 핸들은 문서(패널)가 있어야
  쓸 수 있기 때문입니다. 그래서 알림이 그 한 번의 클릭을 부르는 역할을 합니다.
- **완전 무인**(패널조차 열 필요 없음)을 원하면 길은 **웹스토어 Unlisted**
  하나뿐입니다 — 절차는 `STORE.md`.

## 안전장치 (끄지 않아도 되는 이유)

- **스캔 중에는 절대 재시작하지 않습니다.** 그날 엑셀을 잃기 때문입니다.
- **자동 재시작에서 돌아오지 못한 브라우저에는 두 번 다시 시도하지 않습니다.**
- **폴더는 조용히 묻지 않습니다.** 권한이 만료됐으면 자동 설치를 건너뛰고
  평소의 한 번 누르는 경로로 돌아갑니다.
- **같은 버전은 한 번만 시도합니다.** 실패해도 패널을 열 때마다 반복하지
  않습니다.
- **업데이트는 데이터를 지우지 않습니다** — v3.16.0에서 실측했습니다(같은
  폴더에 덮어쓰기 + DB 버전 상승에도 상품·리스트 그대로).
- 마음에 안 들면 업데이트 상자 안에서 **한 번 눌러 끌 수 있습니다.**
