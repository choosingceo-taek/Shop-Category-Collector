# Market Lens — 프로젝트 헌장

온라인 쇼핑몰의 카테고리/검색 결과를 수집해 **키워드 중심 비주얼 트렌드 리서치**의
원천 데이터(xlsx)를 만드는 크롬 확장(MV3). 최종 비전: 카테고리 구조를 인식해
카테고리별로 상품을 샘플링하고, 그 데이터로 트렌드 레포트를 생성한다.

**사용자는 스타일 디자이너(비개발자).** 시즌별로 "어느 브랜드가 유명하고, 어떤
제품이 잘 팔리고, 그 제품의 원단이 무엇인지"를 리서치한다. UI 문구·에러 메시지·
문서는 이 사용자가 읽는다는 전제로 쓴다.

## 제품 목표 — 무엇을 성공으로 볼 것인가

**디자인팀 구성원 전원이 각자 크롬에서 이 도구를 켜고 계속 쓰는 것.** 기능 수가
아니라 채택과 유지가 목표다. 그래서 우선순위는 이 순서로 판단한다:

1. **정확도가 채택의 조건이다.** 쓰는 사람이 여럿이므로 숫자가 한 번 틀리면
   신뢰가 팀 전체에서 무너지고 다시 열지 않는다. 새 기능보다 기존 수집의
   정확도 검증이 항상 앞선다.
2. **설명 없이 시작돼야 한다.** 팀원마다 붙어서 알려줄 수 없다. 아이콘을 누르면
   사이드 패널이 페이지를 읽고 "여기선 이걸 하면 된다"를 먼저 말해준다.
   (그래서 패널은 부가 UI가 아니라 온보딩의 핵심이다.)
3. **사이트 커버리지**: 팀의 Young Women's 리스트(59개) + Miro 리서치 리스트
   (120개 도메인, Contemporary/Basics/Active — Everlane, Sezane, J.Crew, Vince,
   Lululemon, H&M 등)가 manifest에 등록되어 있다(2026-08). 총 370개 매치 패턴. 대부분 Shopify라 플랫폼 감지로 자동 동작하고, 나머지는
   generic 폴백 — 정확도는 사이트별 진단 출력으로 맞춘다. Inditex 계열(Bershka·
   Pull&Bear·Stradivarius)은 house-brand 팩토리 승격 후보. 원칙: **새 어댑터
   추가보다 정확도·사용성 우선.** 팀이 보는 몰이 대체로 지원된다고
   확인됨(2026-08). 새 어댑터 추가보다 정확도·사용성이 우선.
4. **업데이트가 쉬워야 계속 쓴다.** ZIP 덮어쓰기→리로드→F5는 비개발자에게 매번
   실패 지점이다. 팀 배포는 웹스토어 Unlisted로 가서 자동 업데이트를 받는다.

## 현재 상태 요약 (v1.7.x)

- **컬러 스와치를 상품으로 읽고 있었다**(v3.46.0, 사용자 보고 — Varley
  `?o_cat=Tops&o_cat=Sweatshirts`: 30개 중 15개, 이름이 `Color swatch for Mid Tan`,
  사진은 색 덩어리). 타일 아래 색상 스와치 줄이 있고, 스와치마다 몰이 직접 적은
  `alt="Color swatch for Mid Tan"`이 있다. **v2.5.0의 크기 가드는 몰이 40px이라고
  적었을 때만** 걸러내는데 이 스와치는 크기를 안 적는다 → 사진 경로도 이름 경로도
  스와치를 골랐다. 픽스처로 재현: **30행 전부 스와치 사진**.
  - → **몰이 스스로 "스와치"라고 적은 그림은 상품 사진도 상품명도 아니다**(`isSwatch`,
    alt·aria-label·title). 크기 선언과 같은 종류의 **구조적 사실**이지 클래스 이름이
    아니다. `bestImage`·`bestName` 둘 다 같은 규칙을 쓴다(v3.18.0에 이름 경로가
    사진 규칙을 몰라서 났던 사고의 반복 방지).
  - `varley-e2e` 11건 — 15개만 렌더하고 나머지는 **Load more 뒤에** 있는 그리드에,
    주소에는 몰의 필터가 걸려 있어 **벌크 JSON으로 채우면 안 되는**(v3.27.0) 모양까지.
    수정 전 코드에서 사진 2건 실패(30행 전부 `swatch-*.png`).
  - **확인 못 한 것**: 실제 varley.com. 15/30이 우리 픽스처에서는 눌러서 30이 됐다 —
    라이브에서도 그런지는 다음 스캔이 답한다.

- **축 카드는 순위·개수·주간 막대다 — `2 /6 brands`는 한눈에 안 읽혔다**(v3.45.0,
  사용자 지적 "딱 봐도 시각적으로 알 수가 없다"). 브랜드 채택 수는 v3.0.0의 분석
  단위이고 **여전히 옳지만**, 카드 얼굴에서는 **슬래시 낀 두 숫자**였고 정작 "얼마나
  있나"(상품 수)는 작은 회색 줄이었다.
  - → 얼굴은 **① 순위 · 키워드 · `42 items` · 주간 막대**. 순위는 **상품 수 내림차순
    상위 10개**(`axisRows` 정렬을 products 우선으로). 브랜드 수는 **툴팁으로** 남는다
    — 한 몰이 60장 떨군 것과 시장이 움직인 것을 가르는 그 장치는 없앨 수 없다.
  - 막대는 **share가 아니라 개수**다(`counts`, 카드가 말하는 그 숫자를 주별로).
    수집이 없는 주는 **0이 아니라 빈칸**(회색 2px) — 안 본 주가 사라진 주로 읽히면 안 된다.
  - `shelf-e2e` 22건(수정 전 카드가 정확히 `3 /8 brands`를 얼굴에 찍고 막대가 0개).
- **`All lists`를 없애자 리스트에 안 든 행이 갈 곳이 없어졌다**(v3.45.0, 회귀).
  `fibreblocks-e2e`가 **LAB 전체가 빈 화면**으로 잡아냈다 — 픽스처 행에 `listIds`가
  없어서다. 실제로도 리스트가 생기기 전에 스캔한 행, 남의 백업을 합친 행이 그렇다.
  - → **`Unfiled` 칩**(엑셀이 이미 쓰는 그 서랍, v3.32.0). **들어 있을 때만** 나온다.
    기본 스코프는 **상품이 있는 첫 칩** — 빈 화면으로 열리면 데이터를 잃은 것처럼 보인다.
- **테스트 프로필 재사용이 또 답을 낡게 만들었다**: `databox-e2e`가 지난 실행의
  `wpb_lastbackup` 때문에 "한 번도 백업 안 함"을 못 봤고, `shortgrid-e2e`는 남은 실행
  기록 때문에 ▶가 잠들어 클릭이 타임아웃. 둘 다 프로필을 지우고 시작한다
  (selfupd·rescan에 이어 세 번째다 — **성공처럼 보이는 낡은 답**의 같은 얼굴).

- **한 화면이 같은 말을 세 번 하고 있었다 — 그리고 FABRIC 축이 두 어휘로 세고 있었다**
  (v3.44.0, 사용자 요청 6건).
  - **날짜는 한 번만**: `2026-W34 : 8/17-8/23` 키커 · `WEEK 2026-W34` 태그 · 주차 칩
    셋이 같은 주를 말했다. **칩만 남긴다** — 그게 컨트롤이기도 하다. 날짜 범위는
    **열린 칩 안에** 들어간다.
  - **LAB의 FABRIC 축도 15개 섬유로 센다**(사용자 요청). 전에는 `fabricfam`이라
    화면에 **RIBBED · TERRY · RIB · HEATHER · JERSEY**가 COTTON·NYLON 옆에 있었다 —
    **한 축에 두 어휘라 더할 수가 없다**. 이제 레일과 같은 `fibreFamilies`이고, 카드
    밑의 조성 한 줄(`blends`)도 같은 축을 쓴다. **조성이 없는 행은 이 축에 안 나온다**
    (상품명의 조직 이름으로 대신 세지 않는다 — 그게 두 어휘의 출처였다).
  - **New In과 CLOTHING은 필터를 각자 갖는다**. 한 벌을 공유해서 New In에서 브랜드를
    고르면 CLOTHING도 조용히 좁혀졌고, 그 화면은 **전체인 척하는 일부**였다.
  - **By Brand → CLOTHING**(탭 이름), 그 안의 `By Brand` 큰 제목 제거 — 탭이 이미
    그 이름이다. **LAB 탭 → HOME**: 왼쪽 위 마크가 이미 LAB이라 같은 단어가 그 아래
    또 있으면 다른 제품처럼 읽힌다.
  - **`All lists` 제거**. 리스트 하나가 리서치 질문 하나인데(v1.71) 그걸 합친 화면이
    **기본값**이었다 — 대부분의 사람이 본 것이 그 합산이다. 스코프는 항상 리스트 하나.
  - 축 카드의 숫자에 **툴팁으로 문장을 달았다**(`2 /6 brands` = "이번 주에 뭔가 낸 6개
    몰 중 2곳이 그걸 했다 · 60장을 떨군 몰도 한 번만 센다").
  - `rail-e2e` 41건(수정 전 8건 실패) · `shelf-e2e` 17건(수정 전 FABRIC 축에 정확히
    `Ribbed · Waffle · Terry · Jersey`가 찍히는 것으로 재현) · `weekbucket-e2e` 11건.

- **Gymshark의 조성이 빈칸이던 이유는 태그가 사라진 자리였다**(v3.43.0, 사용자가
  상품 주소와 위치를 지목: Description 탭 → **MATERIALS & CARE** → `100% Nylon`).
  픽스처로 그 페이지 모양을 세우자 **재현됐다**: `100% Nylon`·`100% Cotton`은 **빈칸**,
  `92% Nylon, 8% Elastane`은 **`92% Nylon`만**.
  - 원인은 Shopify 어댑터의 **자체 PDP 리더 사본**(`compFromPdp`)이 마지막에
    `doc.body.textContent`를 읽은 것이다. 텍스트만 남으면 **줄 경계가 없어서**
    `<li>100% Nylon</li><li>Machine wash cold</li>` → **`100% NylonMachine wash cold`**.
    `\bnylon\b`이 성립하지 않아 섬유 이름이 아예 안 잡힌다. v2.8.0의 `New In48 items`와
    **같은 실패**이고 답도 같다 — **그 말을 하는 가장 작은 요소에서 읽는다.**
  - → 그 사본을 지우고 **공용 리더**(`shared.readProductPage`)를 부른다. 거긴 이미
    **`li`를 각자 줄로** 읽는 단계가 있다(그래서 generic·walmart는 멀쩡했다).
    **사본은 낡는다**는 규칙이 또 한 번 확인됐다. cottonon 리더도 같은 모양이라
    `li` 단계를 넣었다.
  - `gymshark-e2e` 12건 — 벌크 JSON에는 **마케팅 문구만**(조성 없음), 조성은 **탭
    패널(hidden) 안 MATERIALS & CARE**에만. 위에는 "stretchy **material**, great for
    all kinds of training", 아래에는 "Machine wash cold" — 셋 다 오답 후보다.
    수정 전 코드에서 3건 실패.
  - **확인 못 한 것**: 실제 gymshark.com. 이 환경은 라이브 몰에 못 간다 — 다음 스캔의
    원단 열이 답한다.

- **필터는 왼쪽에서만 말한다 · FABRIC은 15개 섬유뿐 · 주차 줄은 `2026-W34 : 8/17-8/23`**
  (v3.43.0, 사용자 요청 3건).
  - 레일에서 값을 고르면 **체크만 되고** 피드 가운데 `Filtered to …` 줄은 **없앴다**.
    v3.3.0이 그 줄을 세운 이유(좁혀진 화면은 좁혀졌다고 말해야 한다)는 **레일 자신이**
    지킨다 — 결과 옆에 서 있고, 좁히는 그룹마다 `clear N`이 붙고, **체크가 곧 상태**다.
  - **FABRIC 그룹은 조성에서 읽는다**(`fibreFamilies`). `fabricfam`은 상품명의 조직
    이름을 먼저 답하므로 레일에 **Bonded · Brushed · Heather · Waffle**이 Cotton·Nylon
    옆에 섞여 있었다 — **한 필터에 두 어휘**. 조직 읽기는 LAB의 FABRIC 축에 그대로 있다.
  - 키커 줄은 `2026-W34 : 8/17-8/23`. 날짜는 **로케일을 쓰지 않는다** — 브라우저 설정
    때문에 영어 화면에 `8월 17일`이 찍히고 있었다.
  - `rail-e2e` 31건(수정 전 4건 실패).

- **한 주는 한 더미다 — 일자 탭이 아니라 2026-W34**(v3.42.0, 사용자 요청 "월요일에
  스캔하고 화요일·수요일에 추가 스캔할 수 있으니 주 단위로 합치자"). 피드가 **수집된
  날**로 갈라서, 같은 브랜드가 두 머리글에 나오고 **한 주가 반쪽 두 개**로 읽혔다.
  실제 작업은 한 주 안의 여러 번의 앉음이다.
  - 주는 이미 **월요일 시작**이었다(`bucketStart`) — 바꾼 것은 **이름과 묶음**이다:
    칩이 `08/17` → **`2026-W34`**(주간 스냅샷·엑셀·LAB이 쓰는 그 id), 주 안의 일자
    구획 제거(브랜드 → 카드). 키커 줄이 **그 주의 날짜 범위**를 적는다(`Aug 18 – Aug 24 ·
    Mon–Sun`) — 기록은 W34로 말하고 사람은 그렇게 말하지 않는다.
  - **중복은 구조적으로 없다**: 상품 키가 주소라 수요일의 재스캔은 **같은 행을 갱신**하고
    `addedAt`(첫 관측)은 그대로다 — 그게 그 상품이 **한 주에만** 속하는 이유다.
  - `weekbucket-e2e` 10건 — 월·수·일 세 번의 앉음 + 지난주 상품 하나 + 같은 페이지
    두 번 스캔. 수정 전 코드에서 4건 실패(`["08/10","08/03"]` · 일자 머리글 3개 ·
    `["COS","COS","ALO","ALO"]`).

- **지난주 답을 다시 쓰되, 온전할 때만**(v3.42.0, 사용자 요청 "전주 데이터로 시간을
  줄이자 · 다만 정보가 다 안 오면 그 방법은 쓰지 말자"). 재사용 자체는 v3.30.0에 있었고
  기준이 **조성 하나**였다 — 그래서 **조성은 있는데 사진이 없는 행**이 그대로 재사용되고,
  상품 페이지는 열리지 않고, **사진은 계속 비어 있었다**(VUORI의 모양 그대로).
  - → 재사용은 **이 단계가 존재하는 두 열이 다 있을 때만**: 조성 **그리고** 사진
    (타일이 준 것이든 저장된 것이든). 하나라도 없으면 페이지를 연다.
    **다 가져오는 것이 시간보다 우선**이라는 사용자 지시 그대로다.
  - `rescan-e2e` 14건 — 사진 없는 신선한 행이 **다시 읽히는지**까지(수정 전 코드에서
    8행이 사진 없이 남는 것으로 재현). 프로필을 지우고 시작한다 — 안 그러면 첫 스캔이
    지난 실행의 카탈로그를 재사용해서 **0회 요청**으로 측정된다(성공처럼 보이는 낡은 답).

- **한 질문에 필터는 하나 — 그리고 브라우징에서 개수를 뺐다**(v3.41.0, 사용자 요청
  "왼쪽에 브랜드창이 있으니 가운데엔 주차별 아이콘만" · "전체 탭에 상품 스캔한 수량정보는
  빼자"). 피드 한가운데에 **브랜드 필터가 하나 더** 있었다 — 왼쪽 레일에서 3인치 거리다.
  같은 질문에 컨트롤이 둘이면 서로 어긋나고, **화면이 지금 무엇으로 좁혀졌는지 말하지
  못한다**. By Brand의 카테고리 칩도 같은 이유로 제거. 피드 위에 남는 칩은 **주차뿐**이고,
  그게 이 탭이 존재하는 이유다.
  - **수량은 브라우징 크롬에서 전부 뺐다**: 주차 칩(`08/17 · 401` → `08/17`) · 일자
    머리글 · 브랜드 머리글 · 브랜드 열 · 리스트 칩 · Tier 칩 · 키커 줄. **세는 일은
    LAB의 일**이고 LAB은 그걸 브랜드 단위로 제대로 한다 — 여기서는 "어느 주 · 어느
    브랜드 · 어느 리스트"라는 의미 위에 얹힌 장식이었다.
  - **좁혀졌다는 문장은 남긴다**(v3.3.0 규칙). 다만 숫자 대신 **이름으로** 말한다
    (`Filtered to adanola · Satin · Clear all`) — 부분을 전체로 읽는 사고를 막는 것이
    그 줄의 임무이고, 그건 숫자 없이도 된다.
  - 정확한 수는 **툴팁에 그대로**(리스트 칩) — 비용 0.
  - `rail-e2e` 28건(수정 전 코드에서 7건 실패).

- **LAB은 탭 셋이다 — 그리고 레일은 몰의 필터처럼 생겼다**(v3.40.0, 사용자 요청 4건 +
  몰 필터 스크린샷). 리포트 화면에 남의 가구가 자라 있었다.
  - **Products · Scan lists 탭 제거.** 사진 벽은 **사이드 패널의 PRODUCTS**에 있고
    (리스트 옆이 제자리다), 담고 스캔하는 일은 처음부터 패널의 일이다.
  - **같이 보낼 수 없는 것 둘**: 백업 문(카탈로그가 다른 노트북으로 가는 **유일한 길**,
    v1.83.0)과 사이트 등급(반쪽 스캔이 스스로 ✅가 되는 것을 막는 **유일한 장치**,
    v1.95.0). → **LAB 맨 아래**(각주 뒤, 헤어라인 아래)로. v3.33.0이 "제목 줄에서
    치워라"고 한 그 요구는 그대로 지킨다 — **숫자 위가 아니라 아래**다.
  - **레일은 상자가 아니다**(사용자가 몰 필터 화면을 지목): 테두리 없는 열, 줄(rule) ·
    자간 넓은 대문자 한 단어 · **오른쪽 끝의 꺾쇠**. 그룹 옆 숫자도 없앴다 — 값은
    **다른 그룹을 적용한 뒤** 세므로 뒤에 아무것도 없는 값은 애초에 안 나오고, 숫자는
    그 사실을 반복하고 있었다(v3.29.0에 값 옆 숫자를 없앤 것과 같은 이유).
  - **SILHOUETTE · FIT · DETAIL은 레일에서 뺐다**(사용자 요청). 상품명의 단어에서
    읽으므로 값이 수백 개다 — 사용자 화면의 **DETAIL이 1,172개**였다. **답이 천 개인
    필터는 필터가 아니라 목록**이다. 그 축들은 LAB의 축 카드에 그대로 있다.
  - **순서는 묻는 순서다**: BRAND · CATEGORY · FABRIC · COLOUR.
    By Brand 탭에서는 옆의 브랜드 열이 그 선택기이므로 BRAND 그룹이 빠진다(v3.3.0 유지).
  - `#v-products`·`#v-lists` **마크업과 코드는 남긴다**(숨김) — 리포트·엑셀이 쓰는
    `visible()`이 그 줄의 필터를 읽는다. 다만 **보이지 않는 그리드는 그리지 않는다**
    (300장 카드를 매 렌더마다 만들 이유가 없다).
  - `rail-e2e` 19건(수정 전 코드에서 6건 실패 후 하니스 정지 — LAB에 없는 Data 칩을
    누르려다 타임아웃). `databox-e2e`·`labtools-e2e`는 새 자리로 갱신.

- **몰이 진열한 순서를 스캔과 화면 사이에서 버리고 있었다**(v3.39.0, 사용자 요청
  "실제 URL 내 상품 순서대로 정렬"). 카테고리 페이지는 **나열이 아니라 진열**이다 —
  첫 타일은 MD가 첫 번째로 둔 것이고, 그 순서를 읽는 것이 디자이너가 그 페이지에서
  하는 일의 절반이다. 실행은 그 순서를 배열로 갖고 있었지만 **DB는 순서를 갖지
  않는다** → 카탈로그에서 만든 모든 화면이 수집시각순(또는 아무 순서)이었다.
  - → 스캔이 **자리를 적는다**(`pos`, `j.items`에 넣는 그 자리). 재스캔하면 다시
    찍힌다 — 의미 있는 자리는 **오늘 몰이 보여주는 자리**이므로(가격과 같은 규칙).
  - 되돌리는 정렬은 **브랜드 → 카테고리 → pos**, 엑셀이 원래 갖던 그 모양이다.
    PRODUCTS 정렬 셀렉터에 **`Site order`가 생기고 그게 기본값**, New In의 브랜드
    블록과 By Brand 목록도 같은 순서. **LAB에서 만든 엑셀도** 이걸로 정렬한다 —
    실행 직후 만든 엑셀은 배열 순서 + stable sort로 공짜였지만 카탈로그에서 만든
    것은 아니었다. `pos`가 없는 옛 행은 그룹의 **뒤로** 간다(앞이 아니라).
  - **LAB에서 가격 표를 뺐다**(사용자 요청). 시즌이 무엇으로 만들어졌나와 얼마인가는
    다른 질문이고, 두 번째 표가 축과 기록 사이에 앉아 있었다. 계산은 `trend.js`에
    그대로, **내보낸 대시보드는 자기 가격 분포를 계속 그린다**, `priceBoard` 렌더러도
    남긴다 — 되돌리는 것이 배선이 되도록(v3.35.0의 교훈).
  - `siteorder-e2e` 14건(수정 전 코드에서 가격 2건 + 순서 4건 실패) · `goal-e2e` 23건
    (실제 스캔이 `tee-0…tee-4`를 그 순서로 적는지).

- **몰의 판매용 이름을 그대로 세고 있었다 — 색상 축에 색이 없었다**(v3.38.0, 사용자가
  몰의 컬러 필터 스크린샷과 섬유 15개 목록을 주며 "이 키워드로 필터링하자").
  컬러웨이는 **파는 이름**이다(`Deep Sea Navy` · `Off-White` · `Heather Grey Marl`),
  조성은 **쓰는 이름**이다(`Recycled Polyester` · `Organic Cotton` · `BCI Cotton`).
  그대로 세면 색상 축은 **한 번씩만 보이는 마케팅 문구 200개**가 되고, 한 섬유가
  **네 줄로 쪼개져 넷 다 작아 보인다.** 실측(상품 4개): **색 6줄 · 섬유 6줄** →
  수정 후 **4줄 · 3줄**.
  - → **선반(shelf) 두 개**. 색 12개(사용자가 준 몰 필터 그대로: Beige · Black ·
    Blue · Brown · Green · Grey · Orange · Pink · Purple · Red · White · Yellow)와
    섬유 15개(Polyester · Cotton · **Elastane/Spandex** · Nylon · Viscose ·
    Polyamide · Silk · Linen · Acrylic · Rayon · Wool · Tencel · Polyurethane ·
    Cupro · Acetate). 조직·핏 어휘와 같은 이유의 **닫힌 목록**이다.
  - **영어 색 이름의 머리는 마지막 단어다**: `Deep Sea Navy`→Blue · `Off-White`→White ·
    `Washed Vintage Olive Green`→Green. 앞의 수식어는 처음 보는 것이어도 된다.
    **색을 하나도 안 부르는 컬러웨이**(`Multi`·`Camo Print`)는 **아무 칸에도 안
    넣는다** — 틀린 칸이 빈 칸보다 나쁘다.
  - **COTTON은 부분일치**(사용자 지시): organic·recycled·BCI·Supima·cotton twill 전부
    Cotton. 나머지는 **사용자가 준 순서대로 먼저 맞는 것**이 이긴다(`Viscose Rayon`→
    Viscose · `Polyamide (Nylon)`→Nylon). Viscose와 Rayon, Nylon과 Polyamide는
    **사용자가 따로 적었으므로 합치지 않는다.** Lycra→Elastane/Spandex, Lyocell→Tencel,
    Merino→Wool.
  - **선반에 없으면 그 축에 없다** — Modal·Cashmere·Hemp는 **수집·PRODUCTS·엑셀에는
    그대로 있고**(엑셀은 몰이 쓴 글자 그대로라는 규칙 불변) 섬유 축에만 안 나온다.
    `fabricfam`은 **가장 큰 비율의 섬유가 선반에 있을 때만** 답한다 — 95% 모달 5%
    엘라스테인을 "엘라스테인 옷"이라고 부르지 않는다.
  - **얼린 옛 주간 기록도 읽을 때 선반을 통과한다**(브랜드·카테고리 수리와 같은 규칙,
    v3.6.0). 안 그러면 한 차트가 두 어휘를 동시에 그려서 옛 주가 붕괴로 보인다.
    **상품 수는 더하고 브랜드 수는 최댓값**을 쓴다 — Navy와 Deep Sea Navy를 낸 몰은
    한 몰이라, 더하면 로스터보다 큰 브랜드 수가 나온다.
  - **색은 색으로 보여준다**: LAB 축 카드와 브라우징 레일에 **잉크 점**(`COLOUR_INK`,
    선반 옆에 한 벌만 정의). 몰의 컬러 필터가 그렇게 생긴 이유와 같다 — 목록에서
    눈은 단어보다 색을 먼저 찾는다. 잉크는 **라벨이지 측정값이 아니다**(아무것도
    읽지 않는다).
  - **사진에서 읽는 색도 같은 12개로 접힌다**(`colour.js`의 `shelfOf` — Navy→Blue ·
    Cream→White · Light grey→Grey). 픽셀 판독은 여전히 세밀하게 읽되(스와치를 보여줄
    자리를 위해) **축은 한 어휘**다 — 아니면 같은 옷이 한 색의 두 이름으로 두 번 센다.
  - `shelf-test` 63건 · `shelf-e2e` 14건(수정 전 코드에서 9건 실패 — 레일에 정확히
    `Deep Sea Navy` · `Heather Grey Marl` · `Camo Print`가 찍힌다).
    `fibreblocks-e2e`의 `Lyocell` 기대는 **새 계약(Tencel)으로 갱신**.

- **폴더에 든 것이 가장 새것이라고 가정하고 있었다**(v3.37.0, 사용자 화면 — 칩은
  `v3.33.0 → 3.36.0`인데 바로 아래 배너는 `Version 3.35.0 is in your folder, ready
  to run`). 한 화면의 두 줄이 서로 다른 버전을 말하고, **버튼을 누르면 이미 뒤처진
  3.35.0으로 재시작한다** — 원클릭이 최신에 영영 못 닿는다.
  - `paintUpdateBanner`는 `wpb_filesready.onDisk`가 있으면 **깃허브를 묻지도 않고**
    재시작 배너를 그렸다. 폴더에 뭔가 들어 있다는 사실과 그게 최신이라는 사실은
    다른 것인데, 예전에 끝난 설치 하나가 그 둘을 같은 것으로 만들었다.
  - → **둘 중 새것이 이긴다**: 깃허브가 디스크보다 앞서면 이것은 **설치**이고
    `⚡ Update now`가 나온다. 그리고 **폴더가 지금 뭘 들고 있는지 적는다**
    (`Your folder is holding v3.35.0 — this replaces it.`) — 숫자가 갑자기 바뀌는
    것이 아니라 설명된 채로 바뀌게. 업데이트 상자의 아래 줄도 같은 판정을 쓴다.
  - 순서 비교는 **세그먼트 단위 숫자**다(`verCmp`) — 문자열 비교는 3.9 > 3.36이라고
    말한다. 패널은 `update.js`를 싣지 않으므로 그 규칙 한 벌이 여기에 산다.
  - `updnote-e2e` 26건 — 폴더 상태는 **워커의 디스크 검사를 통해** 세운다(레코드를
    몰래 써 두면 패널을 여는 순간 워커가 바로잡아서, 시험하는 것이 사라진다).
    수정 전 코드에서 3건 실패.
  - **`selfupd-e2e`가 3건 실패하고 있었는데 코드가 아니라 프로필 때문이었다**:
    `/tmp/pw-selfupd`를 재사용하니 지난 실행의 `wpb_autotried`가 자동 설치를 거절하고,
    OPFS에 남은 지난 실행의 파일들이 "폴더에 잘 써졌다"를 통과시켰다. **성공처럼
    보이는 낡은 답**이 테스트 안에도 있었다. 프로필을 지우고 돌리면 20/20 —
    자동 설치 체인 자체는 멀쩡하다.

- **끝난 실행이 화면에서 안 끝났다 — `20/11 ·`**(v3.37.0, 사용자 질문 "저 숫자
  정보는 무엇인지"). `queue.idx + 1` / 리스트 길이, 즉 **11개짜리 리스트의 20번째**다.
  있을 수 없는 자리이고, 이름 자리는 비어 있어 가운뎃점만 남는다.
  - 원인은 v3.36.0이 워커에서 고친 것과 **같은 죽은 레코드**다: 패널은 `queue.active`
    한 장만 보고 있어서, 탭을 닫거나 브라우저를 끄거나 몰이 멈춘 실행이 **영원히
    도는 중**이었다 — ▶는 잠든 채, 진행 줄은 뜬 채.
  - → `queueLive()`: **최근에 쓴 적이 있어야 살아 있다**(`at` 하트비트, v1.93.0).
    **4분**은 워커 워치독·재시작 가드가 이미 쓰는 같은 숫자다. 홀드 중은 예외
    (일부러 멈춘 것이지 막힌 것이 아니다).
  - 카운터도 고쳤다: **리스트 끝을 지나면 말로 적는다**(`Finishing…`). 그 자리에
    숫자를 찍으면 참일 수 없는 숫자가 나온다.
  - `tabs-e2e` 66건 — 30분 조용한 실행 · 끝을 지난 살아 있는 실행 둘 다. 수정 전
    코드가 화면에 정확히 **`20/11  ·`**을 그리는 것으로 재현(사용자 화면과 같은 문자열).

- **끝나지 않은 옛 실행 하나가 재시작을 영영 막고 있었다**(v3.36.0, 사용자 화면
  `v3.35.0 written into market-lens · 24 files. A scan is running…`). 설치는
  **성공했다** — 막힌 것은 재시작이고, 이유는 **"스캔이 도는 중"**이었다.
  - 판정이 `queue.active` **한 장뿐**이었다. 그 플래그는 탭을 닫아도, 브라우저를
    꺼도, 몰이 멈춰도 그대로 남는다 → 그 뒤로 **모든 업데이트의 재시작이 막힌다.**
  - 실행의 모든 쓰기는 **하트비트(`at`)를 찍고**(v1.93.0) 워커의 워치독은 **4분**
    조용하면 "바쁜 게 아니라 막힌 것"으로 읽는다. 같은 숫자를 여기서도 쓴다 —
    그만큼 조용한 것은 남은 기록이고, 그걸 위해 재시작을 미루면 지키는 게 없다.
  - **그리고 배너가 시작할 때 한 번만 그려졌다.** 설치가 끝나 파일이 폴더에 들어간
    뒤에도 계속 "새 버전이 있습니다 — 한 번 누르면 설치됩니다"라고 말해서, **정작
    남은 일(재시작)은 한 번도 제안하지 않았다.** 누르면 또 설치되고 또 같은 문장이
    나온다 — 사용자가 본 그 화면이다. 이제 `wpb_filesready`가 바뀌면 다시 그리고,
    설치 직후에는 워커에 즉시 폴더를 보게 해서 그 자리에서 뒤집힌다.
  - `selfupd-e2e` 20건 — **움직이는 실행은 여전히 안 건드리고**(하트비트가 지금),
    **30분 조용한 실행은 못 막는다**. 수정 전 코드에서 뒤엣것 실패.

- **섬유로 읽는 창(窓)이 LAB에 돌아왔다**(v3.35.0, 사용자 요청 — 옛 대시보드 화면을
  첨부하며 "LAB 아이콘을 누르면 이 시각자료가 나오도록"). v3.0.0이 "축 넷이 같은
  답을 한다"며 걷어낸 다섯 블록 — 워치 카드 · 빈도 랭킹 · 유입량 막대 · share 선 ·
  상승/하락 쌍 — 인데, **옆 질문에 답하는 것이었다**.
  - 축은 `fabricfam`, 즉 **몰이 이름으로 부르는 조직**(satin·poplin·jersey)을 브랜드
    수로 센다. 이 블록들은 `fabric`, 즉 **조성이 말하는 섬유**를 센다 — 몰이 뭐라
    부르든 퍼센트는 코튼이라고 말하는, 측정된 쪽이다. 둘 다 참이고 서로를 대신하지
    못한다. 그래서 되돌린 것이지 취소한 것이 아니다.
  - **새 계산은 하나도 없다.** 전부 `trend.js`에 그대로 있었고 `pulse-test`·
    `compare-test`가 계속 지키고 있었다 — 빠져 있던 건 **그리는 배선**뿐이다.
    다만 `movers`의 렌더러는 같이 사라져서 두 칸짜리 `moversBoard`로 다시 썼다
    (상승과 하락은 한 읽기라 나란히 둔다 — 한쪽만으로는 읽히지 않는다).
  - 그리다가 결함 둘을 잡았다(v2.7.0 규칙): `unit`을 **선언 전에** 쓰고 있어
    LAB이 통째로 "The catalog did not open"이었고, 두 번째 편집이 그 선언을
    삼켰다. 화면에 그려 보지 않았으면 둘 다 배포됐다.
  - `fibreblocks-e2e` 13건 — 픽스처는 **실제로 움직이는 8주**다(코튼·폴리 초반,
    린넨·리오셀 후반). 매주 똑같이 도는 픽스처는 상승/하락이 비어서 **아무것도
    안 그린 채 통과한다**. 그리고 `putScan`은 `addedAt`을 스스로 찍으므로 픽스처가
    여러 주를 원하면 저장 뒤에 날짜를 심어야 한다 — 안 그러면 전부 한 주가 된다.
    수정 전 코드에서 5블록 전부 없음.

- **한 사이트만 다시 긁는다 — 줄마다 ↻**(v3.34.0, 사용자 요청 "원하는 사이트만
  SCRAPPING"). 재스캔에는 늘 이유가 있다: 한 몰이 바뀌었거나, 한 몰이 적게 왔거나,
  한 몰을 방금 고쳤거나. 그 하나를 보려고 **열두 개를 다시 도는 것이 오후 한나절**이다.
  - 실행은 원래 "이 배열을 걷는다"였고 그 배열을 채우는 건 리스트뿐이었다 →
    `startRun(entries, opts)` 하나로 묶고, ▶는 리스트 전체를, ↻는 **원소 하나짜리
    배열**을 넘긴다. 아래쪽은 하나도 안 바뀐다 — **실행은 여전히 리스트의 것**이라
    행이 같은 `listIds`로 들어가고 엑셀·스코프·LAB이 차이를 모른다.
  - 한 사이트는 **확인 상자를 띄우지 않는다**(작고 이름이 붙은 명확한 동작이고,
    그래야 권한 프롬프트에 쓸 제스처가 살아 있다). 여러 개일 때만 묻는다.
  - **이미 도는 중이면 거절한다** — 큐가 하나라서 두 실행이 서로를 뒤섞는다.
  - `tabs-e2e` 62건 — 보내는 메시지를 가로채 **엔트리가 정확히 하나이고 listId가
    그 리스트인지**까지 본다(픽스처 몰은 여기서 못 여니, 계약은 무엇을 보내는가다).
    수정 전 코드에서 ↻ 자체가 없어 클릭이 타임아웃.

- **살림살이는 스캔 탭으로 옮겼다 — 지운 게 아니다**(v3.33.0, 사용자 요청 "DATA와
  오류가 나오는 내용은 안 보이게"). LAB 제목 줄의 주황색 `7 need a look`은 **멀쩡한
  작업 위에 뜬 에러 배너로 읽히고**, Data 칩은 리포트 한가운데의 서류함이다. 둘 다
  시즌에 대한 사실이 아니다.
  - **지우지는 않았다.** Data는 카탈로그가 **다른 노트북으로 가는 유일한 길**이고
    (백업·병합, v1.94.0), 사이트 등급은 **반쪽 스캔이 스스로 ✅가 되는 것을 막는
    유일한 장치**다(v1.95.0). 없애면 각각 백업과 정확도를 잃는다.
  - → **Scan lists 탭 맨 위**로. 그 탭이 이미 "스캔이 무엇을 했나"를 답하는 자리다.
  - `labtools-e2e` 31건 — 헤더에 없고 · Scan lists에 있고 · **눌러서 백업 상자가 실제로
    열리는지**까지(문이 남아 있다는 것이 계약이다). `databox-e2e`는 옛 자리를 클릭하고
    있었으므로 새 계약으로 갱신.

- **엑셀은 리스트마다 탭 하나다**(v3.32.0, 사용자 요청 "fabric 따로, WMN 따로").
  리스트 하나가 리서치 질문 하나인데 한 시트에 전부 부어 놓으면 **어느 질문에도
  답하지 못한다** — FABRIC이 뭘 가져왔는지 보려면 WMN의 행을 손으로 다시 빼내야 한다.
  - `buildKnitWorkbook`에 `ctx.groups`(`{name, items}[])`. 없으면 종전대로 시트 하나.
    시트 이름은 **엑셀 규칙을 여기서 지킨다**(`[]:*?/\` 금지 · 31자 · 중복 시 번호) —
    파일이 안 열려서 알게 되는 대신.
  - **두 리스트에 다 든 상품은 두 탭에 다 나온다.** `listIds`가 합집합이고, 실제로 두
    질문 모두를 위해 담은 것이다. 한쪽에서 빼면 그 탭이 리스트를 잘못 말하게 된다.
  - 리스트에 안 든 행은 **`Unfiled` 탭**으로 — 사라지지 않는다.
  - **LAB 바에 EXCEL 버튼을 뒀다**(HTML 옆, 아웃라인). v3.29.0이 PRODUCTS 탭에서
    `Export Excel`을 뺐는데 그게 **LAB의 유일한 엑셀 출구**였다 — 파일은 LAB에서
    만든다는 같은 규칙 자리로 옮긴 것이다.
  - `labtools-e2e` 23건 — **만든 워크북을 다시 열어 탭 이름과 행 수를 읽는다**(다운로드가
    일어났는지가 아니라 파일 안에 무엇이 들었는지가 계약이다). 수정 전 코드에서 6건 실패.

- **PRODUCTS는 COLLECTOR에서 연 리스트를 보여준다**(v3.31.0, 사용자 요청 "리스트별로
  스크래핑된 상품 정보를 확인할 수 있도록"). 스코핑(`listFilter`)과 스코프 바는
  **이미 있었는데 거기에 닿는 길이 하나뿐**이었다 — 스캔이 끝난 뒤 결과 상자의 View
  버튼. 그래서 실제로는 사진 벽이 늘 **전 카탈로그**였고, 한 리서치 질문을 보는
  사람이 나머지 전부를 섞어서 봤다.
  - **새 컨트롤은 없다.** 위의 탭 스트립이 이미 리스트를 고르므로 PRODUCTS가 그걸
    따라간다(리스트를 바꾸면 벽이 같이 움직인다). LAB의 리스트 스코프(v1.71)와 같은
    규칙 — 리스트 하나가 리서치 질문 하나다.
  - 스코프 바는 **항상 그려진다**: `YOUNG WOMEN'S · 42` 또는 `All lists · 383`,
    그리고 한 번 누르면 둘 사이를 오간다. **가끔만 나타나는 바는 아무도 읽는 법을
    배우지 않는다.**
  - `tabs-e2e` 53건(수정 전 코드에서는 `#scopeclear`가 보이지 않아 클릭 자체가 실패 —
    바가 뜨지 않던 그 상태).
- **엣지 테스트의 전제가 실행 도중 무너지고 있었다**(v3.31.0). `delete
  chrome.sidePanel`만으로는 부족하다 — 크롬은 API 네임스페이스를 **지연 정의**하므로
  다음 접근에서 되살아나고, 그 뒤의 모든 검사는 **사이드 패널이 있는 브라우저**를 재고
  있었다. 이제 `defineProperty`로 undefined를 돌려주게 막는다.

- **이미 읽은 상품은 상품 페이지를 다시 열지 않는다**(v3.30.0, 사용자 제안 "동일한
  상품정보라면 PASS하는게 효율적으로 보여서"). 맞는 자리였다 — 상세 단계가
  **몰당 10.7초 중 7.8초**이고 그 대부분이 상품 페이지 응답 대기다. 주간 재스캔이면
  New In의 대부분이 지난주에 이미 읽은 것이고, **같은 상품 URL의 조성은 바뀌지 않는다.**
  - 실측(`rescan-e2e` 픽스처): 1회차 **상품 페이지 8회** → 2회차 **0회**.
  - **가격은 재사용하지 않는다.** 오늘 가격은 매 스캔 타일에서 오고, 세일 압력은
    LAB이 읽는 지표다 — 지난주 값을 들고 오면 시즌이 평평해 보인다.
  - **한 달이 지나면 다시 읽는다**(`FRESH_MS`). 몰의 수정도, **우리 파서의 개선도**
    그때 옛 행에 닿는다. 조성이 비어 있는 행은 **항상** 다시 읽는다 — 그게 fetch가
    존재하는 이유다.
  - 조회는 **키 단위 point lookup**(`store.getMany`)이다. 60행을 묻자고 전 테이블을
    읽으면 아끼려던 시간을 돌려주는 꼴이다.
  - **재스캔은 지우지 않는다**(기존 규칙 확인): 상품 키가 URL이라 같은 행이 갱신되고
    `addedAt`(첫 관측)은 유지되며 `seenCount`만 오른다. 빈 값은 절대 덮어쓰지 않는다.

- **필터 파라미터는 나열할 수 없다 — 모르는 것은 필터로 본다**(v3.29.0, 사용자 보고
  gymshark·varley·TALA "다 스크래핑되지 않는다"). v3.27.0의 `completeList`가
  **아는 필터를 나열**하는 방식이었는데, 테마마다 자기 파라미터를 만든다:
  Varley `?o_cat=Tops&o_cat=Sweatshirts`, Gymshark `?collections=t-shirts-tops`.
  둘 다 "필터 없음"으로 읽혀서 **컬렉션 전체를 채워 넣을 뻔했다** — 디자이너가
  일부러 걸러낸 것을 도로 넣는 일이고, **적게 나오는 것보다 나쁘다(틀렸는데 맞아
  보인다)**. → 목록을 뒤집었다: **상품 집합을 바꾸지 않는 것으로 알려진 것만**
  화이트리스트(`page`·`sort_by`·`utm_*`·`gclid`…), 나머지는 전부 필터.
- **누름 예산은 "안 되는 버튼"을 위한 것이지 되는 버튼을 위한 것이 아니다**(v3.29.0).
  8회 고정이라 **4개씩 여는 그리드는 42에서 멈췄다**(TALA 58개 중). 이제
  **자란 누름은 예산을 돌려받고**, 라운드 예산도 같이 돌려받는다(`bonus`, 최대 60) —
  라운드가 먼저 바닥나면 같은 반쪽 스캔이 다른 경로로 나기 때문. 끝은 그대로
  보장된다: 60 상한 · 하드 실링 40회 · bonus 상한.
- **개수를 안 적는 몰에서도 "덜 가져왔다"고 말한다**(v3.29.0, `stoppedWith`).
  멈춘 시점에 **Load more 버튼이 아직 화면에 있으면**(또는 다음 페이지 링크가 있으면)
  그건 페이지가 더 갖고 있다고 말한 것이다. 이게 없으면 "그게 전부였다"와 "예산이
  떨어졌다"를 구분할 방법이 없어 **둘 다 ✅로 채점된다.**
- **파일은 LAB에서 만든다**(v3.29.0, 사용자 요청). PRODUCTS 탭의 `Build report`·
  `Export Excel`·템플릿 셀렉터 제거 — 사진 벽은 **아무도 무언가를 발행하기로
  결정하지 않는 화면**이다. 대신 **LAB 필터 바 오른쪽 끝에 HTML 버튼**: 파일에 무엇이
  들어갈지 정하는 세 셀렉터 바로 다음 자리다. **핸들러를 복제하지 않고** 숨겨진
  `#report`를 누른다(사본은 낡는다).
- **레일의 값 옆 숫자 제거**(v3.29.0, 사용자 요청). 그 숫자의 임무(막다른 선택지를
  권하지 않기)는 **목록 자체가 이미 한다** — 개수는 다른 그룹을 적용한 뒤 세므로
  뒤에 아무것도 없는 값은 **아예 안 나온다.** 숫자는 그 사실을 40줄에 걸쳐 한 번씩
  반복하고 있었다.
- `shortgrid-e2e` 18건(테마 자체 패싯 2종 · 4개씩 여는 그리드 · 먹통 버튼 + 그 문장) ·
  `labtools-e2e` 16건.

- **⚡ Update now가 v3.21.0부터 아무에게도 동작하지 않았다 — 리다이렉트 한 홉**
  (v3.28.0, 사용자 화면 `Could not finish: Failed to fetch`). 실측한 체인:
  `github.com/.../releases/latest/download/market-lens.zip` → **302** →
  `github.com/.../releases/download/v3.27.0/...` → **302** →
  **`release-assets.githubusercontent.com/...`**.
  **확장의 fetch는 모든 홉에서 host_permissions에 걸린다.** 그 자산 호스트가 선언에
  없어서 두 번째 홉에서 죽었고, 화면에는 `Failed to fetch` 다섯 글자만 남았다.
  - 옛 주소(브랜치 아카이브)는 **`codeload.github.com`**으로 갔고 그건 선언돼 있었다.
    **v3.21.0이 "안쪽 폴더 함정"을 없애면서 링크를 옮긴 그 순간 원클릭이 죽었다** —
    그리고 그 릴리스부터 지금까지 아무도 자동 설치를 받지 못했다.
  - **테스트가 못 잡은 이유**: `selfupd-e2e`·`updatefetch-e2e`가 요청을 **route로
    가로채 직접 응답**한다 — 리다이렉트가 일어나지 않고, **라우팅은 오리진 검사를
    우회한다**(v1.90.0에 이미 적어 둔 사실). 채워 주는 테스트는 압축 해제를 검사하는
    것이지 다운로드를 검사하는 것이 아니다.
    → `assetfetch-e2e` 6건: **아무것도 라우팅하지 않는다.** 두 호스트를 리졸버로
    로컬 HTTPS 서버에 걸고 **진짜 302**를 내보내며, 패널의 그 버튼이 fetch한다.
    수정 전 매니페스트에서 **`Could not finish: Failed to fetch`** — 사용자 화면과
    같은 문장이 나온다.
  - 권한은 **`https://*.githubusercontent.com/*`** 하나로 선언한다(`raw.` 항목을 대체).
    GitHub은 이 자산 호스트를 **이미 한 번 옮겼고**(objects. → release-assets.),
    호스트가 옮겨지면 **팀 전원의 업데이트가 조용히 죽는다** — 그 실패 모드를 다시
    사지 않는다. 같은 도메인 안이고 우리 릴리스만 오간다.
  - **실패가 어디로 가다 죽었는지 말한다**(v1.99.0 규칙): `Failed to fetch`는 호스트
    이름과 "리다이렉트를 건넌다"는 사실을 달고 나온다.
- **새 버전이 나와도 6시간 동안 "최신입니다"라고 했다**(v3.28.0, 같은 화면
  `v3.26.0 is the latest`). 저장소는 3.27.0을 정확히 답하고 있었다(실측: raw 200).
  워커의 **6시간 캐시**가 배경 타이머용인데 **패널이 물을 때도 그 답**을 줬다 —
  **패널을 여는 것 자체가 질문**인데. 이제 패널의 모든 조회는 `force`다(수백 바이트,
  하루 몇 번). 캐시는 5분 알람에만 남는다. **성공처럼 보이는 낡은 답은 답이 없는
  것보다 나쁘다** — 점선 `v3.26.0 ?` 칩이 이미 따르는 규칙과 같다.

- **반쯤 긁고 ✅라고 말한 원인은 하나였다 — 몰이 개수를 안 적으면 구조가 없었다**
  (v3.27.0, 사용자 보고 "set active 4개만", "athleta 8개만"). v3.7.0이 세운 구조 —
  `Load more`를 누르고 그리드를 다시 훑는다 — 는 전부 **`said`(몰이 화면에 적어 둔
  "66 items")에 매달려 있었다.** 그 문장을 안 쓰는 테마에서는 `said=0` → `short=false`
  → **아무것도 누르지 않고 아무것도 다시 훑지 않는다.** 첫 화면이 곧 전부가 되고,
  `shortOf`도 안 찍히니 **✅로 채점된다.** 두 몰 다 이 모양이었다(픽스처 재현: 수정 전
  코드가 정확히 **4개**와 **8개**를 낸다 — 보고된 그 숫자).
  - **① 개수를 모를 때도 누른다**(`mayPress = said ? short : true`). 안전한 이유는
    따로 있다: 어휘가 **닫혀 있고**(Add to bag을 누르지 않는다), 누름 횟수가 8로
    막혀 있고, 60 상한이 끝을 보장하고, 눌러서 안 자라면 다시 안정으로 끝난다.
    **개수의 진짜 임무는 반대쪽 — 언제 멈출지**이고, 그건 그대로다(몰이 말한 수에
    도달하면 누르지 않는다).
  - **② Shopify는 개수를 적을 필요가 없다** — 컬렉션 JSON이 그 컬렉션에 무엇이
    들어 있는지 정확히 말한다(키 불필요, 조성 때문에 이미 받아 온다).
    **주소에 필터가 없으면**(지킬 사용자의 선택이 없으면) **몰의 목록이 곧 컬렉션**이고,
    그리드가 안 그린 것을 **그린 것 뒤에** 붙인다(머천다이저 순서가 앞에 남는다).
    `completeList` — **필터가 걸린 주소에서는 렌더된 페이지가 계속 결정한다**
    (`filter.*`·`q`·`tag`·해시 패싯·`/collections/<handle>/<tag>`). products.json은
    사용자가 고른 패싯을 모르므로, 거기서 채우면 **걸러낸 것을 도로 넣는 일**이 된다.
  - 가격은 JSON에 통화가 없다 → **그 페이지에서 실제로 렌더된 타일의 기호**를 쓰고,
    읽을 타일이 없으면 숫자만 둔다(몰이 쓴 것만 셀에 넣는다는 규칙).
  - `shortgrid-e2e` 13건 — 두 가지 모양을 픽스처로 세운다: **개수도 버튼도 없는
    Shopify**(4/14)와 **JSON이 없고 View More만 있는 몰**(8/20). 수정 전 코드에서 4건 실패.

- **기억된 폴더와 잠긴 폴더가 같은 얼굴을 하고 있었다**(v3.26.0, 사용자 보고 "3.23에서
  업데이트가 어렵고 매뉴얼대로 하라고 하네"). 자동 설치의 기계는 멀쩡했다 —
  **화면이 상태를 잘못 말한 것**이다.
  - 크롬은 디렉터리 핸들은 재시작을 넘겨 보관하지만 **그 위의 권한은 보관하지 않는다**.
    다음 날 `queryPermission`은 `"prompt"`이고, 클릭 안에서 확인받아야 다시 열린다.
  - 그래서 `knownFolder(false)`가 false가 되고 → `paintAuto`가 버튼을
    **`📂 Choose my Market Lens folder`**, 주석을 **"one time only: point at the
    folder Chrome loaded"**로 그렸다. **한 번만 하면 된다던 그 단계를 매일 다시
    하라고 말한 것**이고, 그러니 수동 절차로 읽힌다. (실측: 옛 코드에서 이 두 문구가
    그대로 나온다.)
  - 그리고 `maybeSelfUpdate`는 **아무 말 없이 false**를 반환했다 — "패널만 열면 알아서
    설치"라는 약속이 **두 번째 세션부터 조용히 깨져 있었다.**
  - → 상태는 **셋**이다: 폴더 없음 / **기억됨·잠김** / 기억됨·열림. 잠긴 폴더에서도
    버튼은 **⚡ Update now**다 — 핸들러가 **클릭 안에서** 권한을 묻고 그대로 설치까지
    가기 때문이다(누르면 실제로 끝난다는 것을 테스트가 확인). 열 때는 상자를 열고
    **"이 한 번을 누르면 된다"**고 적는다. 이 경로에서는 `AUTO_TRIED`를 **찍지 않는다**
    — 시도한 것이 없으므로, 찍으면 그 버전 동안 메시지가 사라진다.
  - 버전 칩도 `folderRemembered()`로 판단한다 — 진짜 클릭이라 합성 클릭이 활성화를
    물고 간다(v1.97.0이 약속한 "칩 한 번"이 잠긴 폴더에서도 성립).
  - **테스트가 이 상태를 한 번도 안 보고 있었다**: `selfupd-e2e`의 핸들은 OPFS라
    **항상 granted**였다. 이제 `FileSystemDirectoryHandle.prototype`을 잠가서
    재현한다 — 19건(수정 전 코드에서 4건 실패).

- **실행 바는 마크만 — 그리고 그중 하나는 그려지지도 않고 있었다**(v3.25.0, 사용자 요청
  "마지막 탭도 아이콘만으로"). 이름을 떼는 건 한 줄이지만, **떼는 순간 마크가 전부를
  말해야 한다** — 그래서 마크부터 쟀다.
  - **EXCEL 마크가 자기 몸통을 안 그리고 있었다**: `M12 3v10.2 … V3z`는 같은 x로
    내려갔다 올라오는 선이라 **면적이 0**이다. 화면에 남는 것은 화살촉과 밑줄뿐이라
    "저장"이 아니라 **"접기"로 읽혔다.** 밑에 EXCEL이라고 적혀 있어서 아무도 못 봤고,
    **그 글자를 떼는 것이 정확히 그 결함을 배포하는 일**이었다.
    → **종이 + 아래 화살표**로 다시 그렸다(운송 컨트롤 셋과 성격이 다른 하나 = 가져가는 파일).
  - **잉크 양을 측정해서 바닥을 정했다**(`glyph-probe`, 96px 래스터라이즈 후 불투명 픽셀
    비율): ▶ 12.7% · ⏸ 13.5% · ■ 16.9% · **옛 EXCEL 9.4%** · 새 EXCEL 20.7%.
    → 계약은 **12%**다 — 진짜 마크들 아래, 안 그려지던 것 위. 추측한 임계값이 아니다.
  - 이름은 **`title`·`aria-label`에 그대로** 있다(호버 한 번, 스크린리더 한 번 거리).
    실행 중이라는 사실은 헤더 위 2px 규칙·진행 바·잠든 ▶가 이미 말한다.
  - 박스는 62 → **50px**(글자 줄이 없어졌다), 마크는 21 → **24px**.
  - `tabs-e2e` 46건(수정 전 코드에서 5건 실패 — 네 개의 글자 + **9.4%**).
    `panelread-e2e`의 "글자가 10px 이상" 검사는 **사라진 것을 재고 있었으므로**
    같은 이유(이 줄이 읽혀야 한다)를 마크 크기로 옮겼다.

- **온도와 반경만 한 단계 부드럽게 — 구조는 그대로**(v3.24.0, 사용자 요청 "전체적인
  ui 색상과 감도를 모던하게"). 레퍼런스 이미지가 현재 화면과 같은 파일로 들어와
  방향이 없었으므로, **말로 고르지 않고 세 벌을 실제로 그려서**(`palette-probe`)
  판단했다. 채택: 순검정 #000 → **#151515**, 바탕 #f5f5f5 → **#f7f7f7**, 헤어라인
  #e0e0e0 → **#ececec**, 모서리 **0 → 10px/8px**, 액센트 러스트 #8a3c17 →
  **#ff5c35**(대시보드와 같은 주황 — 한 제품에 액센트는 하나).
  - **바탕은 여전히 무채색이다.** v1.98.0이 따뜻한 그레이지를 걷어낸 이유가 그대로다 —
    PRODUCTS는 옷 사진의 벽이고, **그 색을 판단하는 도구가 옆에 자기 색을 두면 안 된다.**
    따뜻한 종이 안(대시보드 팔레트 그대로)도 그려 봤고, 이 규칙 때문에 안 골랐다.
  - **팔레트인지 아닌지는 이걸로 검증된다**: 세 벌 전부 **토큰 오버라이드 한 장**으로
    그려졌고 새 규칙이 하나도 필요 없었다. 패널과 LAB이 같은 토큰이라 같이 움직인다.
  - 그리다가 결함 하나를 잡았다(v2.7.0 규칙 — 보고가 아니라 생성): `.tab`이 버튼
    기본값에서 `border-radius`를 물려받아, 반경이 생기자 **포커스 링이 탭 위에 더 큰
    둥근 상자를 하나 더 그렸다.** 밑줄은 밑줄이므로 `border-radius:0`.
  - `tests/labshot.js`로 LAB도 같이 본다 — 두 화면이 한 팔레트라는 것은 **둘 다 그려
    봐야** 지켜진다.

- **하우스 브랜드는 모회사 도메인에 산다**(v3.23.0, 사용자 보고 "athleta인데 gap으로
  나오네"). `athleta.gap.com`을 열었는데 Grab 카드가 브랜드를 **GAP**, 카테고리를
  **"WANT TO SHOP ATHLETA.COM?"**이라고 적었다. 둘 다 묶는 키라 **한 번 틀리면 그
  아래 숫자가 전부 무의미해진다** — 결함이 둘이었다.
  - **① 브랜드**: `brandFromHost`가 등록 가능 도메인(`gap`)을 썼다. 그러면 Athleta ·
    Banana Republic · Old Navy가 **엑셀 한 행, LAB 한 막대로 합쳐진다** — 서로 다른
    네 개의 몰인데. → 이제 **왼쪽 라벨이 이름이면 그것이 브랜드**다. "어디에 있나"를
    말하는 라벨(`shop`·`us`·`m`·`row`(rest of world)·`intl`…)은 **닫힌 목록**이고,
    2자 이하와 숫자도 로케일이다. `shop.lululemon.com` → Lululemon,
    `row.gymshark.com` → Gymshark는 그대로.
  - **② 카테고리**: 첫 `<h1>`을 카테고리로 읽는데, Gap 계열은 그리드 위에 **다른
    스토어로 보내는 인터스티셜**을 놓는다. 그 문장이 스캔 전 행의 카테고리가 됐다.
    → **카테고리는 명사구다 — 질문이 아니고, 웹 주소를 품지 않는다**(`notACategory`).
    몰 이름이 아니라 **모양**으로 거르므로 처음 보는 몰에도 적용되고, 걸리면 주소가
    쓴 이름(`all-new-arrivals`)으로 떨어진다.
  - 이미 담아 둔 항목의 브랜드는 저장된 값이라 바뀌지 않는다 — 다시 Grab하거나
    카드에서 고치면 된다. `naming-test` 24건(수정 전 코드에서 4건 실패 · `notACategory`
    자체가 없었음).
- **＋는 탭과 함께 스크롤하지 않는다**(v3.23.0, 사용자 보고 "추가 탭과 리스트가
  겹친다"). 헤드 전체가 가로 스크롤이라 리스트가 늘어나면 **＋가 탭 이름 위에 겹쳐
  인쇄됐다**. 이제 **스트립만 스크롤하고 ＋는 그 오른쪽에 고정**이다(브라우저가 새 탭
  버튼을 늘 같은 자리에 두는 이유 — 새 리스트를 만들 유일한 길이 밀려나면 안 된다).
  v3.13.0이 "스트립 밖에 고정"이라고 적어 뒀는데, `overflow`가 헤드에 걸려 있어
  **실제로는 안에 있었다.**
- **Filter sites 제거**(v3.23.0, 사용자 지적 "필요성은 없어 보인다"). 리스트는 브랜드로
  접힌 열 몇 줄이고, 위의 탭 스트립이 이미 어느 리스트인지 고른다 — 350px 한 줄에
  아무도 안 하는 검색을 위한 두 번째 입력 상자였다. 필드는 **빈 채로 DOM에 남는다**
  (렌더가 그 값을 읽으므로 지우면 죽는다). Collapse와 개수는 그대로.
- **Grab 버튼의 숫자 배지 제거**(v3.23.0, 사용자 요청 "숫자는 없애자"). 기본 리스트에
  담긴 URL 개수였는데, **쇼핑몰 페이지에 서 있는 사람에게는 아무 뜻이 없고** 빨간 원이
  경고처럼 읽혔다. 그 숫자는 패널의, 자기 리스트 옆에 있다.
- **PRODUCTS의 검색·BRAND·CATEGORY는 고정된다**(v3.23.0, 사용자 요청). 사진 벽을
  스크롤하는 동안 **지금 무엇을 보고 있는지**가 화면에서 사라졌다 — 첫 줄과 함께
  올라갔기 때문. `position:sticky` + 불투명 배경(사진이 비쳐서는 안 된다).
- `tabs-e2e` 21건 — **화면에 그려진 좌표로** 잰다(스크롤러 안의 탭은 잘려도 rect가
  남으므로 **스트립 프레임과 교차시킨 보이는 부분**으로 판정). 수정 전 코드에서 8건 실패.
- 수집: **현재 페이지만** (기본값). 카테고리 전체를 훑으면 리서치 한 번에 쓸 수
  없는 양이 나오고, 목적은 "고른 카테고리들의 리스트"이지 전수 덤프가 아니다.
  무한 스크롤 그리드는 스크롤로 다 펼쳐지는 그 한 페이지가 대상.
  전 페이지 순회 코드는 남아 있고 `singlePage:false`로 켤 수 있다.
- 엔진 라우팅: `sites.js`의 어댑터 레지스트리 `[walmart, target, cottonon, zara, aritzia, cos, massimodutti, shopify, generic]`
  - walmart: 커스텀 SPA 엔진 (embedded JSON 컨테이너 스코핑 + 상세 fetch)
  - target: 커스텀 SPA (?Nao=24 오프셋 페이지네이션, JSON-LD·불릿 상세, multiBrand)
  - shopify: **플랫폼 감지형** (페이지 내 CDN 마커) — Edikted 등 모든 Shopify 몰.
    **`lazyScroll` 필수**: 요즘 테마는 스크롤해야 타일이 렌더되어, 없으면 첫 화면
    8개만 긁고 끝난다(Edikted가 안 긁히던 원인 ①).
    상세는 **컬렉션 벌크 JSON**(`/collections/<handle>/products.json?limit=250`)
    한 번으로 전부 채우고, 거기 없는 것만 PDP `.js`로 폴백. 키 불필요.
    vendor·product_type·전체 옵션값(색상)·compare_at_price·published_at 제공.
    **수집 대상은 여전히 렌더된 페이지가 결정**한다(사용자가 건 스토어 필터 보존) —
    벌크 JSON은 handle로 매칭하는 보강용.
    **벌크 hit은 조성의 종점이 아니다**(v1.68): body_html에 조성이 없으면
    (Edikted의 FABRIC 아코디언, Alo처럼 description 밖에 있는 몰) **PDP HTML까지
    간다** — JSON-LD material/description → 본문 텍스트, 전부 섬유 검증 파서 경유.
    벌크가 답한 조성 없는 행 전체가 빨강이던 원인
  - cottonon: SFCC(Demandware) — URL 슬러그/cgid + PDP variationAttributes.
    PDP 할인가(정가/세일가) 추출 → Current Price 반영
  - zara: 단일 브랜드 SPA (무한 스크롤 lazy-scroll 60라운드, -p코드.html 상품, JSON-LD 상세)
  - **house-brand의 상품 URL 패턴은 좁히는 용도이지 지우는 용도가 아니다**(v1.77).
    Aritzia는 `isProduct`가 `/product/`를 요구했는데 몰이 주소 형태를 바꾸자
    **그리드를 다 읽고도 전부 버려서 0개**가 됐다(엑셀 없음, LAB 없음, sitecheck만).
    이제 필터가 아무것도 안 남기면 **generic이 찾은 타일을 그대로 쓴다** — 살짝
    노이즈가 낀 행은 복구 가능하지만, 설명 없는 빈 엑셀은 아니다. `aritzia-e2e` 12건.
  - cos / massimodutti: **house-brand 팩토리**(`houseBrandAdapter`) — zara 모델 일반화.
    단일 브랜드 + 무한 스크롤 그리드, 카테고리=URL 슬러그(JSON-LD 브레드크럼 우선).
    사이트별 CSS 셀렉터 하드코딩 없음. 이미지 CDN/상품 URL 패턴은 사용자 진단
    (diagnose-generic.js) 출력으로 보정
    - cos 상세=JSON-LD + 섬유% 텍스트(기본 PDP fetch)
    - massimodutti 상세=**Inditex 카탈로그 API**(`itxrest/…/productsArray`, brandId 3).
      PDP HTML엔 조성이 없음 — API의 `bundleProductSummaries[].detail`에서 조성·색상·
      사이즈·세일가를 구조화 JSON으로 직접 수집. store/catalog는 페이지에서 발견
      (하드코딩 아님). list 스크랩은 DOM 그대로, 상세만 API로 교체. `inditex` 헬퍼
  - generic: DOM 휴리스틱 + JSON-LD 병합 폴백. **상세도 한다**(v1.69) — 아래 참조
- **몰을 하나 뚫으면 그 몰의 모든 카테고리가 열린다**(v1.92.0, 사용자 질문 "해당
  브랜드의 다른 카테고리도 가능한거지?"). 세 층 전부가 **주소가 아니라 몰 단위**로
  판단하기 때문이다: ① 권한은 오리진(`https://*.gymshark.com/*`) ② 엔진은 **페이지
  안의 마커**(Shopify) 또는 구조 읽기(generic)이지 URL 패턴이 아님 ③ 버튼은 v1.91.0
  이후 경로와 무관. 그래서 **한 카테고리가 되면 나머지도 된다** — 예외는 house-brand
  어댑터의 상품 URL 패턴뿐이고, 그건 v1.77 이후 아무것도 안 남기면 generic으로 되돌아간다.
  - 액티브웨어 8곳 추가 검증(2026-08, 사용자 목록): VUORI · adidas · adanola · TALA ·
    Varley · Gymshark · Set Active · Athleta. **매니페스트에 없던 건 TALA 하나**였고
    나머지는 `*.brand.com/*`이라 `row.gymshark.com`·`athleta.gap.com` 같은
    서브도메인도 이미 포함. 라우팅: **6곳 Shopify**(페이지 마커) · adidas·Athleta는
    generic — 팀 리스트의 절반이 generic이라는 계측과 같은 그림.
  - **주소 모양이 조용히 틀리는 자리**를 고정했다(`newbrands-test` 41건):
    로케일 접두(`/en-int/`·`/en-us/`)가 카테고리가 되지 않고 **벌크 JSON 경로에는
    남아야** 한다(`/en-int/collections/activewear/products.json`), 반복 쿼리
    (`?o_cat=Tops&o_cat=Sweatshirts`)가 한 값으로 뭉개지지 않는다, 프래그먼트 패싯
    (Athleta `#style=`)이 컬렉션을 가른다 — 안 그러면 두 번째 URL이 "이미 스캔함"으로
    건너뛰어진다.
  - **`/collections/<handle>/<tag>`는 두 조각 다 이름이다**: Gymshark 여성복이
    `/collections/everyday/womens`인데 "Everyday"라고만 부르면 **디자이너가 고른
    절반을 버린다**. 태그도 몰이 쓴 구조이므로 "Everyday Womens". 단
    `/products/`(그 컬렉션 안의 상품 페이지)와 페이지 번호는 카테고리가 아니다.
    벌크 JSON 엔드포인트에는 태그가 들어갈 자리가 없으므로 거기서는 뗀다.
- **분석 단위는 "몇 개 브랜드가 채택했나"다**(v3.0.0, 사용자가 이전 세션 산출물
  `CLAUDE.md`·`MIGRATION.md`·`marketlenssample.html`을 첨부하며 "그대로 적용"). 그 문서의
  1번 원칙이 이것이다 — **한 사이트 안의 점유율(%)이 아니라 "추적 중인 32개 브랜드 중
  몇 개가 이걸 하나"**. 실측으로 왜 다른지 고정했다(`adoption-test`): 한 몰이 린넨 60장을
  떨구면 **상품 기준 95%**, 브랜드 기준 **1/4**. 스무 몰이 하나씩 내면 상품 기준으로는
  더 낮게 보이고 브랜드 기준으로는 **20/20**이다. 앞은 몰 하나가 바쁜 것이고 뒤는
  시장이 움직인 것인데, **제안할 가치가 있는 건 뒤쪽 하나뿐**이다.
  - `unit:"brands"`는 집계 core(`tallyIn`) 하나에만 들어가고 series·movers·pulse·
    ledger·ranked가 전부 그걸 통해 센다. **분모도 같이 바뀐다** — 브랜드 수는 그 주에
    상품을 낸 브랜드 수로 나눈다(상품 수로 나누면 붕괴로 읽힌다).
  - **주간 스냅샷은 두 단위를 다 얼린다**(`dims`·`bdims`). 브랜드 카운트가 없던 시절의
    기록은 그 단위로 **답이 없다고** 처리한다 — 상품 수를 브랜드 로스터로 나누면 1500%가
    찍힌다. 없는 것은 없다고.
  - **변화(델타)는 like-for-like다**(`axisRows`): 두 주 **양쪽에 다 있는 몰**로만 잰다.
    안 그러면 디자이너가 리스트에 브랜드 4개를 추가한 주에 모든 축이 올라가고 화면이
    "웨이브"라고 말한다(v1.72 동일점포매출 규칙을 눈이 실제로 닿는 자리에 적용).
    **헤드라인 개수는 그 주 전체**(이번 주의 사실이므로), 제한되는 건 변화뿐이고,
    새로 들어온/못 읽은 몰은 행 밑에 **이름으로** 적는다.
- **LAB은 축 4줄이다 — FABRIC / COLOUR / FIT / DETAIL**(v3.0.0, 같은 문서). 시즌에 대해
  묻는 질문이 넷인데 셀렉터로 하나씩 보면 나머지 셋을 머리에 이고 있어야 한다. 그래서
  **MEASURE 셀렉터를 없앴고**, 네 축이 항상 같이 서 있다.
  - **FIT은 디테일에서 떼어냈다**(`report.js`의 `FIT` 화이트리스트). oversized가 뜨는
    시즌과 ruched가 뜨는 시즌은 다른 발견인데 한 랭킹에 섞이면 둘 다 묻힌다. 섬유·조직과
    같은 이유로 **닫힌 어휘**(비례·재단 용어는 정착된 무역 언어), 나머지는 종전대로
    스톱리스트라 처음 보는 실루엣은 그대로 디테일로 올라온다. 한 단어는 여전히 한 갈래에만.
  - **FABRIC 축은 몰이 이름에 쓴 조직**(satin·poplin·jersey)**을 쓰고, 없으면 조성이
    말하는 주 섬유**로 떨어진다(`fabricfam`). 기존 `fabric` 축(전 섬유 카운트)은 그대로
    남는다 — 지금까지의 주간 기록이 그 단위로 얼어 있다.
  - 축 하나 = **키워드 카드들**(v3.1.0, 사용자 요청 "LAB내 키워드별로 카드내에 검색어
    시각자료"). 카드 하나 = 이름 · 델타(▲/▼/0/—) · `6/12 brands` **브랜드 수** ·
    상품 수 + **조성 한 줄**(그 이름 밑 상품들의 최빈 조성, 평균이 아님 — 두 블렌드의
    평균은 아무도 안 만드는 블렌드다) · **면적 차트**(자기 범위로 스케일) · 검색 레인.
    행이 아니라 카드인 이유: 디자이너는 한 열을 내려 읽는 게 아니라 키워드를 **나란히
    비교**하고, 행에는 검색 레인이 들어갈 자리가 없다.
  - PULSE 표·to-watch 카드·빈도 랭킹·share 차트·movers 쌍은 **축 행이 같은 답을 하므로
    제거**했다(첨부 문서의 카피 규칙: 같은 사실을 두 군데서 말하지 않는다). 계산 자체는
    `trend.js`에 그대로 있고 `pulse-test`·`compare-test`가 계속 지킨다.
  - **검색 레인은 CSV로 들어온다**(v3.1.0). v3.0.0에서 자리를 비워 뒀던 그 레인이다 —
    첨부 목업의 `#4 · 6w`는 더미였고(`MIGRATION.md`가 "픽스처는 전부 더미"라고 적어 둠),
    **공개 API가 없고 내부 엔드포인트는 기계를 거부하며 소급 수집이 안 된다.** 그래서
    사람이 받는 파일이 **유일하게 정확한 길**이고, 그건 우회로가 아니라 정규 경로다:
    trends.google.com → 단어 비교 → "Interest over time" 다운로드 → LAB 헤더의 **Data
    상자 안 `Add Google Trends (.csv)…`**.
    - 카드에 붙는 기준은 **온전한 단어 포함**이다 — 디자이너는 사람이 검색하는 말
      ("satin dress")을 넣고 축 키는 한 단어("Satin")라, 그 단어를 품은 검색어가
      그 카드로 간다. 한 키에 여러 검색어가 붙으면 **최근값이 가장 큰 것**이 대표한다.
    - 값은 Google의 **0–100 관심도 지수**다 — 순위도 검색량도 아니라서 카드에 그대로
      그 이름으로 적는다(`SEARCH`). `<1`은 0이 아니라 **0.5**로 읽는다.
    - **없는 주는 0이 아니다**: 파일에 없는 주는 행을 만들지 않고 선이 끊긴다(스캔
      데이터와 같은 규칙). **가져오지 않은 키워드에는 레인 자체가 없다** — 평평한 0
      레인은 "아무도 안 검색했다"는, 파일이 한 적 없는 주장이다.
    - 저장은 IndexedDB `trends`(`VER 4`, 키 `단어|주차`)라 **한 번 넣으면 계속 남는다**.
    - `trends-test` 13건 · `gtrends-e2e` 11건(빈 상태 → 가져오기 → 매칭 카드에만 레인 →
      리로드 후 유지).
  - **몰이 자기 이름을 상품명에 쓰면 그건 디테일이 아니다**(v3.1.0). 카드 화면을
    **렌더해 보다가** 발견했다(보고가 아니라 생성 — v2.7.0 규칙): DETAIL 축에 ZARA ·
    ARKET · COS가 각각 카운트와 추이선을 달고 서 있었다. Walmart·Target은 제목에 라벨을
    항상 넣고 하우스 브랜드도 흔히 넣는다("VUORI Performance Jogger") — 그 축은
    **옷에 무엇을 했나**를 읽는 자리다.
    → 브랜드 단어는 **열린 갈래(디테일)에서만** 뺀다. 섬유·조직·핏은 닫힌 무역 어휘이고
    **브랜드가 원단 이름일 수 있다** — Cotton On 상품에서 "cotton"을 지우면 몰이 실제로
    말한 사실을 버리는 것이다. `brandword-test` 9건(수정 전 코드에서 4건 실패 확인).
  - `adoption-test` 20건 · `axes-e2e`·`pulse-e2e`·`snapshot-e2e`·`weekcompare-e2e`·
    `tier-e2e`·`newin-e2e`는 새 계약(축 4줄·브랜드 단위·카드)으로 갱신.
- **엣지에서도 된다 — 다만 문이 하나 막혀 있었다**(v3.22.0, 사용자 질문 "edge에도
  가능하고 chrome에서도 가능한거지"). 엣지는 크로미움이라 이 확장이 쓰는 API가 거의
  전부 같은 것이다. 이 머신에 엣지를 깔 수 없으므로 **차이가 날 수 있는 그 하나를
  지웠다**: 워커 첫 줄 전에 `chrome.sidePanel`을 삭제하고 확장을 올렸다.
  - 살아남은 것: **워커 시작 · 패널 페이지 렌더 · LAB · 스토리지 · 스캔 API 전부**.
  - 살아남지 못한 것 하나가 **하필 유일하게 중요한 것**이었다:
    `setPanelBehavior({openPanelOnActionClick:true})`가 **곧 툴바 버튼**이라,
    그 API가 없으면 **버튼을 눌러도 아무 일도 안 일어난다.** 설치는 되는데 열 수가
    없는 상태 — 측정 전에는 "크로미움이니 되겠지"로 넘어갈 뻔한 자리다.
  - → **버튼이 자기 핸들러를 갖는다**(`openPanel`). 크롬에서는 **절대 안 불린다**
    (패널이 대신 열리는 것이 그 플래그의 뜻이다), 사이드 패널이 없는 브라우저에서는
    **같은 페이지를 탭으로** 연다. 같은 문서·같은 스크립트, 프레임만 다르다.
    알림 클릭도 같은 길을 쓴다.
  - 탭으로 열릴 때는 **폭을 지킨다**(`body.astab`, 430px) — 350px용 레이아웃이
    모니터 전체로 늘어나지 않게.
  - `edge-e2e` 9건(수정 전 코드에서 **버튼 핸들러 없음·눌러도 안 열림** 확인).
    이 테스트는 "탭으로 열린다"가 아니라 **"눌렀을 때 열리기는 하는가"**를 지킨다.
  - 확인 못 한 것: **실제 엣지**. 이 샌드박스에는 크로미움만 있다. 최신 엣지는
    사이드 패널 API를 지원하는 것으로 알려져 있고 그러면 크롬과 완전히 동일하게
    동작한다 — 지원하지 않더라도 이제 탭으로 열린다. **둘 다 대비돼 있다.**
- **다운로드는 릴리스 자산이다 — 안쪽 폴더 함정 제거**(v3.21.0, 사용자 지적 "1번
  단계는 너무 귀찮잖아"). 설치 6단계 중 **가장 자주 틀리는 자리**가 2번이었다:
  GitHub 브랜치 아카이브는 **브랜치 이름의 폴더로 한 겹 감싸므로**, 그 껍데기를 고르면
  크롬은 로드에 실패하거나(manifest 없음) 엉뚱한 경로를 신원으로 삼는다(카탈로그 손실,
  v1.88.1과 같은 사고).
  - `pack.sh`는 **원래 평평한 zip**을 만든다(확장이 루트에). 그걸 릴리스 자산으로
    올리게 했다 — 릴리스 워크플로에 curl + `GITHUB_TOKEN` 한 단계(서드파티 액션 금지
    규칙 그대로, 릴리스 자산은 팀원 전원 브라우저로 가는 코드다).
  - 링크가 **영원히 안 바뀐다**: `.../releases/latest/download/market-lens.zip`.
    브랜치 이름도, 버전도 안 들어간다. `update.js`·`sidepanel.js`·`update.bat`·
    `update.command`·`TEAM.md`가 전부 이 주소를 본다.
  - 설치는 **6단계 → 5단계**, 그리고 없어진 것이 **가장 자주 틀리던 그 단계**다:
    "폴더 하나 만들고 거기에 압축을 푼다. 그 폴더가 곧 확장이다."
  - **두 모양 다 착지해야 한다** — 링크가 바뀌기 전에 깔린 사본은 여전히 브랜치
    아카이브(감싼 모양)를 받는다. `stripTopFolder`는 원래 둘 다 처리하지만 그건
    주장이라 `selfupd-e2e`가 **평평한 zip과 감싼 zip을 둘 다 실제로 설치**해 확인한다.
  - **1번 단계를 아예 없애는 길은 웹스토어뿐**이다(설치가 링크 한 번, 폴더 지정도
    자동 업데이트 장치도 불필요). 절차·심사 문구·자동 배포는 `STORE.md`에 전부 준비돼
    있고, 남은 것은 개발자 계정($5)과 최초 제출 — 사용자 몫이다.
- **끝에서 끝까지 한 번 돌려 보고 팀에 넘긴다**(v3.20.0, 사용자 요청 "문제없이 잘
  돌아갈 수 있도록 확인해서 팀원들에게 바로 보여줄 수 있도록"). 부분을 지키는 검사는
  많았는데 **처음부터 끝까지 도는 검사가 없었다** — `goal-e2e`가 사라진 뒤로.
  - **다시 세웠고, 이번엔 진짜 이동으로 돈다.** `ctx.route`는 **확장이 연 탭의 최상위
    이동을 못 잡는다**(그래서 첫 시도가 0개였다) → 픽스처를 **로컬 HTTPS**로 서빙하고
    `--host-resolver-rules`로 세 호스트를 거기에 매핑한다. 브라우저가 실제로
    `www.everlane.com`을 방문하므로 **선언된 content script가 진짜로 들어간다.**
    이 샌드박스는 브라우저를 에이전트 프록시로 보내는데 그게 리졸버 규칙을 이겨서
    전부 `ERR_TUNNEL_CONNECTION_FAILED`가 된다 → `--no-proxy-server`(전부 로컬이다).
  - 검사하는 것: 모양이 서로 다른 세 몰(벌크 JSON · JSON-LD material · 배경 사진 +
    본문 아코디언) → **Scan all 한 번** → 13행 전부에 **브랜드·상품명·사진·조성** ·
    브랜드가 **디자이너가 지은 이름**(드롭 코드 아님) · 자기 판정 ✅ · 엑셀 버튼 깨어남 ·
    LAB 열림 · **대시보드 리포트 생성**. 21건.
  - **그리고 즉시 결함 하나를 잡았다**: 실제 스캔 데이터로 그린 대시보드에 **중간가
    `$45.00 – $12000.00`**. `parsePrice`가 쉼표를 전부 지워서 **`120,00 €` → 12000**이
    됐다. 엑셀은 몰이 쓴 글자를 그대로 두므로 **스크랩과 차트 사이의 누구도 이상을
    느낄 수 없었다.** 이제 쉼표는 **끝에 한두 자리가 따라오면 소수점**(120,00 · 89,5),
    아니면 천 단위(1,299)이고, 둘 다 있으면 **뒤에 오는 것이 소수점**이다
    (1.299,00 · 1,299.00 둘 다). `price-test` 16건(수정 전 코드에서 12건만 통과).
  - `tests/run-all.sh` 한 줄로 전부 돈다 — 계약 10개 스위트, 프로브는 제외(재는 도구지
    계약이 아니다). **배포 ZIP도 검사한다**: manifest·HTML·importScripts가 부르는
    파일이 전부 `pack.sh` 화이트리스트에 있는지(24개 중 누락 0).
  - 팀원용 설치·사용 안내는 `TEAM.md`(한국어 한 장).
- **산출물은 대시보드다**(v3.19.0, 사용자가 자기 팀의 `Material Intelligence` HTML을
  첨부하며 "LAB에서 HTML 뽑으면 이 UI 및 레이아웃으로"). 첨부 파일을 실제로 렌더해
  구조를 읽고 그대로 옮겼다: **상단바**(마크·제목·검색·날짜) · **섹션 알약 탭** ·
  **왼쪽 필터 레일**(브랜드·카테고리·원단 + 출처 메모) · **히어로 띠** · **KPI 타일 줄**
  (첫 장만 채움) · **랭킹 패널 2열** · **가격 분포 + 섬유 도넛** · **결정 신호 3장** ·
  **상품 월**.
  - **숫자는 여전히 한 벌이다**(v3.8.0 규칙). 전부 같은 `Calc.aggregate`에서 나오고,
    `dashboard-e2e`가 **KPI 헤드라인 = 히어로 칩**, **원단 랭킹 1위 = 도넛 범례 1위 =
    결정 신호**가 같은 이름·같은 %인지 화면에서 대조한다. 그렇게 안 하면 한 파일 안에서
    두 숫자가 갈린다.
  - **도넛 범례가 두 번 정규화하고 있었다** — 링 비율을 다시 100%로 나눠서 Cotton이
    38%(상품 중 비율)가 아니라 25%(링 중 비율)로 찍혔다. 같은 파일 안의 두 숫자가
    같은 이름 옆에서 다른 말을 하는 것이라 **랭킹이 쓰는 값을 그대로** 쓰게 했고,
    링이 무엇의 비율인지 한 줄로 적었다.
  - `38%%` — 단위가 두 번 붙는 것은 **렌더해 봐야 보이는 종류**라 테스트가 본다.
  - 아카이브 성질은 그대로: **외부 참조 0 · 스크립트 1개**(탭·필터), 이미지는 data URI,
    인쇄하면 레일과 상단바가 빠지고 전 섹션이 펼쳐진다.
  - **색은 첨부 문서의 것을 쓴다**(`--dacc:#ff5c35`, 따뜻한 종이, 둥근 카드). 패널·LAB의
    각진 흑백과 다른데, **이 파일은 팀 밖으로 나가는 물건이고 사용자가 그 모양을 골랐다**.
    의도된 이탈이라 여기 적어 둔다.
  - `dashboard-e2e` 27건(수정 전 코드에서 **3건만 통과**).
- **public 전환 확인됨, 그리고 다시 정확도로**(v3.18.0). 인증 없는 요청에
  `raw.githubusercontent.com` **HTTP 200 · v3.17.0** — 확장이 보내는 것과 같은 요청이다.
  알림·배지·자동 설치가 살아 있다. 절차·확인법은 `GO-PUBLIC.md`.
- **생성 탐색을 되살렸고 결함 둘을 잡았다**(v3.18.0, `matrix-probe` 재작성).
  v2.7.0이 세운 그 도구는 사라진 스위트에 함께 있었다. **리더는 `chrome.*` 의존이 0**이라
  이번엔 확장을 올리지 않고 **sites.js를 평범한 페이지에 주입해** 돌린다 — headless로
  83개 모양이 수십 초다.
  - **비어 있지 않음은 정확함이 아니다**: 셀을 페이지가 실제로 말한 것과 대조한다
    (상품 사진인가 색상 칩인가 · 상품명인가 스크린리더 문구인가 · 몰이 쓴 그대로의 가격).
  - **① 색상 칩의 alt가 상품명을 이겼다.** `tinyPicture`(몰이 스스로 100px 이하라고
    적은 그림)는 v2.5.0부터 **사진 열만** 지키고 있었고 이름 경로는 그 규칙을 배운 적이
    없다. 그래서 컬러웨이를 먼저 보여주는 타일이 **"Black"으로 filed**됐다 — 옆에는
    **올바른 사진**이 붙어 있어서 행이 완전해 보이고, 그게 이 결함이 안 보이던 이유다.
  - **② 사진을 CSS 배경으로 그리는 타일에서 몰이 적어 둔 이름을 안 읽었다.** 그런
    요소가 자기 이름을 말할 수 있는 자리는 `aria-label`뿐이다(`<img alt>`와 같은 사실,
    마크업이 강제한 다른 자리). 안 읽으니 이름이 비고, 가격은 제외되고, **행 전체가
    버려져 그 몰이 0개**로 돌아온다 — 이 도구가 낼 수 있는 가장 나쁜 결과다.
    감싸는 링크 + 배경 사진 + 텍스트 없는 이름, 다섯 조합이 전부 이 모양이었다.
  - **축을 늘리는 것이 곧 개발 계획이다** — 실패 표가 어느 축 값에서 몇 번 났는지
    말해 주고, 그게 다음에 고칠 것이다. 지금 축: 사진 그리는 법 6 × 돈 표기 6 ×
    이름 위치 5 × 타일 중첩 3 × 상품 주소 4 × 타일 잡동사니 4.
  - **탐색기와 계약이 한 파일이다**(실패 시 non-zero). 둘로 나누면 어긋난다 —
    수정 전 코드 exit=1, 수정 후 exit=0으로 확인.
- **자동 업데이트는 "패널을 열면 이미 되어 있다"까지다**(v3.17.0, 사용자 요청
  "PUBLIC으로 변경하고 자동 업데이트되도록"). 조각은 전부 있었고 **아무도 시작을
  안 했다**: 워커는 새 버전을 알고, 폴더는 기억돼 있고, 인스톨러는 쓰고, 브라우저는
  스스로 재시작한다 — 그런데 그 전부가 **버튼 하나에 매달려** 있었고 그 버튼을
  누르는 일이 안 일어났다.
  - → **패널이 열릴 때 스스로 설치한다**(`maybeSelfUpdate`). 크롬은 폴더로 로드한
    확장을 갱신하지 않고, **저장된 디렉터리 핸들은 문서가 있어야** 쓸 수 있으므로
    (서비스워커로는 불가) 여기가 자동화의 한계선이다. 그래서 설계가 이렇다:
    **알림이 "나왔다"를 말하고, Market Lens를 여는 순간 이미 설치 중**이다.
  - **비용이 드는 모든 경우에 거절한다**: 폴더 미지정·권한 만료(**조용히 묻지 않는다**) /
    스캔 중(재시작하면 그날 엑셀을 잃는다) / 이 브라우저가 재시작에서 안 돌아온 적 있음 /
    같은 버전 재시도(`wpb_autotried` — 실패해도 열 때마다 반복하지 않는다) / 꺼 둠.
  - 스위치는 알림 스위치 옆(`#uselfupd`, 기본 켜짐). 둘은 **한 약속의 두 반쪽**이라
    같은 자리에 있다 — 하나는 말하고 하나는 한다.
  - 핸들러를 복제하지 않는다 — `$("#uauto").click()`으로 **이미 있는 경로**를 누른다.
  - `selfupd-e2e` 12건(수정 전 코드에서 **4건만 통과**): 실제 폴더 핸들에 진짜 zip을
    풀어 쓰고, GitHub 껍데기 폴더 제거·거절 4종·스위치까지 검사한다. 첫 실행에서
    **테스트가 진짜로 확장을 재시작시켜 브라우저가 죽었다** — 체인이 끝까지 도는
    증거였고, 그래서 재시작만 무력화하고 잰다.
  - 그 테스트가 **원래 있던 결함 하나를 잡았다**: `paintQueue`가 `queue.list[...]`를
    무방비로 읽어, `list`가 없는 낡은 실행 기록 하나면 **패널 전체가 그리다 죽는다**.
    큐는 다른 컨텍스트가 쓰는 유일한 레코드라 방어적으로 읽는다.
  - **public 전환은 사용자 몫**이고 절차·확인법·안전장치를 `GO-PUBLIC.md`에 적었다.
    코드는 전환 즉시 동작한다(코드 변경 불필요).
- **버전 업데이트는 데이터를 지우지 않는다 — 실측**(v3.16.0, 사용자 질문 "버전
  업데이트를 하면 자동으로 데이터가 다 없어지나"). 코드로는 `onupgradeneeded`가 전부
  `if (!contains(...))`라 **추가만 하고 지우지 않는다**. 하지만 읽는 것과 재는 것은
  다르므로 **진짜 업데이트를 수행했다**(`update-keeps-probe`):
  - v3.11.0을 폴더에 깔고 스캔 → **상품 3 · 리스트 1**
  - **DB 버전을 3으로 낮춰** 업그레이드가 반드시 일어나게 함
  - **같은 폴더**를 새 빌드로 덮어씀(⚡ Update now·ZIP 덮어쓰기·update.bat이 하는 그것)
  - 재시작 → **v3.16.0 · 상품 3 · 리스트 1 · 확장 id 동일** ✅
  - **다른 폴더**에서 같은 빌드 로드 → **상품 0 · 리스트 0 · id 다름** ← 유일한 손실 경로
  - 즉 **잃는 방법은 폴더를 옮기는 것 하나뿐**이다(언팩 확장은 경로가 곧 신원). 그래서
    v1.88.1의 설치기 가드(고른 폴더에 Market Lens의 manifest.json이 있어야 함)와
    수동 절차의 "같은 폴더에 덮어쓰기"가 **데이터 보존 장치**다.
  - **업데이트 상자가 그 사실을 말한다**(`.ukeep`). 1년치를 잃을까 봐 업데이트를 미루는
    것이 바로 이 상자가 없애려는 마찰인데, 정작 "지워지지 않는다"는 말이 버튼 옆에
    없었다. 이제 **측정된 사실**로 적혀 있다.
  - `updnote-e2e` 22건 · 프로브는 `tests/update-keeps-probe.js`에 남긴다.
- **인터넷 기록을 지워도 카탈로그는 안 지워진다 — 실측**(v3.15.0, 사용자 질문
  "브라우저에 저장되는거면 사용자가 인터넷 기록을 지우면 이전 정보들이 없어지나"). 1년치
  팀 기록이 걸린 질문이라 문서를 믿지 않고 **실제 크롬에서 지워 봤다**(`bd-probe`).
  - 크롬의 삭제 엔진(설정 대화상자가 부르는 바로 그것)에 **모든 항목 · 전체 기간**으로
    돌렸다 → **상품 2개 · 리스트 1개 그대로**. 대화상자는 **웹 오리진만** 지우고
    확장 저장소는 다른 오리진 종류이며 **그 선택지를 제공하지 않는다.**
    `originTypes.extension`을 명시했을 때만 사라진다 — 대화상자로는 도달할 수 없다.
  - 쇼핑몰 하나의 쿠키·사이트 데이터를 지우는 것도 무해하다(같은 프로브에서 확인).
  - **진짜로 지우는 것**: 확장 삭제·재설치 / **폴더 경로 변경**(언팩 확장은 경로가 곧
    신원이라 옮기면 다른 확장이 되고 카탈로그가 빈다, v1.88.1) / 프로필 삭제·새 노트북.
  - **그래서 Data 상자가 그 말을 하게 했다.** 전에는 "이 브라우저에만 있다"까지만
    말해서 사람들이 가장 걱정하는 질문(기록 지우면?)이 **답 없이 남아 있었다** —
    이제 "기록을 지워도 안 없어지고, 확장을 지우거나 폴더를 옮기면 없어진다"고 적고
    **마지막 백업 날짜**를 함께 말한다(`wpb_lastbackup`). "한 번도 백업 안 함"이
    1년을 잃는 상태이므로 그것부터 화면에 있어야 한다.
  - `databox-e2e` 8건(수정 전 코드에서 4건 실패 확인).
- **배포 브랜치는 `claude/main-session-cudnkx`다 — 그리고 저장소가 private이라
  자동 확인은 지금까지 한 번도 성공한 적이 없다**(v3.14.0, 사용자 요청 "어떤 것이
  맞는지 너가 확인하자"). 추측 대신 재 봤다.
  - **저장소는 하나다.** git 리모트는 `Fabric-Scanner`, `update.js`는
    `Shop-Category-Collector`를 가리켜 두 개처럼 보였는데, 인증 API로 확인하니
    **같은 커밋(10ff196)**이 나온다 — 이름을 바꾼 저장소이고 옛 이름이 리디렉트된다.
  - **브랜치는 main-session이 맞다.** 팀원 크롬에 깔린 사본의 `update.js`에
    `BRANCH = "claude/main-session-cudnkx"`가 **박혀 있다**. 개발 브랜치를 옮기면
    이미 설치된 사본은 새 주소를 영영 모른다(새 주소를 알려면 먼저 새 사본을 깔아야
    하는 순환). `new-session-cudnkx`는 세션 하니스가 붙인 이름일 뿐이다.
  - **진짜 문제는 브랜치가 아니라 private이었다**(실측):
    `raw.githubusercontent.com`에 인증 없이 요청하면 이 저장소는 **404**, 같은 계정의
    **public 저장소는 200**. 확장이 보낼 수 있는 요청에는 자격 증명이 없다
    (`chrome-extension://` 오리진이라 쿠키가 안 실리고, raw는 애초에 쿠키로 인증하지
    않는다). → `check()`는 항상 `{ok:false}`이고 **NEW 배지도 알림도 뜰 수 없다**.
    채팅으로 보낸 ZIP 링크가 되는 이유는 그건 **github.com**이 사용자의 로그인 세션으로
    내주기 때문이다 — 그래서 아무도 이상을 못 느꼈다.
  - **고칠 수 있는 것과 없는 것을 나눠서 했다.** 없는 것: 저장소를 public으로 바꾸는
    것은 회사 자산에 대한 결정이라 사용자 몫이다. 있는 것: **실패가 성공처럼 보이지
    않게 하는 것**.
    - **`v3.13.0`(최신 확인됨)과 `v3.13.0`(확인 못 함)이 글자까지 똑같았다** — 차이는
      아무도 안 하는 호버의 툴팁뿐이었다. 이제 확인 못 하면 **`v3.13.0 ?`**에 점선
      테두리, 툴팁은 "**GitHub에 못 닿아서 새 버전이 있는지 모른다**"고 적는다.
      **성공처럼 보이는 조용한 실패는 검사가 아예 없는 것보다 나쁘다** — 릴리스
      여러 번 동안 그게 최신이라는 말로 읽혔다.
    - `updnote-e2e` 21건(수정 전 코드에서 2건 실패 확인).
  - **자동 알림을 정말 쓰려면 셋 중 하나**: ① 저장소를 public으로 ② 공개 저장소
    하나를 만들어 `manifest.json`만 미러링 ③ 웹스토어 Unlisted(원래 목적지, `STORE.md`).
- **읽히지 않으면 없는 것이다 — 한 단계 크게, 그리고 리스트는 브라우저 탭이다**
  (v3.13.0, 사용자 요청 "글씨와 베너가 작아서 눈에 안 들어온다" + "LAB은 가운데 정렬,
  FABRIC·ACTIVE·GRAPHIC은 크롬 탭처럼 밑에 상품 URL이 보이도록"). 스크린샷 두 장이
  근거였다.
  - **한 번에 한 사이즈씩 줄어들어 있었다.** 8.5px 마크 · 9.5px 브랜드명 · 10.5px 주석 —
    각각은 그때의 컴팩트 예산 안에서 옳았지만 합치면 화면 전체가 **각주 크기**가 됐다.
    본문 13→14px, 리스트 주소 11.5→13.5px, 브랜드 9.5→10.5px, 실행 바 이름 8.5→10px에
    마크 17→21px, 아이콘 버튼 26→30px. **바닥을 10px로 두고 `panelread-e2e`가
    화면에 실제로 그려진 요소의 계산된 크기로 검사**한다(수정 전 코드에서 12/16 실패).
    LAB도 같은 기준으로 올렸다(9→10, 10.5→11.5).
  - **높이는 더 이상 그때만큼 귀하지 않다.** v1.74가 다투던 줄들은 v3.9~3.11이 도구를
    리스트 위로 옮기면서 돌려받았다 — 그래서 이번엔 그 여유를 글자에 썼다.
  - **LAB은 가운데 정렬 · 17px.** 왼쪽 끝에 붙은 단어와 패널 반대편의 화살표는
    **화살표 하나 얹힌 구분선**으로 읽혔다. 이름은 가운데, 화살표는 오른쪽 끝에
    절대 배치 — "이쪽으로 나간다"는 말은 남기되 이름을 밀어내지 않는다.
  - **리스트는 칩이 아니라 탭이다.** 칩 줄 → 요약 줄 → 사이트 목록, **세 줄이 다 비슷하게
    생겨서 어느 것이 어느 것을 지배하는지 말해 줘야** 알았다. 탭 스트립은 그걸
    **구조로** 말한다: 열린 탭이 카드의 흰색을 그대로 쓰고 **아래쪽 변이 없어서**
    밑의 주소들이 곧 그 탭의 내용이 된다. 나머지는 회색에 눌려 있다.
    모서리는 그대로 0 — 빌려온 것은 **구조지 크롬의 둥근 어깨가 아니다**.
    테스트도 클래스 이름이 아니라 **배경색이 같은지 · 사이에 선이 없는지**로 검사한다.
  - **＋는 스트립 밖에 고정한다.** 안에 있을 때 **리스트가 넷이 되면 오른쪽으로
    밀려 나갔고, 새 리스트를 만들 유일한 길이 같이 사라졌다**(수정 전 코드에서 실패
    확인). 브라우저가 새 탭 버튼을 늘 닿는 곳에 두는 이유와 같다. 글자 없는 ＋ 하나면
    되는 자리다 — 탭 스트립 끝의 ＋는 이미 아는 관용이다.
  - 스케줄 줄(`#schedbox`)을 탭 아래로 옮겼다 — 열리면 탭 위에 끼어들어 스트립이
    카드 맨 위라는 사실을 깨뜨렸다.
- **새 버전은 표시가 살아 있어야 보인다 — 그리고 배지는 보는 사람에게만 닿는다**
  (v3.12.0, 사용자 요청 "새로운 버전이 나오면 알림이 가거나 새로 표시가 되도록").
  검사 자체는 멀쩡했다. **표시가 5분을 못 버텼다.**
  - **배지에 작성자가 셋이었고 둘이 무조건 지웠다**(실측: `NEW` → 디스크 검사 1회 →
    `""`). `refreshUpdate`는 NEW를, `checkForReplacedFiles`는 ↻ **또는 빈 문자열**을,
    `reinjectOpenTabs`는 빈 문자열을 썼다. 그런데 디스크 검사는 **5분 알람**이고
    대부분의 시간엔 폴더에 새 파일이 없으므로 `""`를 쓴다 → **NEW는 길어야 5분 살고
    사라지는데 저장된 기록은 계속 "newer:true"**였다. 아무도 못 본 이유가 이것이다.
  - → **배지의 주인은 `paintBadge()` 하나**다. 두 사실을 같이 읽고 **완료에 가까운 순서**로
    정한다: ↻(파일이 이미 폴더에 있다 — 재시작 한 번) > NEW(깃허브에 새 버전) > 없음.
    다른 함수는 사실만 저장하고 배지를 건드리지 않는다.
  - **묻는 일도 5분 알람에 태웠다.** 예전엔 워커가 켜질 때와 **패널을 열 때**만 물었는데,
    패널을 여는 순간은 사람이 이미 궁금해서 보고 있는 순간이다. `refreshUpdate`가 자체
    6시간 캐시를 갖고 있으므로 **하루 최대 4회 · 수백 바이트**다.
  - **배지는 툴바를 보고 있는 사람에게만 닿는다.** 그래서 진짜 알림을 붙였다 —
    **`optional_permissions: ["notifications"]`**, 패널 업데이트 상자 안의 한 줄
    (`🔔 Tell me when a new version is out`)이 그 클릭이다. **필수 권한으로 넣지 않은
    이유**: 웹스토어로 가는 날 필수 권한이 늘면 **팀원 전원의 크롬에서 확장이 비활성화되고
    재승인을 받아야 한다** — 이 기능이 없애려는 바로 그 업데이트 마찰이다. 거절해도
    배지와 버전 칩은 그대로다.
  - **버전당 한 번만 말한다**(`wpb_notifiedfor`). 5분마다 다시 뜨는 알림은 읽지 않고
    닫는 법을 배우게 만든다. 켠 직후에는 **캐시된 답으로도 즉시** 말한다
    (`permissions.onAdded` → `refreshUpdate`), 6시간 뒤가 아니라.
  - **권한은 클릭 안에서 요청한다 — 첫 `await`이 제스처를 쓴다.** 그래서 핸들러가
    현재 상태를 먼저 조회하지 않고 `request()`를 곧바로 부른다(상태는 상자를 열 때
    갱신해 둔 변수로 안다). `updnote-e2e`가 **실제 클릭에서 그것까지** 검사한다.
  - `updnote-e2e` 19건(수정 전 코드에서 배지 2건 실패 확인).
- **테스트는 저장소 안에 산다**(v3.12.0). 스위트가 저장소 밖 스크래치 폴더에 있었고
  **2026-08-18 세션 사이에 그 폴더가 통째로 비워져 약 150개가 사라졌다**(git에 없었다).
  이제 `tests/`에 커밋한다 — `pack.sh`는 화이트리스트라 배포 ZIP에는 절대 안 들어간다.
  복구된 것은 그 세션에서 쓴 3개뿐이고, `encoding-test`는 계약대로 다시 썼다(이건
  없으면 확장 전체가 로드 실패하는 것을 막는 검사라 재작성이 필수였다).
  **나머지는 그 영역을 다음에 손댈 때 그 자리에서 다시 세운다** — 무엇을 지키던
  테스트였는지 다시 유도하지 않고 기억으로 복원하면 아무것도 안 지키면서 통과한다.
  실행법·규칙은 `tests/README.md`.
- **숫자는 그 숫자가 맞는 자리에서만 말한다**(v3.11.0, 사용자 요청 "LAB에 브랜드와
  제품 정보는 없애 / 탭에도 숫자는 없애 / IMPORT는 리스트 우클릭으로"). 세 자리 다
  **이미 다른 데서 말한 것을 한 번 더** 말하고 있었다.
  - **LAB 띠의 `407 products · 12 brands` 제거.** v3.9.0이 "검은 띠에 화살표만 있으면
    구분선으로 읽힌다"고 붙인 줄인데, **그 숫자는 전 카탈로그**(모든 리스트 합계)이고
    그 아래의 모든 것은 **리스트 하나**에 대한 것이다 — 한 화면에 있는 두 숫자가
    **설계상 서로 다른 말**을 한다. 문 안의 내용은 문 안에서, 그것이 속한 범위로.
  - **리스트 칩의 개수 제거.** 바로 아래 줄이 열린 리스트에 대해 `8 sites · 7 brands`를
    말한다 — 리스트를 고르는 순간 반복되는 숫자를, **350px에 이름 여러 개를 넣어야 하는
    칩**이 지고 있었다. 툴팁에는 그대로 남는다(비용 0).
  - **Import는 리스트의 도구다** — 파일을 **열린 리스트 안으로** 읽어들이므로, 그 리스트를
    써낸 export 옆(우클릭 메뉴)이 자리다. 버튼으로 있을 때는 **대부분의 몰에서 그 줄의
    유일한 버튼**이었다(＋Add는 권한 없는 몰에서만 나온다) — 리스트를 처음 만들 때
    누르는 것에 상설 한 줄을 쓰고 있었다. 버튼은 `#ltools`에 **그린 것 없이 남고**
    메뉴가 그걸 누른다(핸들러 복제 금지 규칙 그대로).
  - **메뉴에 있다는 것은 계약이 아니다 — 파일 창이 열리는 것이 계약이다.** 파일 선택은
    사용자 제스처를 요구하는데, 이건 진짜 클릭 안의 합성 클릭이다. `menuimport-e2e`가
    우클릭 → 메뉴 → **실제 filechooser** → 파일 투입 → **메뉴를 연 그 리스트에** 착지까지
    끝까지 몬다(수정 전 코드에서 실패 확인).
  - 그 테스트가 **원래 있던 결함 하나를 잡았다**: 주소만 든 .txt는 브랜드가 없는데
    (텍스트 형식은 머리글 줄에서 브랜드를 받는다) 이름 수리는 **로드 때만** 돌아서,
    Import 직후 화면에 **이름 없는 행**이 남고 그전에 뽑은 엑셀은 그 행들을 브랜드
    없이 묶는다. 이제 병합 직후 `repairNames()`를 돌려 **리로드가 보여줄 것을 저장**한다.
- **누르는 것은 전부 같은 모양이다 — 마크 위, 이름 아래, 박스 안**(v3.10.0, 사용자
  요청 "기능 아이콘을 박스별로 모던하고 심플하게"). 한 패널 안에 **누르는 물건의
  어휘가 셋**이었다: 아래 실행 바는 마크만 있는 박스, 담기 줄은 테두리 없는 회색
  글자, 탭은 **꽉 찬 검정 상자**. 같은 종류의 일인데 셋이 다르게 생기면 무엇이
  버튼인지 매번 다시 배운다.
  - **탭은 밑줄이다.** 솔리드 검정 탭이 바로 아래 LAB 띠와 경쟁했는데, **패널에서
    솔리드여야 하는 것은 LAB 하나**다(v1.62 규칙: 솔리드는 그룹당 하나까지).
    이제 대문자·와이드 트래킹에 현재 것만 밑줄 — 잡지가 섹션을 표시하는 방식.
  - **실행 바의 네 컨트롤은 이름을 되찾았다.** v1.98이 "▶ ⏸ ■ 세 모양이 다르니
    이름은 툴팁으로 충분하다"고 뺐는데, ⬇가 넷째로 들어온 뒤 **한 줄에 글리프 넷**이
    됐다. 주 1회 여는 패널에서 그건 읽는 게 아니라 해독하는 것이다. 마크 위 · 이름
    아래(8.5px 대문자), 홀드는 여전히 **갈 상태**의 마크로 바뀐다.
  - **Import·＋Add도 같은 박스**가 됐다(44px — 실행 바보다 낮다. 리스트를 만들 때
    한 번 누르는 것이지 실행을 보며 누르는 것이 아니다). **폭은 148px로 막는다**:
    대부분의 몰에서 Import가 유일한 버튼인데 350px을 다 차지하면 **누르는 물건이
    아니라 파일 드롭 영역**으로 읽힌다. 둘이 나오면 줄을 정확히 채운다.
  - `＋ Add`는 이제 **글자를 버튼에 쓰지 않는다** — `textContent`를 갈아끼우면
    그림이 지워지고 박스들 사이에 맨 글자 한 줄이 남는다. 이름은 `<em>`에 들어가고
    **마크만 ＋ ↔ ✓로 바뀐다**.
  - **검색 상자는 렌즈를 안에 넣는다**(`.srch`) — 리스트 필터와 상품 검색이 같은
    모양이고, 플레이스홀더는 다른 라벨과 같은 대문자 마이크로 라벨이다.
  - `panelclean-e2e`가 **v3.9.0부터 죽어 있었다**: 그 릴리스가 지운 `.fhead`를 읽어
    30초 타임아웃으로 던지고 있었고, 브랜드도 **머리글에서만** 세어(솔로 행이 된
    3개를 못 보고) 네 개 중 하나만 확인하고 있었다. 지금 계약으로 수리 —
    수정 전 코드에서 `em`이 안 그려지는 것으로 실제 실패 확인.
- **패널은 눈에 바로 들어와야 한다**(v3.9.0, 사용자 요청 "Collector와 PRODUCT, LAB
  아이콘과 함께 개인별 커스텀하는 LIST까지 시각적으로 바로"). 스크린샷에서 실제로
  읽히지 않던 것들을 하나씩 고쳤다:
  - **`PRODUCTS`가 "PRODUCT 8"로 읽혔다.** 마이크로 라벨용 자간(.13em)을 단어에 쓰니
    끝 글자가 떨어져 나왔다. 트래킹은 두세 글자 라벨용이지 **읽는 단어용이 아니다**.
  - **LAB이 구분선처럼 보였다** — 검은 띠에 화살표 하나. 이제 **문 뒤에 무엇이 있는지**
    말한다(`379 products · 12 brands`). 열기 전에 알고 싶은 유일한 숫자다.
  - **리스트가 위의 탭과 같은 모양이었다.** 이름에서 뽑은 **색 점**을 달아서(브랜드와
    같은 방식, 저장 안 함) "보관하는 리서치 질문들"로 읽히게 했다.
  - **한 브랜드에 한 페이지면 한 줄이다.** 대부분의 리스트가 그 모양인데(브랜드 8개,
    페이지 8개) 머리글 + 행으로 **같은 브랜드 이름을 두 번** 쓰고 있었다. 접을 것도
    없으므로 머리글이 **아무것도 안 하는 컨트롤**이기도 했다. `Collapse`도 접을 머리글이
    하나라도 있을 때만 나온다.
  - **행의 도구(↗ ✎ ✕)는 포인터가 그 행에 있을 때만** 보인다. 40줄짜리 리스트에서
    줄마다 글리프 세 개는 컨트롤이 아니라 무늬다(같은 동작이 우클릭 메뉴에도 있다).
- **담기는 페이지에서 한다 — 패널의 Paste·＋Page 제거**(v3.9.0, 사용자 요청 "GRAB으로
  이미 취합이 가능하니"). 다만 **기능을 없앤 게 아니라 중복을 없앴다**:
  - **Paste는 LAB의 Scan lists 탭에 그대로 있다.** 350px 패널에서 4줄짜리 textarea는
    정작 리스트가 있어야 할 자리를 밀어냈고, LAB에는 그게 늘 열려 있을 자리가 있다.
  - **＋ Page는 지울 수 없다 — 권한이 없는 몰의 유일한 문이다.** 접근 권한이 없으면
    content script가 안 들어가고 **Grab 버튼 자체가 없으며**, 크롬은 클릭 안에서만
    권한을 준다. 그래서 **그게 유일한 문일 때만** 나타난다(`read.access`가 false일 때).
    이미 허용한 몰에서는 페이지의 동그란 버튼이 길이고, 이 줄은 버튼 하나 폭이다.
- **고를 수 있는 값은 결과가 있는 값이다**(v3.5.0, 사용자 요청 "BRAND를 먼저 선택하면
  그 해당 브랜드에 적용된 CATEGORY만"). 패널 PRODUCTS의 두 셀렉터가 **각자 전체
  카탈로그에서** 채워져서, ADANOLA를 고르면 다른 몰의 페이지 이름까지 남아 있었고
  그걸 누르면 `No products match these filters` — **화면이 스스로 권한 막다른 길**.
  → LAB 레일과 같은 규칙: **한 필터가 내놓는 값은 다른 필터를 통과한 값**이고 개수가
  붙는다. 양방향이다(카테고리를 먼저 고르면 그걸 가진 브랜드만). **고른 값은 자기
  목록에서 사라지지 않는다** — 사라지면 끄러 갈 데가 없다.
- **카테고리 열은 없앨 수 없다 — LAB이 그걸로 New In을 가른다**(v3.6.1, 사용자 질문
  "차라리 카테고리를 없애는 게 좋나"). 지우면 무엇이 멈추는지 확인했다:
  ① **`isNewIn`이 이 열을 읽는다**(v2.3.0) — 없으면 New In 페이지와 진열 전체를 구분할
  방법이 사라지고, 주차 비교가 like-for-like가 아니게 된다. ② **엑셀이 브랜드 → 카테고리로
  묶는다**(`excel.js`) — 디자이너가 들고 가는 파일의 구조 자체다. ③ 패널 PRODUCTS 필터와
  리스트 행 이름. **없애는 게 아니라 자동 추정이 사람 이름인 척하지 않게 하는 것**이 답이다
  (v1.84.0 규칙: 사용자가 붙인 이름이 이긴다).
  - 그래서 고친 것: **이름 없는 행이 호스트를 대신 보여주고 있었다**(`gap.com`). 그건
    카테고리처럼 읽히는데 카테고리가 아니고, **비용이 안 보인다** — 분석이 이 단어로
    New In을 판정하므로 그 행은 수집은 되고 **LAB에서 조용히 빠진다**. 이제 행이
    `Name this page`라고 말하고(기울임·흐림), 요약 줄이 `· 1 unnamed`로 세고,
    툴팁이 **무엇을 잃는지** 적는다. 고치는 ✎는 바로 옆에 있다. `panelui-e2e` 41건.
- **몰이 만든 id는 카테고리가 아니다**(v3.6.0, 사용자 보고 "이상한 주소가 보인다").
  패널 리스트가 LULULEMON에 `n14f1wz6o10`, nike에 `new womens tops t shirts
  3n82yz5e1x6z9om13`을 달고 있었다. **모양이 둘이라 답도 둘**이다:
  - id가 **자기 세그먼트**인 경우(`/c/women-whats-new/n14f1wz6o10`) — `labelFromUrl`이
    이미 건너뛰고 있었다.
  - id가 **슬러그 끝에 붙은** 경우(`/w/new-womens-tops-t-shirts-3n82yz…`) — 세그먼트가
    통째로 코드가 아니라서 그대로 통과했다. → `stripCodes`가 이름에서 그 토큰만 뺀다.
  - **저장된 라벨은 리로드해도 안 고쳐지고 있었다**: `repairNames`가 **주소처럼 보이는**
    라벨만 다시 뽑았는데(`looksLikeUrl`) 맨 id는 주소처럼 안 생겼다. 이제 `cleanLabel`이
    코드를 빼고, **빈 값이 나오면 그걸 받는다**(예전엔 `|| e.label`로 코드를 되돌렸다).
  - **판정은 일부러 좁다**(`isCodeWord`): 6자 이상 + 숫자 2개 이상 + 글자 2개 이상.
    `1990s`(글자 1)·`Y2K`(3자)·`501`(글자 0)·`Plus 14 24`는 전부 살아남는다 —
    **진짜 이름을 버리는 것이 코드를 남기는 것보다 나쁘다.**
  - **카탈로그의 카테고리 열도 같이 고친다**(`store.cleanCategory`). 엑셀·LAB이 그 열로
    묶으므로 id가 들어가면 한 페이지가 id 수만큼 쪼개진다 — 브랜드 열이 v1.71에 겪은 것과
    같은 실패다. **쓸 때·읽을 때 모두** 적용해서 이미 수집한 행이 LAB을 여는 것만으로
    고쳐진다(마이그레이션·재스캔 없음).
  - 주소에 사람이 쓴 이름이 아예 없으면(Gap `category.do?cid=5664`) **빈 값**이다 —
    지어내지 않는다. `label-test` 25건(수정 전 코드에서 4건 실패 확인) · `panelui-e2e` 37건.
- **Filter sites·Collapse는 담기가 아니다**(v3.5.1, 사용자 질문 "역할 확인하고
  사이트 추가만 하는 기능이라면 중복이니 삭제"). 중복이 아니라서 **지우지 않았다** —
  담기는 위의 `＋ Page·Import·Paste` 셋이고, 이 둘은 **이미 있는 리스트를 덜 보는**
  두 가지 방법이다: **Filter**는 브랜드·카테고리·주소에 그 글자가 든 행만 남기고
  (찾는 동안 접힌 브랜드를 전부 편다 — 가려진 매치는 버그), **Collapse**는 브랜드를
  한꺼번에 접는다(브랜드 머리글은 하나씩 접힌다).
  - 대신 **줄값을 줄였다**: 바로 밑에 따로 있던 `7 sites · 4 brands` 요약을 **같은
    줄로 끌어올렸다**(두 줄 → 한 줄). 두 컨트롤이 작용하는 대상이 그 숫자다.
  - **할 일이 없으면 그리지 않는다**: 필터는 7개 이상일 때, 접기는 브랜드가 2개
    이상일 때. 개수는 항상 말한다. `panelui-e2e` 31건.
- **리스트의 도구는 리스트 위에 있다 — 우클릭**(v3.5.0, 사용자 요청 "아이콘을 따로
  생성하지말고 해당 리스트 우클릭"). 글리프 6개가 한 줄을 차지하면서 매번 해독해야
  했다. 이제 **칩을 우클릭(터치는 롱프레스)** 하면 이름·자동스캔·txt·xlsx·삭제가
  **단어로** 나온다. 메뉴는 **그 리스트를 먼저 선택한 뒤** 연다(포인터 밑의 리스트와
  선택된 리스트가 다르면 삭제가 엉뚱한 걸 지운다). 버튼들은 DOM에 **숨겨진 채로 남아
  있고 메뉴가 그걸 누른다** — 핸들러를 복제하면 사본이 낡는다.
  - **돋보기 삭제**: 크롬이 패널 제목과 툴바 아이콘으로 이미 두 번 말한다. 그게 지고
    있던 "스캔 중" 상태는 **헤더 위 2px 규칙**으로 옮겼다(폭 비용 0, 실행 중에만 존재).
  - **그랩 버튼 토글(◉)은 리스트 도구가 아니다** — 확장 전체 설정이라 헤더 줄로 옮겼다.
- **패널·LAB·Market lens 산출물은 한 팔레트다**(v3.5.0, 사용자 요청 "UI를 깔끔하게",
  `/brand-design-md` → WIRED). 이미 산출물 시트가 그 값으로 조판돼 있었다:
  `--ink:#000 · --body:#757575 · --hair:#e0e0e0 · --canvas:#fff · --parch:#f5f5f5 ·
  --flag:#8a3c17`. 패널과 LAB을 같은 값으로 맞추고 **모서리를 0**으로(잡지의 반경은
  0이다), 카드 그림자는 **선**으로 바꿨다. 알약 모양은 칩에서 사라지고 **진행 막대와
  번호 원만** 둥글게 남는다(그건 칩이 아니다).
- **산출물의 LAB은 LAB이다**(v3.8.0, 사용자 요청 "html로 추출할 때도 image2와 같은
  정보가 보이도록"). 화면은 v3.0.0에 네 축·키워드 카드·**브랜드 채택 수**로 옮겼는데
  HTML 리포트는 옛 대시보드(KPI 타일 · 가격 히스토그램 · 원단/색상/브랜드/카테고리
  막대)를 그대로 내고 있었다. **한 주에 대해 두 개의 리포트**가 나오고, 화면은 "몇 개
  브랜드가 새틴을 하나"를, 파일은 "상품의 몇 %가 폴리에스터인가"를 답했다 — 비교하는
  사람은 어느 쪽을 믿을지 알 수 없다.
  - 고친 방법: **같은 함수가 둘 다 그린다.** `LabView.render`를 아무도 안 보는
    엘리먼트에 렌더해서 그 HTML을 리포트에 넣는다. 숫자가 어긋나는 것이 **구조적으로
    불가능**해진다(세 템플릿 수치 동일 규칙을 화면·파일에도 적용).
  - 리포트에는 그 클래스들의 **조판만** 산다(선·각진 모서리·대문자 마이크로 라벨).
    스크립트·외부 참조는 그대로 0이고 차트는 인라인 SVG라 아카이브 성질이 유지된다.
  - `labHtml`이 없으면 옛 대시보드로 떨어진다 — 리포트를 만드는 다른 경로가 깨지지 않는다.
  - `reportlab-e2e` 11건(수정 전 코드에서 5건 실패 확인) · `tmpl-test` 수리(옛 `.meta`
    줄을 보고 있어 **HEAD에서도 이미 죽어 있었다** — 아무것도 지키지 못하는 테스트였다).
- **여섯 브랜드가 아니라 세 가지 "페이지 모양"이었다**(v3.7.0, 사용자 보고 — adidas·
  nike 0개, set active 4/66, LULULEMON 40/269, ALO YOGA 원단 0). 픽스처로 재현했더니
  옛 코드가 **그 문장들을 글자 그대로** 냈다(`slowgrid-e2e`, 9건 중 6건 실패 확인).
  - **① 스크립트가 나중에 그리는 그리드를 "빈 페이지"로 읽었다.** 안정 판정이
    `0 === 0 === 0`을 안정으로 세어서 **두 번 보고 1.5초 만에** 끝냈다 — adidas·nike는
    문서 준비 후 몇 초 뒤에 칠한다. **우리 시계가 한 말이지 몰이 한 말이 아니었다.**
    → 상품이 0인 동안에는 **최대 9초까지 기다린다**(`ZERO_GRACE`). 이 대기는 **스크롤
    라운드를 쓰지 않는다** — 안 그러면 4초 걸리는 앱이 첫 타일 전에 예산을 다 쓴다.
    진짜로 그리드가 없는 페이지는 어차피 실패할 스캔에서 한 번 9초를 낸다.
  - **② 버튼을 눌러야 다음 판을 주는 그리드가 있다.** 스크롤은 몇 번을 쓸어도 못 연다 —
    set active 4/66, LULULEMON 40/269가 정확히 이 모양이다. 이제 **몰이 말한 개수보다
    적을 때만** `Load more`류를 **최대 8번** 누른다. 누름은 **라운드를 쓰지 않는다**(진전이지
    탐색이 아니다). **어휘는 닫혀 있다** — "Add to bag"·"Apply filters"를 누르는 것이
    버튼을 못 찾는 것보다 나쁘다.
  - **③ `moreWaiting`이 늑대를 불렀다**: 몰이 말한 40개를 다 받고도 라운드를 다 써서
    "아직 더 있다"고 표시했다. **몰의 개수에 도달했으면 완료**다.
  - **④ ALO의 "어디에도 안 적혀 있다"는 우리 요청을 읽은 것이었다.** 상품 페이지가 껍데기로
    와서 스크립트가 채우는 몰이 있다(`textLen`이 짧고 JSON-LD Product도 없음). 이제
    **"페이지가 껍데기로 온다 — 이건 우리 일이고, fetch가 아니라 탭에서 읽어야 한다"**고
    구분해서 말한다. 몰 탓으로 돌리면 다음 라운드가 엉뚱한 데를 본다.
  - 라이브 몰로는 확인 못 했다 — 이 환경은 쇼핑몰에 못 간다. 다음 스캔에서 밴드가 답한다.
- **표시가 있으면 이유가 있어야 한다**(v3.4.0, 사용자가 밴드 스크린샷 4줄을 보내며
  "체크해야한다는 내용... 원인 분석 후"). `band-probe`로 그 네 줄을 **숫자만으로 먼저
  재현**한 뒤 고쳤다.
  - **VUORI가 진짜 결함이었다.** `healthMark`는 사진이 **하나라도** 없으면 ⚠️인데
    `healthWhy`는 **절반 미만일 때만** 말했다 → 60개 중 50개인 몰은 표시만 있고
    문장이 없어 밴드가 **"needs a look"** 다섯 글자로 떨어졌다. v1.95가 막으려던
    실패가 두 목록이 어긋나면서 되돌아온 것.
    → 하나라도 빠지면 개수를 말하고, **마지막에 백스톱**을 뒀다(등급이 매겨졌는데
    문장이 없으면 카운트라도 적는다). `health-test`가 **마크의 모든 가지를 돌며
    말 못 하는 가지가 있으면 실패**시킨다.
  - **set active는 오경보였다.** v1.94 이후 단일 브랜드 몰은 **리스트 이름이
    vendor를 덮으므로** 드롭 이름이 행에 닿지 않는다 — 문장 자체가 "리스트 이름으로
    filed된다"고 말하고 있었다. **이미 해결된 것에 표시를 다는 밴드는 아무도 안 읽는다.**
    이제 **덮어쓸 이름이 없었을 때만** 경고한다.
  - **ALO YOGA는 원인이 없었다.** 원단 0의 이유는 셋이고 **고칠 사람이 다르다**.
    진단 프로브(`diagnoseDetail`)가 이미 상품 페이지를 열어 사실을 적어 뒀는데
    **화면에 안 갔다**. 이제 구분해서 말한다: ① 429/503·403 → 다시 스캔 ②
    **페이지가 조성을 적어 놨는데 우리가 못 읽음 → 우리 일** (몰의 문장을 그대로 인용)
    ③ 어디에도 없음 → 읽을 게 없었다. **세일 배지는 조성이 아니다**("20% off"가
    상품 페이지에서 가장 흔한 %라, 그걸 조성으로 읽으면 몰의 마케팅을 우리 탓으로 삼는다).
  - **adidas**: 우리 필터가 비운 페이지(`funnel`)와 그리드가 아예 없던 페이지는
    한 줄 수정 vs 어쩔 수 없음이다. 기록은 알고 있었는데 말하지 않았다 — 이제
    **거부한 주소 모양까지** 적는다.
  - 모든 줄이 **어느 리더가 읽었는지**를 단다(전용 어댑터의 낡은 규칙과 generic 읽기는
    다른 일이다). `health-test` 45건(25 → 45, 그 네 줄이 픽스처로 들어갔다).
- **LAB 상단은 답이 시작되는 자리다**(v3.4.0, 사용자 요청 3건). `SCOPE` 라벨 제거
  (칩이 이름과 개수를 달고 있다 — v1.98이 패널에서 없앤 것과 같은 이유),
  헤더 집계(`FABRIC · 359 products · 11 brands · 349 with a photo`) 제거 — 옆 칩과
  아래 타일이 이미 말한다. **중복 병합 수만 남는다**(조용히 줄어드는 숫자는 신뢰를
  깬다는 규칙, 병합이 있을 때만 표시).
  - **품목 종류는 PERIOD·INTERVAL과 같은 줄의 셀렉터**가 됐다(칩 한 줄 → 0px).
  - **축 위의 회색 문단 제거.** 다만 그 문단이 지던 두 사실(분석은 New In만 읽는다 /
    종류로 좁히면 얼린 주간 기록이 빠진다)은 **화면 맨 아래 각주로 옮겼다** —
    없애면 부분을 전체로 읽고, 좁힌 화면이 조용히 더 적은 주를 읽는다.
- **브라우징 탭은 왼쪽 레일로 좁힌다 — 일곱 가지 질문**(v3.3.0, 사용자 요청 "lab에
  들어가면 위의 카테고리로 나와야해" + 목업 스크린샷 3장). 피드 위 칩은 **한 번에 한
  가지**만 물을 수 있는데 디자이너의 질문은 합성이다: *새틴 미디 드레스를, 컨템 브랜드에서*.
  - `CATEGORY · FABRIC · SILHOUETTE · FIT · DETAIL · COLOUR · BRAND` —
    **그룹 안은 OR, 그룹끼리는 AND**. New In · By Brand 두 탭이 같은 레일을 쓴다.
  - **값 옆 개수는 다른 그룹들을 적용한 뒤 센다**(faceted count). 안 그러면 satin을
    고른 화면에 `Denim · 12`가 남아서, 누르면 화면이 비는 선택지를 권하게 된다.
  - **좁혀진 화면은 좁혀졌다고 말한다**(`.railnote` — `Showing 2 of 6 · Satin · Clear all`).
    레일은 스크롤·접힘이 되므로, 부분을 전체처럼 보여주는 실패(v2.8.0 절반 그리드와
    같은 종류)를 막는다.
  - **By Brand에는 BRAND 그룹이 없다** — 옆의 브랜드 열이 그 선택기이고 둘이 있으면
    서로 어긋난다. 대신 나머지로 좁히면 **브랜드 열이 따라 줄어** "누가 새틴을 하나"에
    답한다.
  - **SILHOUETTE는 새 축이다**(`report.js`의 `silhouetteOf`). 품목의 **모양**이라
    카테고리(Dresses)도 핏(oversized)도 아니다 — 길이·형태 어휘 + 품목 명사로
    `midi dress`·`slip dress`·`wrap skirt`를 만든다. **FIT 어휘와 겹치지 않게**
    비례·재단 단어는 그대로 FIT에 둔다(한 단어는 한 갈래 규칙). 길이 단어 혼자는
    실루엣이 아니다 — `shirt`는 카테고리이고 `shirt dress`만 모양이다.
    저장하지 않으므로 **이미 수집한 행에 재스캔 없이** 적용된다.
  - 수집에는 영향이 없다(헌장: 속성 필터는 스캔 후 표시 단계에서만). `rail-e2e` 21건.
- **"어느 사이트가 잘 나오고 어느 사이트가 아닌가"에 화면이 답한다**(v2.6.0,
  사용자 요청). 스캔은 URL마다 스스로 채점하고 있었지만(`wpb_sitehealth`), LAB 밴드는
  **실패만** 나열했다 — 그래서 디자이너는 "이 수치가 12개 몰에서 나온 건가 3개에서
  나온 건가"를 화면에서 알 수 없었고, 잘 나온 절반은 서비스워커 콘솔 명령
  (`devsitecheck()`)에만 있었다. 주차 수치를 믿기 전에 묻는 질문이 바로 그것이다.
  - 헤더의 **칩 하나**(`5 sites · 2 need a look` / `5 sites · all clean`)가 상시
    답하고, 누르면 **전 목록**이 열린다: `Nothing came through` → `Partly` →
    `Complete` 순. 칩은 **기존 헤더 줄**에 타므로 목록을 열기 전까지 높이 비용 0이고,
    아무것도 스캔하지 않았으면 칩 자체가 없다.
  - **한 줄은 한 몰이다**(`한 몰은 한 브랜드다`와 같은 규칙). 카테고리 여러 개를
    스캔했으면 합쳐서 `2 pages · 84 products · 84 with a photo · 78 with fabric`으로
    적고, **가장 나쁜 페이지가 그 몰의 마크를 정한다** — New In이 0개로 돌아온 몰은
    "대체로 괜찮음"이 아니다.
  - 좋은 몰도 **숫자를 말한다**(체크 표시만 두지 않는다). 그래야 "잘 나온다"가
    확인 가능한 문장이 된다.
  - 스캔하지 않은 사이트는 목록에 없다고 **명시**한다(모르는 것을 모른다고).
  - `sitelist-e2e` 20건(수정 전 코드에서 13건 실패 확인) · `selfcheck-e2e`는 새 계약
    (실패만 나열 → 양쪽 다 나열)으로 갱신.
- **네 몰이 아니라 네 가지 "모양"이었다**(v2.5.0, 사용자 요청 "동일하거나 유사한
  사이트 구조를 가진 브랜드도 문제없이"). vuori·lululemon·nike·adidas를 하나씩
  고쳤는데, 실제로 고친 것은 브랜드가 아니라 **마크업의 종류**였다. 그래서 다음
  브랜드에 대해 물어야 할 질문은 "lululemon이 되나"가 아니라 **"이 모양이 되나"**다.
  픽스처로 열 가지 모양을 재 봤다(`imgshapes-probe`·`money-probe`, 실측):
  - **사진을 CSS 배경으로 그리는 그리드 → 상품 0개.** 사진이 빠지는 게 아니라
    **엑셀이 통째로 빈다** — `tileAncestor`가 `<img>`를 요구해서 타일 자체를 인식
    못 했다. 이제 **그림은 어떤 방식으로 그리든 그림**이다(`<img>`·`<picture>`·
    `background-image`·`data-bg`류 lazy 속성). `image-set(...)`도 같이 읽는다.
  - **진짜 `<img>`가 `<noscript>` 안에만 있는 테마 → 상품 8개, 사진 0개.**
    스크립트가 켜져 있으면 브라우저가 그 안을 **텍스트로** 두므로 텍스트로 읽는다.
  - **색상 칩이 사진보다 먼저 있는 타일 → 40px 칩이 상품 사진으로 저장.**
    v2.0.0의 깨진 주소와 같은 종류의 실패다 — **"사진 있음"으로 세어져서 검사가
    전부 ✅**. 판단 근거는 **몰이 스스로 적은 크기**뿐이다(가로·세로 둘 다 100px
    이하면 칩). 크기를 안 적은 사진은 절대 버리지 않고, 타일에 사진이 하나뿐이면
    작아도 그것을 쓴다(빽빽한 그리드의 레이아웃일 수 있다).
  - **`89,00 €`·`890 kr`·`89,00 zł`·`CHF 89.00` → 상품 0개.** 이게 가장 넓다:
    리더는 **가격을 찾아 타일로 올라가므로**, 돈을 못 읽는 몰은 가격 열이 빈 몰이
    아니라 **아무것도 없는 몰**이 된다. 유럽 절반이 숫자 뒤에 기호를 쓴다.
    이제 세 가지 순서를 읽는다(기호 앞·기호나 코드 뒤·코드 앞). 통화 문자는
    **닫힌 목록**이라 숫자 옆 아무 단어나 가격이 되지 않고, **기호 뒤에 숫자가
    오면 그 숫자의 것**이라 "Blouse Number 1 €89,00"이 "1 €"로 저장되지 않는다.
    `A$89.00`은 이제 `A$`까지 그대로 남는다(셀은 몰이 쓴 글자여야 한다).
  - **진단이 리더와 다른 질문을 하고 있었다**: `selfDiagnose`가 **PRICE_RE 복사본**을
    들고 있어서, 고친 뒤에도 유로 몰에 대해 "가격 leaf 0"이라고 답했을 것이다.
    이제 리더의 패턴을 그대로 빌려 쓴다(`generic._PRICE_RE`).
  - **"0개" 문장이 우리 잘못을 말할 수 있게 됐다.** 예전엔 "스크롤·지역 선택·차단"
    셋만 제안했는데, 유로 몰에서는 **그 셋 다 틀린 답**이었다(페이지는 멀쩡히 다
    그려져 있었다). 이제 사진에 **반복되는 상품 블록이 있고 읽은 가격이 0**이면
    "N개 상품 블록이 있었는데 가격을 하나도 못 읽었다 — 재시도할 일이 아니라
    우리가 고칠 일"이라고 적는다. 그래서 `rec.why`는 `rec.diag` **다음에** 쓴다.
  - PDP 원단 판독은 12가지 모양(JSON-LD material·description·스펙 표·불릿·아코디언·
    본문 산문·역방향·한글·할인 미끼·케어 문구 미끼·토큰 분리·조성 없음)을 실측한
    결과 **전부 정확**이었다 — 고칠 것이 없었고, 없는 페이지에서 지어내지도 않았다.
  - `shapes-e2e` 43건(수정 전 코드에서 18건 실패 확인) · `health-test` +4건.
- **엔진 커버리지는 측정해서 판단한다**(`scratchpad/coverage.js`, 실제 `SITES.active()`
  호출). Miro 스타터 리스트 623 URL / 132 브랜드 기준(2026-08):
  전용 어댑터 4(3%) · Shopify 페이지 내 감지 62(47%) · **generic 66(50%)**.
  → 팀 리스트의 **절반이 generic**이다. 어댑터를 하나 더 쓰는 것보다 generic을
  고치는 쪽이 항상 더 많은 브랜드에 닿는다. 새 리스트를 받으면 먼저 이걸 돌린다.
- **상세(PDP) 읽기는 사이트 무관 공용 함수다**(`readProductPage`/`fetchProductPage`).
  모든 쇼핑몰이 같은 두 곳에 같은 사실을 적는다 — schema.org JSON-LD Product
  (`material`·`color`·`size`·`brand`·`image`)와 **눈에 보이는 텍스트**(섬유 검증
  파서가 재구성). 조성은 `material` → `description` → `li` → 본문 순으로
  **빈 것만** 채운다. CSS 셀렉터가 없으므로 처음 보는 몰에서도 첫 스캔부터 나온다.
  houseBrand(aritzia·cos)와 **generic이 같은 함수를 쓴다**.
  **generic에 fetchDetail이 없던 것이 원단 열 결측의 최대 원인이었다**(v1.69 이전):
  어댑터 없는 66개 브랜드는 PDP에 조성이 적혀 있어도 구조적으로 영원히 빈칸이었다.
  generic에 **`lazyScroll: 12`도 추가** — 요즘 그리드는 스크롤해야 타일이 렌더되어
  로드 시점 DOM만 읽으면 첫 화면만 긁는다(전부 렌더된 사이트는 안정 감지로 ~1초만 손해).
- 상세 수집(옵션): 원단 조성(섬유 검증), 색상 전체 목록, 사이즈, 브랜드, 정가
- **리스트는 스스로 정해진 시각에 스캔한다**(v1.80.0, 사용자 요청). LAB이 주차별로
  비교하는데 "아무도 안 돌린 주"는 조용한 주가 아니라 **추이의 구멍**이다. 그래서
  리스트마다 시각을 갖는다: `list.schedule = {on, time:"09:00", days:[1..5]}`
  (0=일요일, 빈 배열=매일, **사용자 로컬 시각**).
  - 패널: SCOPE 도구 줄의 **⏱**(기존 줄이라 닫혀 있을 땐 높이 0) → 한 줄
    (시각 · 요일 칩 7개 · Turn on/off). 켜기 전에 **"언제 돌지"를 먼저 보여준다**.
    예약된 리스트는 칩에 ⏱가 붙어 안 열어도 보인다.
  - 워커: `chrome.alarms` 리스트당 1개(`wpb_run_<listId>`), 발동 후 재예약.
    다음 시각은 패널과 워커가 **같은 `ScanLists.nextRun`**을 부른다 — 패널이
    약속한 분을 알람이 못 지키는 일이 구조적으로 없다.
  - 실행 중이면 **10분 뒤로 미룬다**(두 스캔이 한 큐를 공유하면 엑셀이 뒤섞인다).
    미룬 알람은 `finally`의 재예약도, 리스트 편집 시 `syncSchedules`도 덮지 않는다
    (더 이른 알람은 그대로 둔다) — 안 그러면 바쁜 아침이 조용히 하루 건너뛰기가 된다.
  - 권한은 요청하지 않는다(클릭이 없으므로). 이미 허용한 오리진만 돌고 나머지는
    종전대로 실패로 보고된다. **크롬이 켜져 있어야** 하고, 놓친 약속은 다음에
    크롬을 열면 곧 실행된다(건너뛰지 않음).
  - `alarms` 권한 추가. `schedule-test` 19건 · `sched-e2e` 13건 · `schedui-e2e` 21건.
- **데이터는 측정해서 관리한다**(v1.82.0, 사용자 질문에서 출발). 실측:
  상품 1개당 **약 283바이트**(IndexedDB 오버헤드 포함), 브라우저 할당량은 **약 150GB**.
  8개 리스트를 주 1회 스캔하면 연 2.5만 개 ≈ **7MB** — **디스크는 제약이 아니다.**
  진짜 한계는 두 가지였다:
  - **① 실행 중인 큐가 10MB 벽에 부딪힌다**(실측 확인). 수집한 행을 `wpb_queue`에
    담았는데 `chrome.storage.local`은 10MB가 상한이고, 팀 리스트 456개 × 60 =
    **13.7MB → `kQuotaBytes quota exceeded` → 그 실행의 엑셀이 통째로 사라진다.**
    (예약 스캔이 생기면서 아무도 안 보는 새벽에 일어날 수 있는 사고가 됐다.)
    → 행은 **IndexedDB `runrows`**로 옮겼다(워커 경유, 페이지 오리진은 확장 DB에
    접근 못 함). 큐 레코드에는 `runCount`만 남는다. URL마다 전체를 다시 직렬화하던
    비용도 같이 사라졌다. 행은 **다음 실행이 시작될 때** 비운다 — 그전까지는
    재스캔 없이 엑셀을 다시 만들 수 있다.
  - **② 커지는 건 LAB이 매번 하는 일**이다: `allProducts()`가 전 테이블을 읽고
    행마다 수리한 뒤 추이를 계산한다. 그래서 관리 대상은 디스크가 아니라 **작업
    집합**이다. **주간 스냅샷(주당 수 KB)이 이걸 안전하게 만든다** — 상품을 지워도
    그 주의 숫자는 남는다. LAB 헤더의 **용량 칩**(예: `7.3 MB`)이 상시 표시되고,
    누르면 "최근 N개월만 유지"로 정리한다(제거/유지 개수를 항상 말한다).
    **자동 삭제는 절대 하지 않는다** — 사람이 창을 정할 때만.
  - **카탈로그는 브라우저마다 따로 산다**(v1.83.0, 사용자 질문). 팀원 각자의
    크롬에 각자의 IndexedDB가 있고 서로를 모른다 → ① 노트북이 바뀌면 기록이
    사라지고 ② 넷이 나눠 스캔해도 **아무도 팀 전체 추이를 못 본다**.
    서버 없이 푸는 방법은 **파일**이다 — 병합 규칙이 이미 정의돼 있으므로:
    상품 신원=URL(같은 옷은 한 행), **가장 이른 addedAt이 이김**(신상 수가
    부풀지 않음), listIds는 **양쪽 합집합**(가져온 행이 들고 온 것도 포함하도록
    `merge` 수정), 주간 스냅샷은 **줄어드는 쪽으로 덮어쓰지 않음**.
    LAB 용량 칩을 누르면 열리는 줄에 **Free up space… · Back up (.json) ·
    Merge in a backup…** 셋. 한 브라우저가 전원의 파일을 받으면 그게 팀 LAB이 된다.
    운영 권고: 같은 몰을 여럿이 중복 스캔하지 말 것(몰 입장에선 N배 트래픽이고
    결과도 같다) — 리스트를 사람별로 나누고 주 1회 한 명이 병합.
  - **실제 부하로 측정했다**(1인 35브랜드 × 55상품 × 주 1회, v1.83.1):
    1년 ≈ **2.7만 개**(주간 신상 25% 가정). 예전엔 LAB 첫 화면까지 **4.8초**,
    전부 신상인 최악(10만 개)이면 **20.5초**였다. 원인은 조회가 아니라
    **상품 카드를 전부 그리는 것**이었다(읽기+수리는 0.5초).
    → 그리드를 **300개씩** 그린다(“Show 300 more” · “300 of 27,000 shown ·
    exports and reports use all 27,000”). **내보내기·리포트는 항상 전체**를 쓴다.
    2.7만 개 **1.9초**, 10만 개 **6.7초**로 내려갔다. 2만7천 장을 스크롤하는
    사람은 없다 — 실제로 읽는 건 필터와 분석 탭이다.
  - `storage-e2e` 28건(옛 방식이 실제로 quota 초과로 실패하는 것 + 병합 정확도까지
    고정) · 측정 도구 `storage-probe.js`.
- **엑셀은 누를 때만 나온다**(v2.2.0, 사용자 요청 "실제 뽑겠다는 아이콘을 누르기
  전까지는 자동으로 추출할 필요없으니"). 실행의 일은 **카탈로그를 채우는 것**이고,
  그건 몰마다 끝나는 즉시 끝난다(`catalogSave`) — PRODUCTS·LAB은 파일과 무관하게
  이미 갖고 있다. 파일을 만드는 건 **그 실행의 사진을 전부 다시 내려받는 일**이라,
  LAB을 보려고 스캔한 사람에게는 순수한 비용이다.
  - 실행 바에 **⬇** 하나가 늘었다(같은 줄, 높이 비용 0). 끝난 실행에 행이 있을 때만
    깨어 있고, 누르면 그때 만든다. 행은 **다음 실행이 시작될 때** 비워지므로 그
    전까지는 재스캔 없이 언제든 뽑을 수 있다.
  - 만드는 곳은 **패널**이다(패널이 ExcelJS를 이미 싣는다) — 스캔했던 탭이 닫혀
    있어도 된다.
  - 실행의 마지막 줄도 파일 얘기를 그만두고 **"N개가 PRODUCTS·LAB에 있다 · 주의가
    필요한 사이트 M개 · ⬇로 엑셀"**로 바뀌었다.
- **같은 상품 URL은 최신 값으로 갱신된다**(v2.2.0에서 테스트로 고정). `store.merge`는
  비어 있지 않은 새 값이 옛 값을 **이긴다** — 몰이 사진을 다시 찍거나 가격을 바꾸면
  그게 반영된다. 바뀌지 않는 것은 `addedAt`(첫 관측)뿐이고, 그건 LAB의 "신상"
  기준이라 의도된 것이다. `rescan-e2e`가 재촬영·재가격을 실제로 검사한다.
- 리스트 실행(Run all): URL마다 파일이 떨어지지 않는다. 각 URL은 카탈로그에만 쌓고,
  **리스트가 끝나면 xlsx 한 개**를 만든다(중간에 Stop해도 그때까지 분을 저장).
  URL당 상한은 **60개 고정**(v1.68, 사용자 요청 "무조건 첫 페이지만") — 패널에
  선택지가 없다. 무한 스크롤 몰은 "현재 페이지"가 카테고리 전체라 상한이 없으면
  수백 개로 불어나는데, 60이 대략 첫 "더 보기" 이전 = 디자이너가 실제로 보는 분량.
- **카테고리는 사용자가 붙인 이름이 이긴다**(v1.84.0). 몰은 자기가 내준 컬렉션만
  안다 — Alo의 패싯 URL은 Tanks든 Hoodies든 `/collections/new-arrivals`라서
  주소에서 카테고리를 뽑으면 **서로 다른 리서치 질문 넷이 한 제목 아래로 뭉친다**.
  엑셀·LAB이 묶는 기준이 바로 그 열이므로, 리스트 항목의 label이 있으면 그것을
  쓴다(Grab 카드에서 사용자가 그 페이지를 보며 직접 적은 이름). **브랜드는 반대로
  빈 칸만 채운다** — Walmart·Target처럼 한 페이지에 여러 브랜드가 있으면 행마다
  자기 브랜드가 맞다. 패싯이 다른 두 URL은 `collectionSig`가 쿼리를 보므로
  건너뛰지도 않는다. `facet-e2e` 10건.
- 출력: **7컬럼 xlsx** — 브랜드 · 카테고리 · 상품명 · 썸네일 · URL · 원단 · 혼용률.
  행은 **브랜드 → 카테고리** 순으로 묶는다. 같은 브랜드+카테고리 안에서는
  사이트 노출 순서를 그대로 둔다 — 그게 MD의 랭킹이다(정렬은 stable).
  원단(섬유명)과 혼용률(비율)은 별도 컬럼. 혼용률은 페이지 텍스트를 잘라 오는 게
  아니라 실제 발견된 `숫자% 섬유` 쌍으로 **재구성**한다(JSON-LD 안에 조성을 넣는
  Zara류에서 마크업이 새는 것을 원천 차단). 출처 규칙 — 실측값 검정 /
  미확인 "정보 확인" 빨강 + 원인.
  **조성 파서는 양방향·한글**을 읽는다(v1.68): "95% Cotton 5% Elastane"뿐 아니라
  "Cotton 95%, Spandex 5%"와 "폴리에스터 87% 스판덱스 13%"(Alo /ko-kr/)도.
  두 방향이 서로의 모양을 가운데에 품고 있어("87% 스판덱스") **짝을 더 많이
  설명하는 해석이 이긴다**. 한 글자 섬유(면·마·견·울)는 한글이 양옆에 붙으면
  무효(화면·겨울 오탐 방지), off/sale/할인 줄은 통째로 제외. `compko-test` 16건.
- **담기는 페이지에서, 정리는 패널에서**(v1.70, 사용자 요청). 디자이너는 브라우징
  중에 볼 카테고리를 발견한다 — 한 건 담자고 사이드 패널로 시선을 옮기면 그 흐름이
  끊긴다. 그래서 **페이지 우하단 원형 Grab 버튼**(`fab.js`, Shadow DOM)이 담기를
  전담한다: 누르면 브랜드·카테고리가 이미 채워진 카드가 뜨고(둘 다 그 자리에서 수정
  가능 — "입맛대로"), 리스트가 **색 칩**으로 나열되며, 칩을 누르면 그 리스트에 들어간다
  (+1 애니메이션 → 0.95초 후 자동 닫힘, 버튼에 개수 배지). 같은 URL 재담기는 추가가
  아니라 **갱신**이고, 이미 담긴 리스트 이름을 말해 준다. `＋ New list`도 카드 안에 있다.
  - **이름 규칙은 `lists.js`에 하나만 둔다**(`brandFromHost`·`cleanLabel`·`brandFor`).
    Grab과 패널이 각자 이름을 만들면 같은 몰이 엑셀에서 두 그룹이 된다. 그래서
    `lists.js`를 **content_scripts에도 주입**한다(`ENGINE_FILES`에도 추가).
  - FAB은 더 이상 스캔 제어판이 아니다(구버전은 좌하단 다크 패널 + 필터 + Pause/Reset).
    스캔 제어는 패널 하단 바로 갔다. FAB에 남은 실행 버튼은 "지금 보는 이 페이지"
    하나뿐 — 그건 여기서 시작하는 게 왕복이 없다.
  - **버튼은 주소가 아니라 몰에 붙는다**(v1.91.0, 사용자 보고 "새로 사이트에 들어가면
    아이콘이 반영이 안되서 새로고침을 해야하네"). 정적 content_script는 **경로로**
    매칭되고 팀 리스트는 경로로 적혀 있다(`www.walmart.com/browse/*`·`/c/*`·`/shop/*`).
    그래서 **권한을 다 가진 몰인데도** 홈·상품 페이지·목록에 없는 주소 모양에서는
    스크립트가 안 돌아 **버튼이 없다**. 실측: `/browse/women` ✅ · `/` ❌ · `/ip/…` ❌.
    밖에서 보면 "될 때만 되고 새로고침하면 낫는다"로 보이는데, 새로고침이 듣는 건
    그 주소가 마침 매칭될 때뿐이다.
    → 진짜 질문은 **권한**이고 그건 몰 단위로 답이 있다. 워커가 `tabs.onUpdated`에서
    **오리진 권한이 있으면 경로와 무관하게 엔진을 넣는다**. 권한이 없으면 url 자체가
    안 오므로 **나머지 웹에는 비용이 0**이다(새 권한 없음).
    - 페이지가 다 그려진 뒤(`complete`)에만 넣는다. 로딩 중 url 변화는 뒤따르는
      `complete`에 맡긴다 — 반쯤 지어진 페이지에 주입하지 않는다.
    - **몰이 페이지 안에서 라우팅**해도(`changeInfo.url` + 이미 complete) 같은 취급.
      우리가 가진 오리진에서만 url이 오므로 이게 정확히 대상 집합이다.
    - **허용한 순간이 버튼이 나타나야 할 순간**이다: 등록(`registerContentScripts`)은
      *다음* 로드부터라 이미 열려 있는 탭은 영영 빈다 → `permissions.onAdded`에서
      `armGrantedTabs()`가 그 탭들에 직접 넣는다.
    - 권한이 아예 없는 새 몰은 여전히 버튼이 없다 — 크롬이 클릭을 요구하므로
      패널의 `＋ This page`가 그 클릭이고, 한 번 허용하면 그 뒤로는 항상 나온다.
    - `fabreach-e2e` 18건(같은 몰의 네 주소 · 미허용 몰 · 페이지 내 라우팅 ·
      이중 주입 없음 · 패널 ◉/◎ 숨기기 유지).
  - **숨기기는 설정이지 편도가 아니다**(v1.77). `Hide button`이 버튼을 없애고
    되돌릴 길을 아무 데도 남기지 않아서, 담기의 주 경로가 영영 사라졌다.
    이제 패널 SCOPE 헤더의 **◉/◎ 토글**(기존 아이콘 줄이라 높이 비용 0)이 켜고 끄고,
    `fab.js`가 `wpb_fab_hidden` 변화를 듣고 있어 **보고 있는 탭에서 바로** 되돌아온다.
  - 패널의 담기 카드는 사라지고 `＋ This page`가 **Import·Paste와 같은 크기의 3분할**로
    남는다 — Grab이 닿지 못하는 페이지(권한 없는 새 몰)의 폴백이라 없애지는 않는다.
- **LAB이 안 열리던 원인은 DB 잠금이었다**(v1.88.0). 릴리스가 스토어를 추가하면
  DB 버전이 오르는데, **리로드 전 코드로 돌던 서비스워커가 옛 커넥션을 쥐고 있으면**
  IndexedDB가 업그레이드를 막고 `open()`이 영원히 안 끝난다 — 콘솔은 조용하고
  화면은 "Loading…"이라 **"LAB에 들어갈 수가 없다"**로 보인다. 이제 모든 커넥션이
  `onversionchange`에서 스스로 물러나고(자동 해결), 그래도 막히면 **"확장을 ↻ 하라"**를
  화면에 적는다(무한 대기 금지). `labopen-e2e` 6건.
- **실행 바의 세 컨트롤은 각자 이름을 말한다**(v1.88.0, 사용자 요청). 회색 글리프
  둘로는 어느 것이 홀드고 어느 것이 정지인지 알 수 없었다. **▶ Scan all · ⏸ Pause ·
  ■ Stop**, 그리고 홀드 중에는 그 버튼이 **▶ Resume**으로 아이콘까지 바뀐다
  (같은 버튼의 두 상태이므로 갈 상태를 보여준다).
- **Grab 카드는 담기 하나만 한다**(v1.88.0, 사용자 요청). "Scan this page"(한 장짜리
  스캔 → 엉뚱한 엑셀)와 "Hide button"(주 경로를 없애는 편도)을 제거했다. 숨기기는
  패널의 ◉/◎ 그대로. FAB 아이콘은 **툴바 아이콘과 같은 렌즈·같은 주황 그라디언트**로
  통일(`#d2691e→#f0a04b`) — 하나의 제품에 돋보기가 둘일 이유가 없다.
- UI: **사이드 패널**(툴바 아이콘 = 리서치 컴패니언) + 페이지 우하단 Grab 버튼.
  패널은 탭이 바뀔 때마다 페이지를 읽어 상황을 말하고 **맞는 행동 하나를 제안**한다
  (지원 쇼핑몰=전체 스캔 / 그 외=상품 담기 / 미허용 사이트=권한 요청). 스캔 중에는
  진행 상황을 그대로 중계. ⏸/▶/✕, 작업 서명으로 이전 작업 혼입 차단.
  실행 컨트롤은 **패널 하단 고정 바의 아이콘 3개**다(v1.70, 사용자 요청 "Scan /
  PAUSE / RESET을 아이콘으로 형상화해서 간단하고 심플"): **▶ 실행**(유일하게 글자를
  달고 있고 solid) · **⏸ 홀드** · **■ 정지**, 모두 원형. 홀드·정지는 실행 중이 아닐 때
  잠들어 있고(disabled), 홀드 중일 때만 색이 들어온다 — 색은 상태에만 쓴다는 규칙 그대로.
  정지는 물어보고 멈추며 안전한 쪽(수집분 Excel 저장)이 기본 동작이다.
  상한 셀렉터는 여전히 없다(첫 페이지 60 고정).
  **움직이는 부분은 브랜드 목록 하나뿐**이다 — LAB 카드·리스트 칩·담기 카드·필터는
  위에 고정, 실행 바는 아래에 고정, 그 사이에서 `#listbody`만 스크롤한다.
  (수백 개를 스크롤하다가 "지금 담기" 버튼이나 실행 버튼을 찾으러 되돌아가지 않는다.)
  컨트롤은 현재 보고 있는 탭이 아니라 **작업 중인 탭**(queue.tabId)으로 간다.
  **LAB 진입 카드가 패널에서 가장 눈에 띄는 요소**다 — 유일한 솔리드 잉크 블록.
  **패널은 흰색·무채색·박스다**(v1.98.0, 사용자 요청 "누리끼리한 색상이 아닌 흰색과
  무채색, 박스로 이루어진 에디토리얼"). 바탕이 따뜻한 그레이지(`#efece5`)였는데
  그게 화면 전체에 도는 색조로 읽혔다 — **옷 사진의 색을 판단하는 도구가 그 옆에
  자기 색을 두면 안 된다.** 이제 `--bg:#f2f2f2` · `--card:#fff` · 헤어라인
  `#e6e6e6` · 모서리 `--r:3px`. 그림자 0개(전부 선으로), 알약 모양 0개(전부 박스).
  주황 액센트는 **실행 중 상태 하나에만** 남는다.
  - **실행 바는 아이콘만**(v1.98.0, 사용자 요청). v1.88.0에서 이름을 붙인 이유는
    "회색 글리프 둘로는 홀드와 정지를 구분 못 한다"였는데, 지금은 ▶ · ⏸ · ■ 세
    모양이 다르고 홀드는 눌린 동안 ▶로 바뀐다(갈 상태를 보여주는 규칙 유지).
    이름은 `title`·`aria-label`에 그대로 있어 호버 한 번, 스크린리더 한 번 거리다.
  - **담는 세 길은 한 단어씩**: `＋ Page` · `⬆ Import` · `⌨ Paste`. 셋 다 같은
    일(리스트에 사이트 넣기)이라 "list"를 세 번 반복하고 있었다.
  - **SCOPE 라벨 제거**. 카드가 하나만 담고 아래 행들이 그게 뭔지 말한다 — 열 때마다
    다시 읽는 단어는 가구다. 그 줄을 통째로 도구 6개에 줘서 **22px → 26px**로 키웠다.
    (도구 중 중복은 없다 — 이름변경·삭제·txt·xlsx·그랩버튼·예약은 서로를 대신하지
    못한다. 문제는 개수가 아니라 크기였다.)
  - **빈 리스트에 설명문을 두지 않는다**(사용자 요청). 바로 위 버튼 셋이 채우는
    세 가지 방법이고 이름이 붙어 있다 — 그걸 소리 내어 다시 읽는 문단은, 정작
    리스트가 있어야 할 자리에 놓인 가구다. `EMPTY` 한 단어만 남긴다.
  UI 언어는 **iOS 그룹 리스트**다(v1.62, 사용자 요청 "아이폰 UI처럼"): 바탕에
  **흰 카드**가 떠 있고, 카드 자체가 그룹핑이라 테두리가 필요 없다.
  **바탕은 중성 회색이 아니라 따뜻한 그레이지**(`#efece5`)이고 모서리는 16px이다
  (v1.70, 사용자 요청 "너무 딱딱하고 눈에 들어오지 않아"). 중성 회색 + 각진 모서리는
  *채워 넣을 양식*으로 읽히고, 같은 레이아웃이 따뜻한 바탕 + 넉넉한 라운드에서는
  *손에 쥐는 물건*으로 읽힌다 — 구조는 그대로 두고 온도와 반경만 바꿨다.
  섹션 제목은 카드 안 맨 위, 행은 44px에 **인셋 구분선**(카드 끝이 아니라 글자
  시작점부터), 탭은 **세그먼티드 컨트롤**, 모서리 10px, 버튼은 대문자 트래킹 대신
  문장형. 틴트는 iOS 블루가 아니라 **검정을 유지**한다 — 승인된 CTA 색이고 일반
  iOS 클론처럼 보이지 않는다. **다크 모드를 따라가지 않는다**: 브라우저 테마를 따르면
  디자이너가 흰 패널 옆에서 검은 LAB을 열게 되고, 옷 사진의 색 판단은 흰 바탕에서
  해야 한다. 팀의 Market Research 화면과 같은 언어이고, 패널과 LAB이 같은 토큰을
  쓴다: 흰 바탕, 얇은 헤어라인, 각진 모서리(`--r:4px`), 대문자 마이크로 라벨.
  **패널 카드는 두 개다**(v1.63): **SCOPE**와 하단 실행 바.
  이름은 행이 아니라 **목적**을 부른다(v1.74, 사용자 요청) — "Sites"는 담긴 것을
  설명하지만, 실제 물건은 *이번 리서치의 관찰 범위*다. **패널과 LAB이 같은 단어**를
  쓴다(같은 물건이므로). 카드 제목이 아니라 **마이크로 라벨**이라 화면을 거의 쓰지 않는다. 리스트 고르기·
  페이지 담기·목록 보기·Import는 전부 *같은 물건(리스트)에 대한 조작*이라
  네 카드로 갈라 두면 배워야 할 기능 넷으로 읽힌다 — 합치면 관리하는 물건 하나로
  읽히고, 그게 사실이다. 스캔만 성격이 다른 행위(밖에 나가서 일함)라 카드를 유지한다.
  카드 헤더는 **아이콘 + 제목만**(설명 줄 제거, 사용자 요청) — 헤더 오른쪽
  아이콘 열은 **리스트 자체를 다루는 것만**(✎ 이름변경 · 🗑 삭제 · .txt · .xlsx 내보내기).
  **사이트를 넣는 세 가지 길은 한자리에 모은다**(v1.65, v1.70에서 3분할 동급으로):
  `＋ This page` · `⬆ Import list` · `⌨ Paste URLs`. 같은 일(리스트에 사이트 추가)인데
  하나는 헤더 아이콘, 하나는 맨 아래 접힘이면 같은 일로 보이지 않는다. 한 건 담기는
  이제 페이지의 Grab 버튼이 주 경로라 여기서는 셋 중 하나로 동급이다.
  붙여넣기 상자는 누를 때만 열린다 —
  4줄짜리 textarea가 상시 자리를 차지하면 정작 사이트 목록이 화면 밖으로 밀린다. LAB의 탭만 예외로
  아이콘+이름+부제 2줄이다(📈 LAB·Weekly trends / 🆕 New In·Arrivals by week /
  🏷️ By Brand / 🧺 Products / 🗂️ Scan lists), 본문 섹션 제목에도 픽토그램. 색은 상태에만 쓴다(실행 중·스캔 중인 행).
  **솔리드 검정은 그룹당 하나까지** — LAB 카드와 실행 바의 ▶ 하나가 가져간다
  (똑같은 버튼 두 개는 하나로 읽힌다).
  - **LAB 툴바도 같은 라벨 박스**로 묶는다 — FIND / NARROW / PERIOD / ORDER /
    TAKE AWAY. 드롭다운 9개가 한 줄에 늘어서면 무엇이 무엇인지 읽히지 않는다.
    (툴바는 PRODUCTS 그리드에 작용하므로 LAB 탭에선 통째로 숨고, 피드/브랜드
    탭에선 검색 그룹만 남는다.)
  - **리스트는 칩**이다(드롭다운 아님): 리스트가 몇 개고 각각 몇 개인지 가만히 있어도
    보인다. 리스트별 도구(✎·🗑·Export)는 열린 리스트 아래에만 나온다.
  - **담길 값은 담기 전에 보인다** — 브랜드 · 카테고리. 엑셀·LAB이 묶는 기준이 그
    둘이라 틀린 것을 발견할 자리가 여기다. **Grab 카드의 입력 필드 두 개**가 그 자리다.
    패널의 상태 줄(`#now`)은 **제거**(v1.73) — 담기는 페이지에서 하므로 탭을 중계하던
    그 줄은 평생 "Browser page — can't be added"만 말하고 있었다. 폴백 버튼이
    자기 조건을 title로 말한다(`Add as 브랜드 · 카테고리`).
  - **행 라벨이 주소면 카테고리가 아니다**(`labelFromUrl`, v1.73). 옛 import가
    "cottonon.com/AU/co/women/wom…"을 라벨로 저장해 두면 무엇을 보는 행인지 알 수 없다.
    URL 경로에서 **몰이 직접 쓴 슬러그**를 꺼내 쓴다(로케일·라우팅 단어·불투명 코드
    `n1jux6`·`cat90030`는 건너뛰고 위로 올라간다). 추측이 아니라 몰의 표기다.
    `label-test` 13건.
  - **목록은 브랜드별 접이식 블록** + 브랜드명에서 파생한 색 점(저장하지 않으므로
    어디서나 같은 색) + 개수. 스타터 리스트를 넣으면 수백 행이라 **7개 이상일 때
    필터**가 나타나고, 필터 중에는 어떤 그룹도 접힌 채로 두지 않는다(가려진 매치는 버그).
    Scan은 기본값이라 태그를 달지 않는다 — 예외만 표시한다.
    **행은 브랜드·카테고리만 보인다**(v1.68, 사용자 요청): 주소 줄은 없고 URL은
    행 툴팁과 ↗에만 산다. **플랫폼 라벨은 브랜드가 될 수 없다** — "Shopify store"로
    저장된 옛 항목은 로드 때 도메인 브랜드(Edikted)로 소급 수리되고(`repairNames`),
    새 담기는 `platform` 플래그(shopify·generic 어댑터)를 보고 처음부터 도메인
    브랜드를 쓴다. 이 브랜드가 실행 때 빈 행을 채우므로 엑셀 브랜드 열도 같이 고쳐진다.
    라벨은 `cleanLabel`이 "… | ZARA United States"류 꼬리(브랜드/도메인/보일러플레이트
    세그먼트만)를 떼고 저장한다 — 진짜 카테고리에 붙은 하이픈은 건드리지 않는다.
  진행 상태 줄은 작업이 끝나면 지운다 — 남아 있는 "저장됨…"은 아직 도는 것처럼 보인다
  (원인: `report()`가 job 전체를 읽고-쓰기 하는데 늦게 도착한 콜백이 끝난 job을
  되살렸다. 이제 active가 아니면 쓰지 않고, 카탈로그 저장도 await 한다).
- **clip(상품 담기)는 제거됨**(v1.47). "＋ Add this page"와 구분이 안 되는 두 번째
  담기 경로였고, 손으로 고른 표본이라 LAB 통계에서는 어차피 제외해야 했다.
  `clip.js`·우클릭 메뉴와 함께 **`contextMenus` 권한도 반납**했다. 이미 담긴
  `source:"clip"` 행은 카탈로그에 그대로 남는다(삭제하지 않음).
- **PULSE가 LAB의 첫 답이다**(v1.75, 사용자 요청 "지표 변화·상승/하락 추이").
  스캔 직후 디자이너가 읽는 건 차트 여덟 개가 아니라 **표 하나**다 — 지표 한 줄에
  ① 지난 스캔 대비 변화(%p) ② **스파크라인**(최근 8구간의 모양) ③ 점유율과 상품 수.
  `MEASURE` 셀렉터로 원단·색상·디자인키워드·브랜드·카테고리·Tier 전부 같은 표로 본다.
  - 변화는 **like-for-like**(`weekCompare`의 matched)라, 이번 주 새로 담은 브랜드가
    올린 수치는 트렌드로 올라오지 않고 커버리지 줄로 간다.
  - 스파크라인은 **원본 share 히스토리**다(구간마다 기준이 달라지는 선은 더 나쁘다).
    각 줄이 자기 범위로 스케일되므로 2→4%도 40→80%만큼 읽힌다. 정확한 값은 옆 칸.
  - **STABLE의 폭은 표본이 정한다**: `max(1.5%p, 상품 1개분 share)`. 20개짜리 주에
    상품 1개는 5%p라 그걸 상승이라 부르면 노이즈가 표 맨 위로 온다. 400개 주에선
    2%p가 12벌이라 안정이라 부르면 진짜 변화를 숨긴다. **그 폭을 화면에 적는다**.
  - `pulse-test` 26건 · `pulse-e2e` 22건.
- **LAB의 첫 화면은 원단과 디자인 디테일 둘이다**(v1.87.0, 사용자 요청). 디자이너의
  질문은 "원단이 어떻게 움직이나"와 "디테일이 어떻게 움직이나" 둘인데, 그 둘이
  MEASURE 셀렉터의 8개 중 하나씩이라 **둘을 같이 보려면 전환하고 앞의 것을 외워야**
  했다. 이제 LAB 최상단에 **🧵 Fabric · ✂️ Design detail**이 나란히 서고, 각각
  **장기 추이선 + 지금 상위 3개 + 상승/하락**을 달고 있다. 셀렉터는 그대로 아래
  섹션들을 움직인다(색상·브랜드를 보고 싶은 주가 있으므로).
  - **디자인 키워드의 원천이 상품명에서 상품 상세로 넓어졌다.** "Airbrush Tank"에는
    스퀘어넥도 셔링도 없다 — 그 단어는 **상세 페이지의 몰 자기 문구**에 있고,
    조성을 가지러 이미 그 페이지에 간다. 이제 그 문구를 **가져와서 저장**한다
    (`design`, 400자 상한 · 설명 + 스펙 불릿). Shopify는 자기 JSON의 `body_html`로
    같은 것을 **PDP 요청 없이** 얻는다(팀 리스트의 47%).
  - **세탁 라벨은 뺀다** — wash·tumble dry·imported·style #·"model is 5'9\""는 모든
    옷에 다 있어서 진짜 어휘를 덮어버린다.
  - **키워드가 아니라 텍스트로 저장**한다: 무엇이 디테일인지 정하는 어휘는 리포트
    층에 있고 계속 좋아진다 — 텍스트로 두면 **재스캔 없이** 옛 스캔도 개선된 판독을
    받는다. `axes-e2e` 17건.
- **분석은 New In 페이지만 읽는다**(v2.3.0, 사용자 요청 "NEW로 작업된 상품들만
  반영해야 주차별로 어떤 상품이 보이고, 원단·핏 키워드가 달라졌는지 알 수 있으니까").
  추이는 "이번 주와 지난 주가 어떻게 다른가"를 답하는데, **두 주가 같은 종류의
  표본일 때만** 성립한다. 몰의 New In은 몰이 "이번에 낸 것"이라고 말한 것이고,
  Tops는 몇 달 된 진열 전체다 — 섞으면 **한 몰을 처음 스캔한 주가 거대한 신상 주로
  잡히고**, 그 아래 원단·핏 수치는 시즌이 아니라 재고를 설명한다.
  - **무엇이 New인지는 디자이너가 붙인 이름이 정한다** — Grab 카드에 적은 라벨이고
    그게 곧 카테고리 열이다. 그래서 재스캔이 필요 없고, 이름을 고치는 순간 바뀐다.
    (`NEW_LABEL` — new·new in·new arrivals·just in·latest·drop·신상 …)
  - **화면이 스스로 말한다**: "수집한 N개 중 M개가 New In 페이지에서 왔고 분석은
    그것만 읽는다". 전체의 일부로 낸 수치가 전체에 대한 수치로 보이면 안 된다.
    New In 페이지가 하나도 없으면 **빈 화면이 아니라 이유**를 적는다.
  - **주간 스냅샷도 같은 모집단으로 얼린다**(`<list>|new`·`new`). 옛 전체-카탈로그
    기록을 덮어쓰지 않고 **자기 id로 따로 쌓는다** — 다른 모집단이 서로의 기록을
    고쳐 쓰면 안 된다는 v1.72 규칙 그대로.
  - **MEASURE는 원단 + 이름 키워드 3축만** 남는다(가격은 자기 섹션이 있어 셀렉터가
    필요 없다). 색상·브랜드·카테고리·Tier는 제거 — 그건 "누가·어디서"이고 그건
    New In·By Brand 탭이 답한다. 이 탭은 "시즌이 무엇으로 만들어졌나"를 답한다.
  - **New In·By Brand·PRODUCTS는 그대로 전부 보여준다**(사용자 요청) — 브라우징은
    전수가 맞다. 좁히는 것은 분석뿐이다. `newin-e2e` 12건.
- **INSIGHTS는 옷 종류로 가른다**(v2.4.0, 사용자 요청 "가격, 키워드, 원단을 기준으로
  카테고리별로"). **카테고리 열로는 가를 수 없다** — v2.3.0 이후 분석은 New In만 읽어서
  그 열은 모든 행이 "New in"이다. 그건 디자이너가 그랩한 **페이지 이름**이지 옷 이름이
  아니다. 같은 페이지로 들어온 원피스와 레깅스가 한 숫자가 되면 "린넨이 올랐다"는
  실제로는 "원피스가 린넨이고 이번 주에 원피스가 많았다"이다.
  - 판정은 **화면을 그릴 때** 두 단계로 한다: ① 몰이 스스로 말한 product_type
    (Shopify가 제공 — 카테고리 열은 사용자 라벨이 이기므로 **행에 따로 보관**한다)
    ② 상품명. 저장하지 않으므로 **이미 수집한 행에 재스캔 없이** 적용되고, 어휘를
    고치면 옛 스캔도 같이 좋아진다(v1.87의 design 텍스트와 같은 원리).
  - 몰의 단어도 **같은 어휘를 통과시킨다** — "T-Shirts"·"Tees"·"Tops"는 한 서랍이고,
    몰마다 서랍을 만들면 브랜드 간 비교라는 목적 자체가 사라진다. 활동복처럼 이름에
    옷 종류가 없는 상품(Alo "Alosoft Bliss")은 **몰의 product_type만이** 제자리를 찾아준다.
  - 좁히면 **가격·키워드·원단이 한꺼번에** 따라간다(하나의 필터, 탭마다 다시 거는
    필터가 아님 — v1.71의 리스트 스코프와 같은 규칙).
  - **얼린 주간 기록은 종류로 못 나눈다** — 전 모집단의 한 숫자다. 그래서 종류를 고르면
    스냅샷을 읽지 않고 **그 사실을 화면에 적는다**(전체 수치가 한 종류의 이름 아래
    놓이는 일 금지).
  - **자기 높이를 스스로 벌었다**: 티어 칩·종류 칩·출처 문장은 셋이 한 가지를 말하므로
    **한 줄**(`.labctx`)로 합치고, 카운트 타일은 두 축 차트 **아래로** 내렸다
    (v1.87이 선언한 "첫 화면은 원단·디테일"을 레이아웃이 비로소 지킨다).
    첫 답까지 **274px → 201px**(1280×800, 예산 266px).
  - **`compact-e2e`의 LAB 예산이 v2.3.0부터 아무것도 재고 있지 않았다** — 픽스처가
    Tops/Tanks라 LAB이 빈 화면이었고 `section.sec`이 없어 0px으로 통과했다.
    (그 사이 v2.3.0이 실제로는 274px로 예산을 넘겨 있었다.) 픽스처를 New In으로
    고쳐 다시 재게 했다. 같은 이유로 `tier-e2e`·`snapshot-e2e`·`lab-test`도 수리.
  - `bytype-e2e` 20건(수정 전 코드에서 11건 실패 확인).
- **LAB**(구 CATALOG 탭, `catalog.html` 기본 화면): 쌓인 스캔을 주차별로 보는 화면.
  ① 주차별 최근 변화(데이터 있는 마지막 두 구간 비교) ② 지금 주목할 키워드
  (신규 / N구간 연속 상승 — **근거를 항상 함께 표시**) ③ 구간별 신규 수
  ④ 점유율 추이 ⑤ 기간 전체 상승·하락 ⑥ **가격·세일 압력** ⑦ 주차별 기록 표.
  - 기준(dim) 기본값은 **원단**이다. 사용자가 원단 담당이고 조성은 실측값이라
    가장 강한 신호다. 색상이 그 다음, 디자인 키워드가 그 다음.
  - **가격·세일 압력**(`priceByPeriod`): 구간별 중앙가/평균가/범위/세일 비중/
    평균 인하율. 세일 비중이 주마다 오르면 상품이 바뀌기 전에 시즌 종료·재고
    압박이 먼저 보인다. 중앙값을 함께 쓴다(코트 한 벌이 티셔츠 평균을 끌어올림).
    가격이 없는 구간은 0이 아니라 **null**("수집 안 됨"과 "공짜"는 다르다).
  - **상품명에서 나오는 키워드는 세 갈래로 나눈다**(v1.76, 사용자 요청): 쇼핑몰은
    제목에 성격이 다른 세 가지를 쓰고, 디자이너는 그걸 따로 추적한다.
    **원단**(Material in name — linen·denim·leather·cashmere) ·
    **조직**(Weave/structure — poplin·rib·jacquard·ponte·seersucker·boucle) ·
    **디테일**(Design detail — ruched·corset·peplum·smocked). 한 랭킹에 섞으면
    셋이 서로 경쟁해서 하나를 읽는 동안 나머지 둘을 지나친다. MEASURE 셀렉터에
    각각 들어 있고 PULSE·차트·랭킹 전부 같은 축으로 본다. 한 단어는 **한 갈래에만**
    들어가므로 이중 계산이 없다.
    - 원단·조직은 **화이트리스트**다 — 실루엣 어휘와 달리 섬유명·조직명은 100년째
      닫힌 집합이라 나열하는 게 제한이 아니라 정확성이다.
    - 디테일은 **여전히 스톱리스트** — 스톱워드·원단·조직을 뺀 나머지가 디테일이라
      처음 보는 실루엣도 그대로 올라온다.
    - 조성 열(`fabric`)은 별개의 더 강한 측정으로 남는다. 이건 **상품명**을 읽는데,
      조직과 디테일은 몰이 이름에만 쓰기 때문이다. `namekw-test` 43건.
  - **디자인 키워드는 카테고리 명사·사이즈·색상·마케팅 문구를 제외**한다.
    Dresses를 스캔하면 "dress"가 100%가 되어 트렌드처럼 보이지만 동어반복이고,
    Walmart는 제목에 "Sizes XS-XXXL"을 넣어 사이즈가 1위로 올라왔다. 색상은
    별도 축이 있어 이중 계산이 된다. 남는 것이 실루엣·디테일 어휘
    (asymmetric, corset, balloon, smocked, peplum…) = 디자이너가 추적하는 것.
    화이트리스트가 아니라 **스톱리스트**라 처음 보는 실루엣도 그대로 올라온다.
  계산은 `report/trend.js`(순수 함수, LLM·외부 API 없음), 렌더는 `lab.js`.
  - **주간 스냅샷**(IndexedDB `snapshots`, 키 `2026-W32`): 상품은 재스캔·정리로
    변하는 살아있는 데이터라, 그 주의 집계를 따로 얼려 둔다. 주당 수 KB(연 수백 KB).
    상품이 남아 있는 주는 항상 상품에서 다시 계산하고, 상품이 사라진 주만
    스냅샷이 대신한다. **재계산이 기록을 줄이지 못한다**(더 작은 값으로 덮어쓰기 금지).
  - 빈 구간은 0%가 아니라 **선을 끊는다** — "안 봤다"를 "사라졌다"로 그리지 않는다.
    상승/하락도 달력 창이 아니라 **데이터가 있는 구간**을 반으로 갈라 비교한다.
  - **Tier 분류**: 팀 브랜드 시트(xlsx)를 Import하면 `Brand Level` 열에서 Tier를 읽어
    `lists.tierMap`(브랜드→Tier)을 만들고, **표시 시점에 브랜드명으로 붙인다**.
    스캔 때 찍지 않으므로 **이전에 수집한 상품에도 소급 적용**된다(재스캔 불필요).
    LAB·신상 피드·브랜드 탭의 Tier 칩, 브랜드 레일의 Tier 그룹, LAB 기준(dim)의 Tier,
    리포트의 Tier 칩·그룹·배지가 모두 이 맵 하나를 쓴다.
  - **빈도 랭킹**(`ranked`): 선택한 기준을 창 전체에서 많이 보인 순으로 나열.
    점유율 차트가 "오르는가"를 답한다면 이건 "무엇이 많이 보이는가"를 답한다.
    카운트는 **상품 수**다 — 한 상품명이 같은 단어를 반복해도 한 표.
  - **신상 피드 / 브랜드 탭**: 같은 카탈로그 데이터를 브라우징용으로 프레이밍
    (Market Pulse의 감도만 차용, 복제 아님). 신상 피드 = 주차 칩으로 고른 주 안을
    **일자별(최신 날 먼저) → 브랜드별** 섹션으로, 주 안에서 **브랜드 칩 필터**(개수 표시,
    주를 바꾸면 리셋, 표시만 좁힐 뿐 수집엔 무관); 브랜드 = 상품수 순 브랜드 레일 +
    카테고리 칩 + "이번 주 신규 N". "신상"의 정의는 addedAt(그 주에 처음 관측) —
    모든 몰에 공평한 유일한 기준. clip은 제외(손으로 담은 건 신상이 아니다).
- **컬렉션 신원(`collectionSig`)은 쿼리·프래그먼트를 전부 본다.** 의미 있는 파라미터를
  사이트마다 이름으로 나열하는 방식은 확장되지 않고 조용히 틀린다 — Abercrombie는
  한 경로(`/shop/wd/womens`)에서 `categoryId`+`facet`으로 카테고리를 가르고, Gap은
  해시(`#style=…&neckline=…`)로 가른다. 그래서 **페이지 커서·트래킹 키만 빼고
  나머지는 모두 신원**이다(빼는 것: page·pageId·start·offset·Nao·mlink·utm_*·
  gclid 등). 커서를 빼야 페이지를 넘겨도 같은 컬렉션으로 인식된다.
  해시만 다른 URL은 `location.href` 대입으로 리로드가 나지 않으므로 **강제 리로드**한다.
- **하루에 여러 번 스캔해도 중복이 반영되지 않는다**(v1.73, 사용자 요청 — `rescan-e2e` 11건).
  세 층 모두에서 보장한다: ① **카탈로그** — 상품 키가 URL이라 재스캔은 기존 행을
  갱신하고 `addedAt`(첫 관측)은 그대로다 → LAB의 "신상"이 부풀지 않는다(`seenCount`만 오름)
  ② **엑셀** — `rowKey`가 카탈로그와 같은 정규화(쿼리 제거, 단 `pid`·`pelement`는 유지)를
  써서 한 상품의 주소 변형이 한 행으로 합쳐진다(옛 코드는 raw id 비교라 변형이 새어 들어왔다)
  ③ **실행 큐** — 같은 컬렉션(`collectionSig`)을 이미 훑었으면 그 URL은 **건너뛴다**
  (`q.doneSigs`/`q.skipped`). 리스트가 시간이 지나며 같은 페이지로 가는 항목을 갖게 되면
  스캔 시간이 두 배가 되던 것을 막는다. 단 `?v1=`처럼 **의미 있는 파라미터는 다른
  컬렉션**이므로 건너뛰지 않는다(Abercrombie facet 규칙과 동일).
- 중복: 상품 키는 URL이라 **재스캔은 중복을 만들지 않는다**(기존 행을 갱신, addedAt 유지).
  상품 URL의 쿼리는 버리지만 **상품 id 역할을 하는 키는 남긴다**(Inditex `pelement`,
  Gap 계열 `pid`) — 안 그러면 `product.do?pid=N` 전 상품이 한 행으로 합쳐진다.
  한 상품이 서로 다른 URL로 잡히는 경우만 `store.dedupe`가 **브랜드+상품명 완전일치**로
  합친다. 브랜드나 이름이 비면 합치지 않고, 살아남은 행이 **가장 이른 addedAt**을 갖는다
  (첫 관측이 트렌드 기준). 합친 수는 헤더에 "중복 N개 합침"으로 항상 표시 — 조용히
  줄어드는 숫자가 신뢰를 깬다.
- **실행은 조용히 멈출 수 없다**(v1.93.0, 사용자 보고 "스캔하다 중간에 adidas에서
  멈췄다"). 큐는 탭을 URL마다 이동시키고 **그 페이지의 엔진이 job을 이어받는** 구조인데,
  이어받는 자리에 한 줄짜리 침묵의 출구가 있었다:
  `if (!cur || !sameCollection(cur.url, location.href)) return;`
  몰이 **요청한 주소와 다른 주소로 답하면**(지역/로케일 리디렉션 · 동의 인터스티셜 ·
  canonical 재작성) 그 `return`이 실행되고 **그걸로 끝**이었다 — 큐는 active인 채,
  idx는 그대로, 실패 기록도 없고, 패널은 계속 "스캔 중"을 보여준다. **엑셀도,
  사이트 리포트도, 설명도 없다.**
  - **같은 몰 안의 리디렉션은 몰이 그 주소에 대해 내놓은 답**이다. 그래서 도착한
    페이지를 스캔하고 리디렉션을 `q.redirects`에 기록한다(리스트의 주소가 낡았을
    수 있다는 신호). 다른 호스트면 해석하지 않고 워치독에 넘긴다.
  - **페이지가 아예 안 열리면 페이지 안의 코드로는 알 수 없다** — 그래서 워커가
    밖에서 본다. `s()`와 `setQueue()`가 **모든 쓰기에 `at`을 찍고**(진행은 전부
    이 두 곳을 지난다), 알람이 1분마다 심장박동을 본다. **4분** 넘게 조용하면
    바쁜 게 아니라 막힌 것이다(큰 그리드 스크롤 + 상품 60개 순차 fetch도 매 단계
    쓰기가 있으므로 정상 실행은 절대 조용하지 않다).
  - 막힌 것은 **실패한 URL이지 실패한 실행이 아니다**: `wpb_sitehealth`에 ❌와
    사람 말로 된 사유를 남기고, idx를 넘기고, 탭을 다음 주소로 보낸다 — 거기서
    엔진이 평소대로 이어받는다. 마지막 URL이었으면 이미 끝낸 주소로 돌려보내
    **평소의 종료 경로가 엑셀을 쓴다**(ExcelJS는 페이지에만 있다).
  - 홀드 중인 실행은 막힌 실행이 아니다(`job.paused` 확인). `stall-e2e` 15건 —
    리디렉션·무응답 주소·"한 몰이 망가져도 나머지는 수집됨"까지 고정.
- **SCAN과 SCAN?의 차이**(v1.93.0, 사용자 질문). **둘 다 실행에 포함된다** —
  `Run all`은 `scannable !== false`를 돈다. 차이는 확신이지 실행 여부가 아니다:
  **Scan**=오리진 권한을 이미 갖고 있거나 전용 어댑터를 아는 몰(기본값이라 **칩을
  그리지 않는다**) · **Scan?**=아직 접근 권한이 없는 몰(실행 시작 시 크롬이 한 번에
  물어본다) · **Ref**=`file://`·`chrome://` 같은 **비웹 주소뿐**.
  - **판정이 두 벌로 갈라져 있던 것이 버그였다**: 패널이 열릴 때 도는 복구는
    "어댑터를 알거나 **오리진을 갖고 있으면**" true인데, **담는 쪽(Import·Paste·
    스타터 리스트)은 앞의 절반만** 물었다. 앞의 절반에 답하는 몰은 거의 없다
    (Shopify는 페이지 안 마커로 감지되고 팀 리스트의 절반이 generic) → **매니페스트가
    완전히 커버하는 몰 8개를 붙여넣어도 전부 "unknown"으로 저장**되고, 리스트가
    **모든 줄에 SCAN?**을 달고 돌아왔다. 스캔은 정상이었지만 화면은 안 될 것처럼
    말했고, 패널을 닫았다 열어야 복구가 따라잡았다.
    → 이제 `judgeScannable()` **하나**를 모든 경로가 쓴다. 담는 즉시 옳고,
    다시 열어도 달라지지 않는다. `scanq-e2e` 11건.
  - **모든 줄에 붙는 태그는 정보가 아니다**. Scan은 기본값이라 숨고 예외만 보인다.
- **웹 주소에 Ref는 없다**(v1.57). 사용자가 담는 이유는 스캔하려는 것이므로
  http(s) URL에는 `scannable:false`를 절대 쓰지 않는다. 근거는 라벨이 아니라 능력이다 —
  서비스워커가 **권한을 가진 페이지에 엔진을 주입**(`ensureEngine`: ping →
  `permissions.contains` → `scripting.executeScript` 5개 파일, 이미 있으면 재주입 금지)
  하므로 매니페스트에 없는 몰도 스캔된다. 실행 중 큐의 탭은 `tabs.onUpdated`로
  자동 재주입 — 정적 스크립트가 없는 사이트에서도 리스트가 끊기지 않는다.
  권한은 **클릭 안에서** 요청한다(Chrome 제스처 요건): Add this page는 그 오리진,
  Scan all은 리스트 전체를 한 프롬프트로. 거절해도 Scan?(실행 포함)이지 Ref가 아니다.
  `scannable`은 여전히 3상태 — true(어댑터를 알거나 오리진 보유) / undefined(모름,
  Run all은 `!== false`라 실행함) / false는 **file://·chrome:// 같은 비웹 주소 전용**.
  복구는 false뿐 아니라 **undefined도 재판정**한다 — 오리진을 보유하면 답이 정해진
  것이라 "Scan?"으로 남길 이유가 없다(Abercrombie가 지원 사이트인데 Scan?이던 원인).
  목록 태그 Scan / Scan? / Ref도 같은 의미다. (`scannable:false`를 웹 URL에 쓰면
  Run all이 그 몰을 통째로 건너뛴다 — Edikted 원인 ②, "10개인데 SCAN ALL (7)" 원인.)
- 목록 내보내기/가져오기: 리스트를 **.txt / .xlsx**로 빼서 편집하고 다시 Import하면
  갱신된다. 형식은 `lists.js`의 `toText`/`toGrid`가 만들고 `parseList`/`parseGrid`가
  그대로 읽는다(round-trip 테스트로 고정). URL이 신원이라 같은 URL이면 추가가 아니라
  **브랜드·카테고리를 갱신**하고, 파일에 없는 항목은 건드리지 않는다(한 브랜드 시트만
  가져와도 나머지가 지워지지 않음).
- **타일 텍스트는 상품명이 아닐 수 있다**(v1.59, 실제 엑셀 2건에서 발견).
  Abercrombie는 타일마다 스크린리더 안내문("Activating this element will cause
  content on the page to be updated.")을 넣어 그게 전 행의 상품명이 됐고, Edikted는
  타일 안 사이즈 선택 컨트롤 때문에 전 행이 "Select Size"가 됐다. `bestName`은
  ① 숨겨진 노드(aria-hidden·sr-only 클래스·1px 클립·display:none) ② 컨트롤 내부
  (button·select·option·label·role=button) ③ **UI 문구 스톱리스트**를 배제한다.
  스톱리스트는 단어가 아니라 **문구 전체 일치**라 "New Season Slip Dress"·
  "Plus Size Corset Top"은 그대로 통과한다(화이트리스트가 아니므로 처음 보는
  상품명도 통과). 타일에서 쓸 텍스트가 없으면 **URL 슬러그**를 쓴다 — 쇼핑몰이
  직접 쓴 이름이고 추측이 아니다. JSON-LD/상품 JSON의 이름은 슬러그를 덮어쓴다.
- **브랜드 열은 읽을 때 수리된다**(`store.cleanBrand`, v1.71). 브랜드는 엑셀·LAB·
  리포트의 묶는 키라 틀리면 한 몰이 상품 수만큼 쪼개지고 그 아래 숫자가 전부 무의미해진다.
  브랜드가 아닌 것 세 가지를 **모양으로** 잡아 도메인 브랜드로 되돌린다(알려진 목록이
  아니므로 처음 보는 몰도 적용): ① 스타일 코드(공백 없음+숫자+구분자, `S23548_NAVY`)
  ② 호스트명(`shop.lululemon.com`·`gap.com`) ③ 플랫폼 라벨. 진짜 브랜드는 그대로 둔다
  (숫자·기호가 있어도 공백이 있으면 통과 — Rag & Bone·3.1 Phillip Lim·Ninety Percent).
  **placeholder 이미지와 같은 방식으로 저장 시·읽을 때 모두** 적용해서, 옛 스캔이 남긴
  행이 LAB을 열기만 해도 고쳐진다(마이그레이션·재스캔 없음). `brandrepair-e2e` 13건.
- **LAB은 리스트별로 본다**(v1.71, 사용자 요청). 리스트 하나가 리서치 질문 하나다 —
  Young Women's와 Kids를 평균 낸 추이는 둘 다 설명하지 못한다. 그래서 **탭 위에
  리스트 스코프 칩**(All lists + 리스트별, 개수 표시)이 있고, 차트·신상 피드·브랜드
  레일·상품 그리드가 **한 번에** 좁혀진다(탭마다 다시 거는 필터가 아니다).
  근거는 상품의 `listIds`(store.merge가 합집합으로 유지)라 재스캔이 필요 없다.
  선택은 `wpb_labscope`에 저장되어 다시 열어도 그 리스트로 돌아온다.
  **주간 스냅샷도 리스트별로 따로 쌓인다**(v1.72): id가 `<listId>|2026-W32`,
  카탈로그 전체 기록은 종전대로 `2026-W32`. 리스트 기록이 전체 기록을 덮어쓰는 일이
  구조적으로 불가능하다. `labscope-e2e` 17건.
- **주차 비교는 "같은 몰"끼리만 한다**(`weekCompare`, v1.72, 사용자 질문에서 출발).
  LAB의 모든 share는 "그 주 신상 중 %"인데, 두 주가 **같은 몰을 봤을 때만** 비교가
  성립한다. 실제로는 리스트가 자란다 — 브랜드를 새로 담고, 카테고리를 추가하고,
  한 몰의 스캔이 실패한다. 그러면 코튼 많은 브랜드 하나가 들어온 것만으로
  "Cotton +30%p"가 찍히고, 한 몰을 못 읽은 것이 "그 색 붕괴"로 보인다.
  - 그래서 각 주가 **로스터**(그 주에 실제로 상품을 낸 `브랜드 · 카테고리` 집합)를
    기록하고, 두 주는 **양쪽에 다 있는 컬렉션**으로만 비교한다(동일점포매출 원칙).
    브랜드가 아니라 **브랜드+카테고리**가 단위다 — Zara에 Knitwear를 추가한 것도
    커버리지 변화인데 브랜드 단위로 보면 놓친다.
  - 화면에는 **like-for-like가 헤드라인**, **커버리지 변화가 그 옆에 항상 노출**
    (+ 새로 들어온 컬렉션 이름 · − 이번 주 못 읽은 컬렉션 이름), 원본(전체) 수치는
    접힌 채로 "물량용이지 트렌드용이 아니다"라고 명시. 겹치는 컬렉션이 0이면
    숫자를 내지 않고 그 사실을 말한다.
  - 로스터는 스냅샷에도 저장(`collections`, 400개 상한)되어 상품을 정리한 뒤에도
    그 주가 어떤 기반이었는지 확인할 수 있다. `compare-test` 26건 ·
    `weekcompare-e2e` 15건.
- **한 몰은 한 브랜드다 — 멀티브랜드 리테일러만 예외**(v1.94.0, 사용자 보고
  "gone banana가 아니라 setactive가 브랜드 이름이야"). Set Active는 자기 라벨을
  파는데 상품마다 **드롭 이름**을 Shopify vendor에 넣는다("JUL 2026 - GONE BANANAS",
  "AUG 2026 - CORE"). 그걸 브랜드로 읽으면 **한 몰이 시즌 달력으로 쪼개지고**, 그
  아래 모든 수치가 무의미해진다. 기존 가드는 스타일 코드 모양만 걸렀는데 드롭 이름은
  평범한 이름처럼 생겨서 **모양으로는 판정할 수 없다.**
  → 판정 기준은 **몰의 성격**이다: `a.multiBrand`(Walmart·Target)면 행이 이기고,
  아니면 **리스트 항목의 브랜드가 이긴다**(카테고리 라벨과 같은 규칙, v1.84.0).
  Shopify·generic 몰 전부가 여기 해당한다. `brandimg-e2e` 15건.
- **사진이 없다는 것도 벌크 JSON의 종점이 아니다**(v1.94.0, 사용자 보고 "vuori 썸네일이
  안 보인다"). v1.68이 **조성**에 대해 정한 규칙("벌크 hit은 verdict가 아니다")이
  **사진에도 그대로 적용**되어야 했는데 조성만 PDP로 갔다. VUORI는 벌크 JSON이 조성을
  답해서 체인이 거기서 멈췄고, 레이지 그리드는 타일 사진을 안 줬고, 결과는
  **원단 열은 가득한데 카드 60장이 전부 NO IMAGE**.
  - **사진이 상품이 아니라 variant에 달린 몰이 있다**(`variants[].featured_image`).
    `shopifyImage`가 `images[0]`·`featured_image`만 봐서 VUORI에서 빈손이었다.
  - 조성 **또는 사진**이 비면 PDP를 읽는다. 이미 파싱한 문서라 og:image·JSON-LD image를
    같이 꺼내는 비용은 0이고, **부족한 행만** PDP를 부른다.
- **사진이 안 나온 진짜 원인은 우리가 주소를 부순 것이었다**(v2.0.0, 사용자가
  lululemon 주소를 직접 열어 보내 준 응답 `{"message":"Bad Request.","errorCode":"9E401001"}`).
  **그 한 줄이 세 번의 추측을 끝냈다** — 거부(403·CORS·권한 없음)는 "주소는 맞는데
  못 준다"이고, Bad Request는 **"그 주소를 못 알아먹겠다"**다. 주소가 틀린 것이다.
  - 원인: `widestFromSrcset`이 srcset을 **쉼표로 split**했다. 그런데 **이미지 주소
    안에 쉼표가 있다** — Adobe Scene7(lululemon·Aritzia) `?wid=800&op_usm=0.5,2,10,0`,
    Cloudinary는 경로에 `c_fill,w_600,h_800`. 후보 하나가 다섯 조각으로 부서지고
    **너비 서술자가 붙은 조각이 이겨서** `0&fmt=webp`가 상품 사진으로 저장됐다.
  - **검사가 이걸 못 본 이유**: 그 쓰레기도 몰 주소로 resolve하면 그럴듯한 https가
    되어 **행은 "사진 있음"으로 세어졌다.** 그래서 스캔은 ✅였고 세 번의 수정이
    전부 "주소는 맞는데 거부당한다"를 전제로 엉뚱한 데를 고쳤다.
  - 고친 것 둘: ① **srcset은 쉼표로 자르지 않는다** — URL에는 공백이 없으므로
    "주소 → 선택적 서술자 → 쉼표"로 토큰이 명확하다(`parseSrcset`).
    ② **`store.cleanImage`가 경로 없는 주소를 버린다**(저장 시·읽을 때 모두) —
    이미 오염된 행은 LAB을 여는 것만으로 빈 값이 되고, 그러면 **사진 커버리지
    검사가 비로소 잡는다**. `srcset-test` 16건(수정 전 코드에서 6건 실패 확인).
- **세 번 고쳤는데 같은 보고가 왔다 — 실패가 이유를 안 말했기 때문이다**(v1.99.0,
  사용자 3회 보고 "vuori·nike·lululemon·adidas 여전히 image blocked"). v1.94는
  워커 재요청, v1.96은 권한 구분을 넣었고 **테스트는 통과했다.** 그런데 실제로는
  여전히 안 나오고, **화면에 있는 것은 "IMAGE BLOCKED" 다섯 글자뿐**이라 매번
  원인을 추측해야 했다(CDN 호스트 · CSP · referrer 세 번 다 빗나감).
  **그게 진짜 결함이다** — v1.95의 규칙("도구가 스스로 말했겠는가")을 사진 실패에는
  적용하지 않았다.
  → 이제 실패가 **호스트와 사유를 카드에 직접 적는다**(`images.lululemon.com` /
  `HTTP 403`). 워커는 `{ok:false, error, need}`로 답하고, LAB이 그걸 모아
  검사 밴드에 넣고 **Copy this line** 버튼을 단다 — 콘솔을 열지 않고 사유를
  그대로 보낼 수 있다. 사유가 없으면 다음 라운드도 추측이 된다.
- **"IMAGE BLOCKED"는 주소가 죽은 게 아니라 묻는 사람이 문제다**(v1.94.0, 사용자 보고
  nike·lululemon·adidas). LAB은 `chrome-extension://`이고 이 몰들은 사진을 **자기
  사이트에만** 내준다(CORS 헤더 없음·Referer 검사). 주소도 수집도 정상인데 카드만 회색.
  → **서비스워커는 그 제약을 안 받는다**(호스트 권한 보유 — 엑셀 썸네일 임베드에 이미
  쓰던 경로). 실패한 이미지만 워커로 다시 받아 bytes로 꽂는다. 주소 기준 공유 캐시
  (한 그리드가 같은 CDN을 수백 번, 컬러웨이가 같은 사진을 반복). **페이지가 스스로
  불러온 사진은 메시지 비용이 0**이고, 그래도 실패하면 그때 IMAGE BLOCKED.
  이미지 CDN은 브랜드 도메인의 서브도메인이라(`static.nike.com`·`images.lululemon.com`·
  `assets.adidas.com`) **새 권한이 필요 없다**.
- **리스트 칩은 상품 수가 아니라 브랜드 수를 센다**(v1.94.0, 사용자 요청). 리스트는
  리서치 질문 하나이고, 질문의 크기는 **몇 개 몰을 보느냐**다 — 사용자가 고른 숫자.
  상품 수는 그 주에 몰들이 뭘 올렸느냐의 결과라 "387 vs 431"로는 어느 리스트가 더 큰
  작업인지 알 수 없다. 정확한 상품 수는 칩 옆 총계 줄에 그대로 있다.
- **용량 칩은 바이트가 아니라 문(門)의 이름을 단다**(v1.94.0, 사용자 질문 "용량은 왜
  필요한지"). 정당한 질문이었다 — 실측으로 **상품당 283바이트 / 할당량 150GB**라
  숫자 자체는 영영 의미가 없다. 하지만 그 뒤에 있는 것은 의미가 있다: **Back up ·
  Merge in a backup · Free up space**, 즉 **한 브라우저의 카탈로그가 다른 브라우저에
  닿는 유일한 길**이다(카탈로그는 브라우저마다 따로 산다, v1.83.0). 그래서 칩은
  **`Data`**로 이름을 말하고, 용량은 **정말 저장소 얘기인 줄 안쪽**에 남는다.
- **Shopify `vendor`는 브랜드가 아닐 수 있다.** Edikted는 vendor에 스타일 코드를
  넣어 브랜드 열이 S24503_AQUA…로 채워졌고, 엑셀이 브랜드로 묶으니 **단일 브랜드가
  상품 수만큼 쪼개졌다**. vendor가 그 상품의 handle과 같거나 공백 없는 코드
  (영문+숫자+구분자)면 브랜드가 아니라고 보고 비운다 → 리스트 항목의 브랜드가 채운다.
  상품명도 마찬가지로 **Shopify `product.title`이 정본**이라 타일 추정을 덮어쓴다.
- **"사진 없음"에는 원인이 둘이고 카드가 구분해서 말한다**(v1.79.2). 값이 아예
  없는 것(스캔이 못 가져옴)과 주소는 있는데 몰이 거부하는 것(핫링크 차단)은
  고치는 곳이 다르다. 이제 빈 값은 `NO IMAGE`, 로드 실패는 **`IMAGE BLOCKED`**
  (툴팁에 그 주소)이고, 썸네일에 `referrerpolicy="no-referrer"`를 달아 핫링크
  차단을 먼저 피한다. 헤더 줄에 **`N with a photo`**가 상시 표시되어 재스캔 없이
  둘 중 어느 쪽인지 즉시 보인다.
- **사진 주소를 만드는 곳은 이제 한 곳이다**(`absImage`, v1.81.2). Edikted를 고친
  자리는 **목록 타일**뿐이었는데, 레이지 그리드가 사진을 끝내 안 내주면 사진은
  **상품 페이지**(JSON-LD `image` · `og:image`)에서 온다 — 그 리더들이 똑같이
  `/^https?:/`를 요구해 몰이 스킴 없이/루트 상대로 적은 주소를 버리고 있었다.
  Aritzia(하우스 브랜드)와 Victoria's Secret(어댑터 없음=generic)이 정확히 이
  경로에 의존한다. 이제 **타일 · JSON-LD · og:image · 플랫폼 API 전부**가
  `absImage(u, base)` 하나를 거친다(스킴 없으면 https:, 상대경로면 그 페이지
  기준으로 해석, 그래도 http(s)가 아니면 버림). 리더 4벌에 흩어져 있던 같은
  규칙을 한 줄로 합쳤고, PDP 리더에는 **페이지 URL을 넘긴다**(없으면 상대경로를
  풀 기준이 없다). `pdpimage-e2e` 12건 — 수정 전에는 5건 실패.
- **사진 주소는 저장 전에 절대주소가 된다**(v1.79.1). Edikted 64행이 이름·가격·
  게시일까지 다 있는데 카드가 전부 NO IMAGE였던 원인: Shopify 테마는 사진을
  **스킴 없이**(`//edikted.co.uk/cdn/shop/files/x.jpg`) 쓰는데 `cleanImage`가
  `http(s)`를 요구해 **멀쩡한 주소를 버렸다**. 루트 상대(`/cdn/shop/…`)도 마찬가지고,
  그대로 저장해도 LAB(`chrome-extension://`)에서는 확장 자신을 기준으로 풀려 깨진다.
  그물 셋: ① 타일을 읽는 자리에서 `abs()` ② `cleanImage`가 `//`를 `https:`로 **수리**
  (버리지 않음) ③ Shopify **자기 JSON의 사진**(`images[0].src`·`featured_image`)이
  그리드가 렌더 안 된 타일을 채움 — 있는 사진을 덮지는 않는다. `shopimage-e2e` 11건 ·
  `image-test` 30건.
- 썸네일: 무한 스크롤 그리드는 화면 밖 타일의 `src`가 1x1 placeholder라 그대로 읽으면
  Zara 결과에 이미지가 통째로 비었다. `bestImage`는 srcset(가장 큰 후보) ·
  `<picture><source>` · data-src류 · 배경이미지까지 훑고 placeholder를 버린다.
  그래도 비면 상세 단계에서 PDP의 JSON-LD `image` → **og:image**로 채운다(있는 값만).
  **placeholder는 어디서나 빈 값 취급**(`store.cleanImage`): 저장 시 걸러지고 읽을 때도
  정리된다 — 수정 전 스캔이 data: URI를 값으로 저장해 두면 "이미 채워짐"으로 보여
  재스캔·PDP 폴백이 전부 막혔던 것의 근본 수정. 검은 카드 대신 NO IMAGE로 보인다.
- **업로드일은 수집만 하고 화면에 쓰지 않는다**(v1.81.1, 사용자 요청). Shopify만
  제공해서 대부분의 브랜드가 빈칸인데, **일부만 답하는 열은 성립하지 않는 비교를
  부른다**. 뜻도 디자이너가 읽는 것과 다르다 — "몰이 스토어에 노출시킨 시각"이라
  재공개하면 갱신되고 오래 걸려 있던 상품은 작년 날짜를 달고 있다(신상 그리드에
  2025가 뜬 이유). 값은 행에 그대로 남기고(몰이 말한 사실이므로 지우지 않는다),
  **화면의 모든 기간은 수집일 하나로** 센다 — 모든 몰에 같은 잣대. LAB 카드의
  "Published" 줄 · 상단 카운트 · 기간 기준 셀렉터 · "Newest published" 정렬 제거.
  (아래는 제거 전 배경 기록)
- 업로드일(`launched_at`)은 **몰이 자기 상품을 온라인 스토어에 공개한 시각**이다
  (Shopify `published_at`). 디자인 발표일도, 그 카테고리에 들어온 날도 아니고,
  내렸다 다시 올리면 **갱신된다**. 우리가 만든 값이 아니라 몰이 적은 값이라 카드
  툴팁에 원본 타임스탬프를 그대로 보여준다. 표시는 **보는 사람의 로컬 날짜**다
  (v1.79.2까지는 ISO를 잘라 UTC 날짜를 보여줘, 한국 아침에 게시된 상품이 전날로
  보일 수 있었다).
- 업로드일(`launched_at`): 쇼핑몰이 스스로 밝힌 게시일. **Shopify(`published_at`)만 제공**하고
  나머지는 값이 없다 — 추정하지 않는다. 다른 몰에 값이 있는지는 사용자가
  `diagnose-dates.js`를 PDP 콘솔에 붙여넣은 출력으로 확인한다. 카탈로그에서 기간 기준을 수집일/업로드일로
  전환할 수 있고, 업로드일 기준일 때 값이 없는 행은 제외된다(다른 값으로 대체 금지).
  LAB 추이는 여전히 addedAt만 쓴다 — 몰마다 기준이 다른 값을 섞으면 비교가 깨진다.
- 리포트(HTML, `report/reportgen.js`): 기본 템플릿은 **대시보드 레이아웃** — 연한 민트
  사이드레일 + 3개 섹션(01 New In: 일자별→브랜드별 / 02 By Brand: 브랜드별→카테고리별,
  앵커 칩 / 03 LAB: **파스텔 KPI 카드 4장(숫자 + 같은 지표의 미니 차트)** → 라운드 카드
  안의 차트들). New In 상단에 **브랜드 칩 필터**(개수 표시 · 비는 날짜 자동 숨김 ·
  날짜 카운트 재계산). 탭 전환은 10줄 인라인 스크립트뿐이고 스크립트가 없으면(인쇄 등) 전
  섹션이 쌓여 보인다 — 아카이브 성질(외부 참조 0, 이미지 data URI)은 그대로.
  lookbook/data 템플릿은 기존 문서형 유지, 세 템플릿의 수치는 동일해야 한다.
- post-scan 필터: 브랜드/주브랜드만/이름 포함·제외 (내보내기 시점 적용)
- **UI 문구는 영어**로 통일(2026-08, 사용자 요청). 파일명 정규식의 `가-힣`은
  리스트 이름이 한글일 수 있어 남긴 것 — 문구가 아니다. `compact-e2e`가 LAB 본문에
  한글이 남아 있으면 실패시킨다(v1.74에서 `emerging`의 근거 문구 2개가 새어 있었다).
- **화면은 일이 쓰고, 가구가 쓰지 않는다**(v1.74, 사용자 요청 "SIMPLE AND COMPACT").
  측정해서 지킨다(`compact-e2e` 15건 — 예산이지 스타일이 아니다):
  - 패널 400×800에서 **URL 시작 전 소모 376px → 247px**, 행 높이 39 → 28,
    보이는 행 **6/11 → 11/11**. 헤더가 렌즈+세그먼트 2줄에서 1줄로, LAB 블록이
    부제를 버리고 1줄로, 카드 헤더가 아이콘+제목에서 마이크로 라벨로.
  - LAB 1280×800에서 **첫 답 전 소모 383px → 233px**. 가로 밴드 3개(스코프·탭·툴바)가
    쌓여 있던 것을 **스코프는 제목 줄로 올리고**, 탭은 부제를 버려 1줄로, 툴바 라벨은
    위가 아니라 옆으로. 기능은 하나도 빼지 않았다 — 리스트 칩·탭 5개·툴바·트랜스포트 모두 그대로.
  - 규칙: **새 컨트롤은 자기 높이를 다른 것에서 가져와야 한다.** 예산 테스트가
    "전체의 1/3 미만"으로 고정하므로, 무언가 추가하면 이 테스트가 먼저 깨진다.
- 패널 배치는 위에서부터 **LAB 카드 → 리스트 카드(내보내기·Scan all·Pause/Reset·
  상한 → Add this page → 항목들) → Import**. 수집의 목적지(LAB)가 먼저, 큐레이션이
  그 다음, 담기는 리스트에 속한 행동이라 리스트 카드 안에 둔다.
- 아이콘은 **렌즈**(웜 액센트 그라디언트). 라이트/다크 툴바 양쪽에서 보여야 하므로
  근검정·근백색을 쓰지 않고, 16px에서 뭉개지지 않게 획 두께를 크기별로 다르게 렌더한다
  (`scratchpad/mkicon.js`가 하나의 SVG에서 4개 크기를 뽑는다).
- 테스트: `tests/`의 Node 스위트(저장소 안 — `tests/README.md`) + Playwright로 실제 Chrome에 확장을 로드해
  패널·카탈로그·스캔 흐름을 검증(xvfb 필요, headless는 확장 로드 불가)

로드맵과 미구현 항목은 `SPEC.md` 참조.

## 아키텍처 원칙 (ALWAYS / NEVER)

- **NEVER 사이트별 CSS 셀렉터 하드코딩.** Walmart·Target류는 클래스명이 해시라
  금방 깨진다. 추출은 항상 `구조화 데이터(embedded JSON / JSON-LD) → DOM 휴리스틱
  → config 힌트` 순서의 다단 폴백. DOM 셀렉터는 최후의 폴백으로만, 시맨틱한
  속성(`data-*`, aria, href 패턴) 위주로.
- **ALWAYS 사이트가 아니라 "플랫폼 타입"을 타겟한다.** 엔진 = Shopify / SFCC /
  커스텀 SPA. 새 사이트 지원 = 플랫폼 감지 → 기존 엔진 라우팅이 우선이고,
  전용 어댑터는 그게 안 될 때의 선택지다. (cottonon 어댑터는 SFCC 엔진으로
  일반화할 것 — SPEC Phase 1)
- **NEVER 트렌드 왜곡.** 스캔 전(pre-scan)엔 범위 필터만(카테고리·깊이·정렬).
  가격·색·소재·브랜드 같은 속성 필터는 스캔 후(post-scan) 내보내기/분석 단계에서만.
  분포를 보존해야 트렌드가 보인다. (현재 팝업 필터는 내보내기 시점 적용 = 준수)
- **ALWAYS 권한 최소.** `permissions`/`host_permissions` 추가는 스토어 심사를
  느리게 하므로, 추가 시 반드시 사유와 심사 영향을 커밋 메시지에 명시.
  미사용 권한은 발견 즉시 제거. (현재 `permissions: ["storage","downloads","sidePanel","scripting","activeTab"]` — downloads는 일부 리테일러(Target)가 앵커
  다운로드를 막아 SW 경유 저장에 필요. 호스트 접근은 기본 0: 우클릭 담기는 activeTab,
  패널 버튼은 클릭 시점에 해당 오리진만 `optional_host_permissions`로 요청)
- **정량 레포트는 LLM 크레딧 없이 순수 계산으로.** 서술형 해석은 선택 레이어이며
  확장 안에 LLM을 넣지 않는다. xlsx를 Claude에 핸드오프하는 방식으로.
- **자동 감지는 "출발점"이지 "정답"이 아니다.** 카테고리 자동 감지 → 사용자가
  정리 → 프로필 저장(chrome.storage) → 재사용.
- **zero-hallucination 출력 규칙.** 셀 값은 사이트에 실제로 있는 텍스트만.
  추론 필요 → 빨간 "재확인 필요", 못 찾음 → 빨간 "정보 확인"(+원인).
  `excel.js`가 강제한다. 새 필드를 추가할 때도 이 규칙을 따른다.

## 작업 관행

- **반쯤 긁고 성공이라고 말하는 것이 가장 나쁘다**(v2.8.0, 생성 탐색 2라운드
  `harden-probe`). 라이브 몰 없이 두 가지를 실측했다 — **스크롤로 타일을 내주는 그리드**와
  **거부하는 상품 페이지**.
  - **48개짜리 그리드에서 16개만 걷고 ✅로 채점했다.** 엑셀이 온전해 보이므로 아무도
    모른다. 원인이 셋이었다: ① 스크롤이 **문서 맨 아래로 점프**해서, 그리드 밑에 긴
    푸터처럼 높은 것이 있으면 **트리거를 지나쳐 버린다**(그 뒤로 영영 안 걸린다)
    ② 한 번 지나치면 **다시 훑지 않는다** ③ 몰이 적어 둔 **"48 items"를 못 읽었다** —
    요소 텍스트가 붙어서 `New In48 items`가 되는데 패턴이 단어 경계를 요구했다
    (가격이 이름과 붙던 것과 **같은 종류의 실패**다: 답도 같다 — **그 말을 하는 가장
    작은 요소에서 읽는다**).
  - 고친 뒤: **한 화면씩 내려가며** 훑고(트리거가 뷰포트를 통과한다), 몰이 "더 있다"고
    말하는 동안 **맨 위로 돌아가 다시 훑는다**(최대 4회 · 60개 상한과 라운드 예산이
    끝을 보장). 결과 **16 → 48**. 이미 다 그려진 그리드는 자라지 않으므로 **비용 0**
    (3개 몰 실측 16.1초, 변화 없음).
  - 그래도 부족하면 **화면이 숫자로 말한다**: "이 페이지는 48개라고 적어 놨는데 24개가
    들어왔다 — 그리드가 나머지를 안 내줬다"(⚠️). 조용한 ✅는 이제 불가능하다.
  - **429는 기다리면 답이 달라지는 유일한 거부**다. 평범한 몰은 상품 1개당 PDP 1회라
    60번을 연속으로 두드리는데, 중간부터 throttle이 걸리면 **원단 열이 그때부터 빈다.**
    이제 **한 번만 참을성 있게 재시도**하고(1.5초), 그래도 거부하면 밀어붙이지 않는다.
    그리고 문장이 **상태 코드를 말한다**: "no fabric on any of the 12 — the shop's
    product pages answered 429 (too busy), so scan it again later." 원단이 빈 이유가
    몰의 혼잡인지·거부인지·원래 안 적혀 있는지에 따라 **고칠 사람이 다르다.**
  - `harden-e2e` 8건(수정 전 코드에서 4건 실패 확인).
- **보고를 기다리지 않는다 — 모양을 생성해서 결함을 찾는다**(v2.7.0, 사용자 요청
  "목표를 달성할 때까지 알아서 개발하는 방법"). 이 프로젝트의 결함은 **전부** 디자이너가
  화면에서 발견해 보고하는 경로로 왔다. 그런데 쇼핑몰은 브랜드가 아니라 **독립적인
  선택들의 조합**이다 — 사진을 그리는 방식 × 돈을 쓰는 순서 × 이름의 위치 × 타일 중첩 ×
  상품 주소 모양 × 타일 안 잡동사니 × 페이지 문맥. 선택이 독립적이므로 **결함도
  조합에서 난다**: 색상 칩 버그와 유로 버그는 브랜드를 테스트해서는 절대 못 찾고
  **조합을 테스트해야** 나온다.
  - `matrix-probe.js`가 그 조합을 **생성**한다: 각 축의 모든 값을 기준선에 대해 한 번씩,
    그다음 결정적(무작위 아님) 샘플로 전체 조합 — 지금 **134개**. 실제 크롬에 확장을
    올리고 리더를 그대로 돌린다. 라이브 몰이 필요 없다 — **리더를 깨뜨리는 것은
    마크업이고, 마크업은 생성할 수 있다.**
  - **비어 있지 않음은 정확함이 아니다**(v2.0.0·v2.5.0의 교훈). 셀 값은 페이지가 실제로
    말한 것과 대조한다: 상품 사진(칩·호버컷·저해상도 사본이 아님), 상품명(스크린리더
    문구가 아님), 몰이 쓴 그대로의 가격.
  - 첫 실행에서 **3건**을 찾았다(아무도 보고하지 않은 것들):
    ① **이름 끝의 숫자가 뒤 가격과 붙었다** — "Style Number 1" + "89,00 €" →
    **`189,00 €`**. 마크업은 두 노드 사이에 공백을 안 넣으므로 감싸는 요소의 텍스트가
    한 덩어리로 읽힌다. 이제 **가격을 품은 더 작은 요소가 있으면 그쪽을 쓴다**
    (501·Air Max 90·Tee 2.0처럼 숫자로 끝나는 이름은 흔하다).
    ② `/p/style-number-1/PROD1.html` → 상품명 **"Prod1"**.
    ③ `/product.do?pid=901` → 상품명 **"Product.Do"**. 주소에서 이름을 꺼낼 때
    **마지막 조각을 무조건** 쓰고 있었다. 이제 뒤에서부터 훑으며 **라우팅 단어·파일명·
    불투명 코드**를 건너뛰고, 사람이 읽으라고 쓴 조각(구분자가 있는 것)을 고른다.
    아무것도 없으면 **빈 칸**이다 — 지어낸 이름은 빈 칸보다 나쁘다(LAB이 단어로 센다).
  - **축을 늘리는 것이 곧 개발 계획이다.** 새 값을 하나 추가하면 그 값이 실패에 몇 번
    나타나는지 표로 나오고, 그게 다음에 고칠 것이다. `matrix-e2e.js`는 같은 생성기를
    계약으로 고정한다(HEAD에서 134개 중 **7개 실패** 확인).
  - **목표 문장 자체도 테스트다**: `goal-e2e.js`는 네 가지 몰 원형(Shopify 벌크 JSON ·
    JSON-LD 조성 · 유로 표기+배경 이미지+아코디언 · 그리드에 사진이 아예 없어 PDP가
    채워야 하는 몰)에 **실제 리스트 실행**을 걸고, 모든 행에 **브랜드·상품명·사진·조성**이
    있는지, 브랜드가 사용자가 지은 이름인지, 스캔의 자기 판정이 ✅인지, 그리고 **LAB
    한 화면에 다 보이는지**까지 검사한다. 27건.
- **결함 하나를 고치는 절차**(v1.99.0에서 합의, 사용자 요청 "앞으로도 동일한
  방법으로"). 이 프로젝트의 결함은 거의 전부 "디자이너가 화면에서 발견 → 보고"로
  왔고, 그걸 줄이는 것이 목적이다. 순서를 지킨다:
  1. **재현부터. 추측으로 코드를 고치지 않는다.** 페이지 구조든 속도든 UI든,
     먼저 계측하거나 픽스처로 재현한다(`fabprobe`·`speed-probe`·`newbrands-probe`).
     보고를 받은 즉시 원인을 "아마 이것"으로 정하고 손대면, 지금까지처럼 세 번
     연속 빗나간다.
  2. **옛 코드로 먼저 돌려 본다.** 새 테스트가 **수정 전 코드에서 실제로 실패**하는지
     확인한다. 실패하지 않으면 그 테스트는 아무것도 지키지 않는다.
     (`git stash`/`git checkout <sha> -- <file>` 후 실행, 반드시 되돌릴 것.)
  3. **고칠 것은 둘이다** — 그 결함과, **그것을 못 본 검사**(v1.95 규칙).
     "이걸 도구가 스스로 말했겠는가"를 먼저 묻는다.
  4. **도구가 사람이 읽는 말로, 사람이 보는 자리에서** 말하게 한다. 콘솔 명령
     (`devsitecheck()`)은 디자이너에게 존재하지 않는 것과 같다 — LAB 상단 검사
     밴드, 패널의 마지막 줄, 카드 위의 사유가 그 자리다. 실패는 **호스트·상태·
     사유**를 달고 있어야 다음 라운드가 추측이 되지 않는다.
  5. **회귀 스위트 + `encoding-test.js`**를 돌리고, 실패하면 그것이 옛 설계를
     검사하던 것인지(테스트를 새 계약으로 고친다) 진짜 회귀인지(코드를 고친다)를
     **구분해서** 처리한다. 죽은 필드를 보던 테스트는 빈 값에 통과하므로
     (`q.rows`가 v1.82.0 이후 그랬다) 아무것도 지키지 못한다 — 발견하면 고친다.
  6. **커밋·푸시하고 ZIP 링크와 버전을 함께 안내**한다. 커밋 메시지에는 증상이
     아니라 **원인**과 그 판단의 근거(측정값)를 적는다.
  7. **못 고친 것은 못 고쳤다고 말한다.** 이 환경은 라이브 쇼핑몰에 접근할 수
     없다 — 확인 못 한 것을 확인한 것처럼 쓰지 않고, 무엇이 있으면 확정되는지를
     한 줄로 적는다.
- **NEVER 유니코드 noncharacter(U+FFFF, U+FFFE, U+FDD0~FDEF)를 소스에 넣지 말 것.**
  "가장 뒤로 정렬" 같은 sentinel로 쓰기 쉬운데, 크롬이 "isn't UTF-8 encoded"로
  그 파일을 거부하고 **manifest 전체가 로드 실패**한다(확장이 아예 설치 안 됨).
  Node 테스트는 이걸 못 잡으므로 `tests/encoding-test.js`가 배포 목록의 파일 단위로 검사한다.
  정렬에서 빈 값을 뒤로 보내려면 비교 함수에서 명시적으로 분기할 것.
- 모든 변경은 Node 테스트(**`tests/`** — 저장소 안에 커밋된다, `tests/README.md`)로
  회귀 확인 후 커밋.
  라이브 사이트 접근이 차단된 환경이므로, 실제 페이지 구조는 사용자가 보내 주는
  실측 출력으로 확인한다 — 추측으로 셀렉터를 고치지 않는다.
- **리스트 실행은 URL마다 엑셀을 만들고 그걸 버리고 있었다**(v2.1.0, 사용자 질문
  "자동으로 building excel하느라 시간이 오래 걸리는 것인지"). 그렇다 — 그리고
  **대부분이 낭비였다.** build 단계가 URL마다 `buildWorkbook`을 호출해 **그 몰의
  썸네일을 전부 내려받아** 워크북에 넣는데, 리스트 실행이면 그 `bytes`를 쓰지 않고
  버린다(파일은 마지막에 `queueExport`가 하나 만든다). 14개 몰이면 **버려질 워크북
  14개 + 사진을 두 번 내려받기**다.
  - 그 코드가 실제로 쓰던 건 `kept`(내보내기 필터를 통과한 행)뿐이고, 그건
    `WPBExcel.filterKept`가 **ExcelJS도 네트워크도 없이** 준다.
  - 실측(3개 몰 × 60개, 이미지 80ms): **33.3초 → 16.1초**, 이미지 요청 **426 → 242**.
    14개 몰 기준 **약 2.6분 → 1.3분**.
  - **상품은 엑셀과 무관하게 몰마다 카탈로그에 들어간다**(`catalogSave`가 URL마다
    호출됨) — 엑셀을 안 뽑아도 PRODUCTS·LAB에는 이미 들어와 있다. 중간에 Stop해도
    그때까지 분은 남는 이유가 이것이다.
  - **재스캔은 옛 사진을 덮어쓴다**(`store.merge`): 새 값이 비어 있지 않으면
    이긴다. 다만 **재스캔 전까지는 옛 주소가 그대로 남으므로**, 주소가 깨져 있던
    몰(v2.0.0)은 한 번 다시 스캔해야 사진이 나온다.
- **실행 속도는 측정해서 줄인다**(v1.96.0, 사용자 요청 "14개 브랜드가 시간이 많이 든다").
  추측 대신 계측했다(`speed-probe.js` — 3개 몰 × 상품 60개, PDP 응답 250ms):
  **몰당 10.7초 중 상세 단계가 7.8초**였고 그 대부분이 *기다리는 시간*이었다.
  - **레인 4 → 8.** 상세는 순수 네트워크 대기라 탭이 놀고 있었다. 8은 브라우저가
    몰 페이지 하나에 여는 연결(호스트당 6 + 프리커넥트)보다 여전히 적다.
  - **URL 사이 대기 1500ms → 400ms.** 콜백이 나갈 시간만 있으면 되는데 URL마다
    1초를 버렸다 — 팀 리스트면 20초.
  - **스크롤 안정성 확인 700ms → 450ms.** 규칙은 그대로(개수·높이가 두 번 안정),
    보는 간격만 줄였다. 이미 다 그려진 그리드가 매번 2초를 증명에 쓰던 것이 절반이 된다.
  - **이미 있는 사진 때문에 PDP를 열지 않는다**(`readDetail(a, url, repaired, have)`).
    v1.94에서 "사진이 없으면 PDP" 규칙을 넣었는데, 판정을 *상세 결과*로만 해서
    타일이 사진을 준 행도 페이지를 열었다.
  - 결과: **32.2초 → 18.2초**(3개 몰). 14개 몰 기준 **약 2.5분 → 1.4분**.
  - 더 못 줄이는 부분: 몰이 응답하는 시간 자체. generic 몰은 상품마다 PDP가
    유일한 원단 출처라 요청 수를 줄일 수 없다(Shopify는 벌크 JSON 한 번으로 끝난다).
- **사진이 안 보이는 세 번째 원인은 권한이다**(v1.96.0, 사용자 재보고 "aritzia·
  lululemon·adidas·vuori 여전히 안 보인다"). v1.94가 워커로 다시 받게 했지만
  **워커도 우리가 가진 호스트만 받을 수 있다.** 대부분의 몰은 자기 도메인의
  서브도메인에 사진을 두지만(`images.lululemon.com`·`assets.adidas.com` ✅),
  **Aritzia는 Adobe Scene7**(`aritzia.scene7.com`)이고 Cloudinary·Amplience·
  Contentful도 흔하다 — **우리 목록이 그걸 다 적을 수는 없다.**
  → 이제 워커가 **왜 실패했는지 구분해서 답한다**: 권한이 없으면 `need: origin`.
  LAB은 그걸 모아 밴드에 **"Show these photos" 버튼**을 띄우고, 클릭 안에서
  `permissions.request`를 한다(크롬의 제스처 요건). 매니페스트에 CDN을 계속
  추가하는 대신 **모르는 몰에도 통하는 길**이다.
- **왜 매번 사용자가 먼저 발견해야 했나**(v1.95.0, 사용자 지적 "이런 것들을 왜 내가
  직접 언급을 해야 수정하는거야"). 이 프로젝트의 결함은 **전부 같은 경로**로 왔다 —
  디자이너가 LAB을 열고, 드롭 이름으로 쪼개진 브랜드나 회색 카드 60장이나 빈 원단
  열을 보고, 말해 준다. **그게 진짜 버그였다.** 원인은 셋이고 셋 다 고쳤다:
  - **① 검사가 깨지는 것을 안 봤다.** Set Active는 **✅였다** — 모든 행에 이름·사진·
    가격·조성이 있었으니까. "한 몰이 한 브랜드인가"를 아무도 안 물었다. 원단은
    전무(全無)일 때만 ⚠️라 **60개 중 3개도 건강**으로 통과했다.
    → `saidBrands`(몰이 스스로 말한 브랜드, 리스트 이름을 덮어쓰기 **전**)를 기록하고,
    `multiBrand`가 아닌데 이름이 여럿이면 ⚠️. 원단·사진은 **비율**로 본다
    (`THIN_FABRIC 0.25` · `THIN_PHOTO 0.5`). `health-test` 21건.
  - **② 판정이 아무도 안 보는 곳으로 갔다.** VUORI는 **제대로 잡혔었다**(사진 0/60).
    그런데 그 사실은 서비스워커 콘솔의 `devsitecheck()`에만 있었다. 디자이너는 그걸
    영원히 치지 않는다 — **도구가 알고 있어도 아무 가치가 없다.**
    → 판정을 **사람이 읽는 문장으로 스캔 시점에 써 두고**(`rec.why` —
    "VUORI: no photos at all (60 products)") **LAB 상단 밴드**에 띄운다. 깨끗한
    스캔이면 **아무것도 그리지 않는다**(높이 0 — 컴팩트 예산 유지). `selfcheck-e2e` 8건.
  - **③ 스캔이 있는 곳에서 안 보이는 결함이 있다.** 표시할 때만 실패하는 사진
    (nike·lululemon)은 수집 시점엔 정상이다. LAB이 그걸 세어 **같은 밴드에** 넣는다.
  - 규칙: **새 결함을 고칠 때는 "이걸 도구가 스스로 말했겠는가"를 먼저 묻는다.**
    아니라면 고칠 것이 둘이다 — 그 결함과, 그것을 못 본 검사.
- **사이트 진단은 스캔이 스스로 한다**(v1.67.0, 사용자 요청 "diagnose js 수동
  확인이 아니라 그 부분도 자동화"). 사이트 뚫기의 수동 단계는 "파일 하나 보내기"만
  남는다.
  - **모든 스캔이 URL마다 판정을 기록**한다(`wpb_sitehealth`, collectionSig 키,
    최근 300개): 상품 수 · 이름/사진/가격/원단 채움 수 · 엔진 · ✅/⚠️/❌. build 단계
    진입 시 `recordHealth()`가 쓰고, 실패해도 스캔은 절대 안 죽는다(전부 래핑).
    **원단은 전무(全無)일 때만 ⚠️**(`fabric×all`, withSpec 실행에 한함) — 개별 상품이
    조성을 안 밝히는 건 정상이지만 사이트 전체 0은 추출이 깨진 것이다. 이때는
    상품 페이지 1개도 자동 촬영한다(`diagnoseDetail`): PDP의 JSON-LD 타입·material,
    본문의 `숫자%` 문장 실물 최대 4개, Shopify `.js`의 description 길이·% 개수.
    diagnose-pdp.js가 손으로 하던 그 확인이다(Edikted·Alo가 계기).
  - **실패한 사이트는 그 자리에서 자동 촬영**(`selfDiagnose()`): 플랫폼 마커 ·
    JSON-LD 타입 요약 · 링크 경로 모양 상위 6개(숫자→N 접기) · 반복 그리드 후보 ·
    이미지 lazy 속성 분포 · 가격 leaf 수 · 실제 타일 1개(1.4KB 상한, 전체 4KB 상한).
    diagnose-generic.js가 손으로 모으던 사실들이다. ✅는 촬영하지 않고, 페이지네이션
    으로 스캔한 페이지를 떠났으면 엉뚱한 페이지를 찍지 않도록 건너뛴다.
  - **막히면 페이지를 직접 읽고 스스로 고친다**(v1.79, 사용자 요청 "확장 프로그램이
    알아서 F12에 들어가 사이트 구조를 파악해서 반영"). 진단은 *말해 주는* 데서
    끝났는데, 리스트가 빈 엑셀로 돌아온 디자이너는 정규식 한 줄이 고쳐질 때까지
    도구가 고장 난 상태로 기다려야 했다. 이제 어댑터가 0을 내면 **구조 리더
    (generic)로 페이지를 읽어 그걸 수집한다** — 우리의 낡은 규칙보다 몰의 마크업이
    우선이다. 모델은 안 들어간다(헌장): "링크·사진·가격이 반복되는 덩어리"는
    구조적 사실이고, 개발자가 F12에서 보는 것도 그거다.
    - **가드레일**: 타일 4개 이상 + 이름 60% + (가격 50% 또는 사진 50%)일 때만
      채택한다. 저널/배너 페이지를 상품으로 지어내는 것은 0개보다 나쁘다.
      거절하면 종전대로 funnel + 페이지 사진으로 보고만 한다.
    - **상세까지 고쳐야 절반이 아니다**: 목록 주소를 못 알아보는 어댑터는 상품
      주소도 못 알아본다(Shopify 상세는 `/products/` 요구). 수리된 페이지에서는
      **사이트 무관 리더가 먼저**, 어댑터가 백업(`readDetail`) — 안 그러면 이름·
      사진만 있고 **원단 열이 비는** 결과가 나온다.
    - **레이지 그리드도 열린다**: 스크롤 종료 판정을 어댑터 개수로만 하면 0에서
      즉시 "안정"이라 첫 화면만 남는다. 0일 때는 구조 리더 개수로 센다.
    - **배운 것은 몰 단위로 기억한다**(`wpb_siteprofile`): 호스트 → {낡은 어댑터,
      상품 주소 모양 `/p/<slug>/N`, 횟수}. 서비스워커 콘솔 **`devprofile()`**이
      그대로 작업 목록이 된다.
    - **수리된 사이트는 절대 ✅가 아니다** — 엑셀은 온전하지만 어댑터는 낡은
      것이므로 ⚠️로 남겨 sitecheck에 한 줄 남긴다(전부 ✅면 파일이 안 생기므로
      ✅로 두면 영영 안 고쳐진다). 그 줄은 "엑셀은 정상 · 이 몰 상품 주소는
      `/p/…` 모양이니 그걸 받아들이게 고쳐라"까지 적는다.
    - 못 하는 것: 페이지에 없는 데이터(비공개 API·로그인·봇 차단)는 여전히 실패로
      보고된다. `selfheal-e2e` 21건 · `funnel-e2e` 16건.
  - **"0개"에는 원인이 두 가지고, 리포트가 어느 쪽인지 말해야 한다**(v1.78).
    페이지가 타일을 안 준 것(못 읽는 몰)과, 타일은 다 있었는데 **우리 어댑터가
    전부 버린 것**(우리 규칙이 낡은 것)은 고치는 곳이 완전히 다른데 사진만으로는
    구분되지 않는다 — Aritzia가 후자였고 리포트엔 "0 products"만 있었다.
    이제 전용 어댑터가 0을 냈을 때만 **generic으로 한 번 더 세어 보고**(`rec.funnel`),
    타일이 있었으면 "페이지는 N개를 줬고 X 어댑터가 하나도 안 남겼다 — 몰이 아니라
    우리 필터가 비운 것"이라고 적고 **버려진 주소 3개**를 함께 남긴다(URL 패턴이
    바뀐 모양이 눈에 보인다). sitecheck 파일에서 **⚑ 줄이 diag 덤프보다 먼저** 온다 —
    그게 답이고 덤프는 답이 없을 때만 필요하다. `funnel-e2e` 15건.
  - **0개를 수집한 실행은 그 사실을 말한다**(v1.77). 엑셀이 없으니 조용히 끝나서,
    디자이너가 본 것은 "낯선 .txt 하나"뿐이었다. 이제 마지막 줄이 "수집 0개라
    엑셀이 없다 · sitecheck 파일이 사이트별 결과를 말한다"로 끝난다 —
    마지막 한 줄이 전체 이야기여야 한다.
  - **스캔이 남기는 파일은 엑셀 하나뿐이다**(v1.79.2, 사용자 요청). sitecheck .txt는
    개발자용 채널인데 엑셀만 원한 사람의 다운로드 폴더에 낯선 파일을 남겼고,
    0개 수집 실행에서는 그게 **유일한 산출물**이었다. 이제 리포트는 **저장만**
    되고(`wpb_sitecheck`) 서비스워커 콘솔 **`devsitecheck()`**이 전문을 출력한다
    (`devsitecheck(true)`면 그때 파일로 저장). 실행 마지막 줄은 여전히
    "Excel saved · N of M sites need attention"으로 상태를 말한다.
    (구버전 동작) 실패가 있을 때만 `sitecheck_리스트_날짜.txt`가 자동 다운로드됐다: 전체 집계 + 실패 사이트별 판정·URL·diag JSON,
    맨 위에 "엑셀 자체는 정상, 이 파일을 개발자에게 보내라"는 안내. 전부 ✅면
    파일이 안 생긴다 — 디자이너가 여분 파일을 볼 일은 "고칠 게 있을 때"뿐.
    중지 시엔 **끝난 URL만** 넣는다(잘린 스캔을 broken으로 오진하지 않게 idx 미만만).
  - `content.js`의 **`probe` 메시지**는 그대로(읽기 전용, 스캔 없이 현재 페이지 판정).
    `{detail:true}`를 주면 **상품 1개의 PDP까지 시험**해 조성이 나오는지 본다 —
    원단은 그리드가 아니라 PDP에서 나오므로 목록만 읽는 점검으론 예측할 수 없다.
  - **`devcheck.js`**(서비스워커 콘솔): **`devsweep()`** = 브랜드당 카테고리 1개 +
    PDP 1개 샘플로 **전 카탈로그를 한 번에** 판정(620 URL을 132회로 줄인다 —
    깨지는 건 카테고리가 아니라 몰이다). `devhealth()` = 스캔이 이미 기록한 판정을
    순회 없이 즉시 출력(+실패분 붙여넣기 블록). `devcheck()`/`devcheckStop()` =
    실제 스캔 없이 리스트를 프로브로 순회(옵션: `{brandsOnly, detail, limit, list}`).
  - **패널 UI는 Sites 카드 + 하단 Scan 바 두 개로 고정**한다. 진단 UI를 넣지 않는다.
  - 레이지 그리드는 프로브가 렌더된 것만 세므로 실제 스캔보다 적게 나온다.
    믿을 신호는 정확한 수가 아니라 **0이냐 아니냐**다.
  - 콘솔용 `diagnose-*.js`도 그대로 둔다(더 깊은 덤프가 필요할 때).
  - `devcheck.js`·`diagnose-*.js`는 **`pack.sh` 화이트리스트에 넣지 않는다** —
    배포 ZIP에 개발 도구가 섞이면 심사만 느려진다.
  - 테스트: `scratchpad/autodiag-e2e.js` 25건 — 3사이트(정상/사진 결측/판독 불가)
    리스트런에서 판정 3건·촬영 2건·sitecheck 1개·전부 ✅면 0개까지 고정.
- **업데이트는 아이콘 하나다 — chrome://extensions에 안 간다**(v3.2.0, 사용자 요청
  "굳이 CHROME EXTENSION URL에 들어가지 않더라도"). 남아 있던 두 가지를 고쳤다.
  - **① 칩이 버전만 말하고 상태는 안 말했다.** `v3.1.0`만 적혀 있어서 "지금 게 최신인가"는
    상자를 열어야 알 수 있었는데, **한 번 눌러야 보이는 것은 화면에 없는 것과 같다**.
    이제 헤더 칩이 그대로 `v3.1.0 → 3.2.0`(새 버전 있으면 표시 + 강조)이라고 적는다.
    기존 줄이라 **높이 비용 0**이고, GitHub에 못 닿으면 화살표 없이 번호만 — 없는 사실을
    말하지 않는다.
  - **② 마지막 재시작이 chrome://extensions를 사람 앞에 열었다.** 언팩 확장은 새 파일이
    **리로드해야** 적용되고, 방법은 `chrome.runtime.reload()`뿐이다. v1.89의 측정
    (`reload-probe`로 **재측정** — 여전히 안 돌아옴)은 이 하니스가 커맨드라인으로 확장을
    로드하기 때문이라 데스크톱 크롬을 대변하지 못한다. 그래서 **탭을 없애는 대신 안 보이게**
    했다: 안전망 탭은 **백그라운드**(`active:false`)로 열리고, **돌아오는 즉시 워커가
    닫는다**(탭 id를 `wpb_reloadtry`에 실어 보낸다). 한 번 돌아온 브라우저는 **탭 자체를
    안 연다**. 성공 경로에서 디자이너는 chrome://extensions를 **한 번도 보지 않는다**.
  - **재시작하면 패널도 같이 죽으므로 결과를 말할 자리가 없다.** 워커가 `wpb_updated`에
    적어 두고, 패널이 다음에 열릴 때 **`Updated to v3.2.0`**을 한 번 말한다.
  - 안전장치는 그대로: **스캔 중이면 재시작하지 않는다**(그날 엑셀을 잃는다) · **한 번
    안 돌아온 브라우저는 다시는 자동 재시작하지 않는다**(`wpb_reloadok:false`).
  - update.bat 등으로 **파일이 이미 폴더에 있는 경우**의 배너도 "chrome://extensions를
    열어 ↻" 지시문 대신 **`↻ Restart on the new version` 버튼**을 단다.
  - 새 권한 없음(`chrome.tabs.remove`는 권한이 필요 없다). `restart-e2e` 21건
    (수정 전 코드에서 12건 실패 확인) · `oneclick-e2e`·`updatefetch-e2e`·`selfupdate-e2e`는
    새 계약으로 갱신.
- **업데이트는 버튼 하나 + ↻ 하나다**(v1.86.0, 사용자 요청 "원클릭"). 브라우저가
  사람이 놓치던 두 단계를 대신한다: **다운로드 → 압축 해제 → 확장 폴더에 덮어쓰기**.
  - **폴더는 딱 한 번 고른다**(`showDirectoryPicker`, 권한이 유지됨 → IndexedDB에
    핸들 저장). 그 뒤로는 **⚡ Update now** 한 번이 전부다.
  - 압축 해제에 라이브러리를 넣지 않는다 — zip의 central directory는 고정 필드
    몇 개고 나머지는 `DecompressionStream("deflate-raw")`가 한다(`installer.js`).
    GitHub이 씌우는 **바깥 폴더 한 겹을 자동으로 벗긴다** — 사람들이 잘못 복사하던
    바로 그 껍데기다.
  - **가드레일 둘**: ① 받은 압축 파일에 `manifest.json`·`content.js`·`sites.js`가
    다 있어야 하고 ② **고른 폴더 안에 이미 Market Lens의 manifest.json이 있어야**
    한다(v1.88.1). 한 단계 위 폴더를 고르면 거기에 멀쩡한 확장이 하나 더 생기고
    **크롬이 실제로 로드한 폴더는 낡은 채로 남는다** — 다음 리로드에서
    `File path cannot be resolved`가 나는 경로다. 언팩 확장은 **경로가 곧 신원**
    이라 폴더가 바뀌면 다른 확장이 되고 **카탈로그가 빈다**. 그래서 잘못 고르면
    쓰지 않고 "크롬이 로드한 폴더, manifest.json이 들어 있는 그 폴더를 고르라"고
    말한다.
  - **github.com은 매니페스트에 상시 선언한다**(v1.90.0, 사용자 요청 "github 들어갈
    필요없이 사이드패널에 클릭만으로"). zip **바이트를 읽어야** 하므로 단순 다운로드와
    달리 호스트 권한이 필요한데, 이걸 클릭 안에서 물으면 **Block을 누른 사람은 자기
    업데이트 경로를 없앤 것**이고 되돌리는 길이 패널에 없다. 상시 권한 3개
    (`github.com`·`codeload.github.com`·`raw.githubusercontent.com`)는
    **Market Lens 자신이 오는 곳**이라, 물어볼 가치가 있는 쇼핑몰 오리진과 성격이 다르다.
  - **"새 버전이 있다" 배너의 버튼이 곧 업데이트다** — 예전엔 `⬇ Get the new ZIP
    instead`가 **GitHub 탭을 열었다**(거기서부터는 다시 손으로 하는 그 5단계다).
    이제 `⚡ Update now` 하나가 업데이트 상자를 열고 그대로 설치까지 간다.
    핸들러는 배너를 띄우는 콜백 안이 아니라 **한 번만** 붙인다 — 나중에 붙는 핸들러는
    일찍 누른 사람에게 아무 일도 안 하는 버튼이다.
  - **결과 문구를 버튼 다시 그리기가 지우지 않는다**: `finally`의 `paintAuto()`가
    `#uautonote`를 안내 문구로 덮어써서, 성공해도 실패해도 **"writes straight into …"**만
    남았다(눌렀는데 아무 말도 없는 것으로 보인다). 이제 그 실행이 남긴 말이 있으면
    버튼 글자만 고친다.
  - **다운로드 자체를 시험한다**(`updatefetch-e2e` 21건): github.com을 실제 zip으로
    라우팅해 `#uauto`를 눌러 **fetch → unzip → 폴더 기록 → 문구**까지 그대로 돌린다.
    단, **라우팅은 CORS를 우회한다**(실측 — 선언 안 한 호스트도 200이 온다). 그래서
    권한은 fetch가 성공한 것으로 증명하지 않고 **매니페스트와 `chrome.permissions.contains`**로
    직접 확인한다. 404 응답이면 문구로 말하고 **폴더는 그대로**여야 한다.
  - **버전 칩을 누르면 업데이트가 끝난다**(v1.97.0, 사용자 요청 "버전을 누르면 자동으로
    업데이트되고 새로고침도 자동으로"). 칩이 상자를 열고 거기서 한 번 더 눌러야 했다.
    이제 **폴더를 이미 알고 있고 새 버전이 있으면 누르는 순간 설치까지** 간다
    (폴더가 없으면 첫 누름은 여전히 폴더 질문, 새 버전이 없으면 그냥 상자를 연다).
  - **리로드는 이 브라우저가 자격을 증명한 뒤에만 자동이다.** `chrome.runtime.reload()`는
    언팩 확장을 내리는데 돌아온다는 보장이 없다 — **디자이너 크롬에서 확장이 사라지는
    것이 클릭 한 번보다 나쁘다.** 그래서 v1.89의 자가 측정을 그대로 쓴다: 첫 리로드는
    클릭이고(메모를 남긴다), 돌아오면 `wpb_reloadok=true`가 기록되고, **그 다음부터는
    설치가 끝나면 스스로 리로드한다.** 증명된 뒤에는 chrome://extensions 안전망 탭도
    열지 않는다(매번 탭 하나는 쓰레기다).
  - **스캔 중에는 절대 리로드하지 않는다** — 실행 중 리로드는 그날 엑셀을 잃는다.
    그때는 "스캔이 끝나면 ↻를 누르라"고 말한다. `updatefetch-e2e` 25건 · `oneclick-e2e` 31건.
  - **리로드도 패널 버튼으로 시도한다 — 단, 되는 브라우저에서만**(v1.89.0, 사용자
    요청 "새로고침 없이"). `chrome.runtime.reload()`는 테스트 브라우저에서 3가지
    방식 모두 **돌아오지 않았다**(실측). 하지만 그 브라우저는 확장을 커맨드라인으로
    로드하므로 **데스크톱 크롬을 대변하지 못한다** — 그래서 확장이 **자기 브라우저에서
    직접 확인**한다: 리로드 직전에 메모를 남기고, 돌아오면 지우고, 다음에 패널을 열
    때까지 메모가 남아 있으면 **"이 브라우저는 자동 리로드에서 돌아오지 않는다"**로
    기록하고 **버튼을 다시는 제안하지 않는다**. 아무도 두 번 잃지 않는다.
  - **누르면 chrome://extensions를 먼저 연다**: 돌아오면 탭 하나가 남을 뿐이고,
    안 돌아오면 **이미 복구 화면을 보고 있다**(사라진 확장을 찾아 헤매지 않는다). `oneclick-e2e` 19건(실제 `FileSystemDirectoryHandle`에 기록·껍데기
    제거·하위 폴더 생성·deflate 원본 일치·거부 케이스) · `redownload-e2e` 18건.
- **다시 받는 것이 곧 업데이트다**(v1.85.0, 사용자 요청 "자동 업데이트가 어려우니
  다시 다운로드할 수 있도록"). 크롬은 **직접 로드한 폴더를 절대 갱신하지 않는다**
  — 디스크의 파일이 곧 확장이다. 그래서 재다운로드는 우회로가 아니라 **정규 경로**이고,
  채팅 메시지 속 링크가 아니라 패널 안 한 곳에 있어야 한다.
  - 헤더의 **버전 칩**(`v1.85.0`, 기존 줄이라 **높이 비용 0** — 테스트로 측정)을
    누르면 **번호 붙은 3단계**가 열리고 각 단계에 그 일을 하는 버튼이 있다:
    ① **⬇ Download the latest** → `market-lens.zip`으로 다운로드 폴더에 저장
    (워커의 `chrome.downloads`를 쓰므로 브랜치명이 아니라 알아볼 이름이 붙는다)
    ② 압축 풀어 **같은 폴더에 덮어쓰기**(폴더를 유지해야 리스트·카탈로그가 남는다)
    ③ **Open chrome://extensions** 버튼 → ↻. 확장은 chrome:// 페이지를 열 수 있다
    (추측이 아니라 실측 확인). 열려 있던 쇼핑몰 탭은 스스로 새 엔진을 받는다.
  - 맨 아래 줄이 **지금 어디쯤인지** 말한다: "v1.85.0이 최신" / "v1.86.0이 나왔다" /
    "**이미 폴더에 있다 — 3단계만 남았다**"(update.bat이 이미 돈 경우). `redownload-e2e` 16건.
- **업데이트는 더블클릭 하나 + 리로드 한 번이다**(v1.81.0, 사용자 요청 "그냥 바로
  업데이트되는 편리한 방법"). 확장 폴더 안의 **`update.bat`**(맥은 `update.command`)이
  ZIP을 받아 **같은 폴더에 덮어쓴다** — 압축 풀기·안쪽 폴더 찾기가 사라지고 폴더
  경로가 그대로라 확장 ID·설정·카탈로그가 유지된다(자기 자신은 `/XF`로 제외 —
  실행 중인 배치 파일을 덮어쓰면 나머지 줄이 깨진다).
  - **확장이 디스크를 스스로 본다**: 워커가 5분마다(패널을 열면 즉시) 자기
    `manifest.json`을 fetch해 **실행 중 버전과 디스크 버전을 비교**한다. 다르면
    툴바에 ↻ 배지, 패널에 "폴더에 준비됨 · chrome://extensions에서 ↻ 한 번" 한 줄.
    "새 버전이 있다"와 "새 파일이 이미 폴더에 있다"는 남은 거리가 달라 구분해 말한다.
  - **자동 리로드는 하지 않는다**(측정 결과). `chrome.runtime.reload()`는 unpacked
    확장을 내리고 **다시 올라오지 않는 것을 확인**했다 — 디자이너 브라우저에서
    확장이 사라지는 것보다 클릭 한 번이 낫다. 클릭은 사람에게 남긴다.
  - **F5는 없어졌다**: 리로드 직후 워커가 열려 있는 쇼핑몰 탭에 엔진을 다시 넣는다
    (`reinjectOpenTabs`, 버전당 1회). "Extension context invalidated"의 원인이 이거였다.
  - `selfupdate-e2e` 14건. 진짜 자동 업데이트는 여전히 웹스토어 쪽(`STORE.md`).
  - (구버전 절차) ZIP 덮어쓰기 → 확장 ↻ 리로드 → 대상 탭 F5.
- **커밋·푸시할 때마다 ZIP 다운로드 링크를 함께 안내한다** (사용자 요청).
  `https://github.com/choosingceo-taek/Shop-Category-Collector/archive/refs/heads/claude/main-session-cudnkx.zip`
  압축을 풀면 폴더가 한 겹 더 생기므로 `manifest.json`이 직접 든 안쪽 폴더를 로드해야 한다.
  버전 번호도 같이 알려 리로드 반영 여부를 사용자가 확인할 수 있게 한다.
- 배포 방향: 팀 배포는 Chrome 웹스토어 **Unlisted** 게시. Private은 개발자
  계정이 Google Workspace 조직 소속일 때만 가능한데 **이 회사는 Microsoft
  기반**이라 선택지에서 빠진다(2026-08 확인).
  **자동 업데이트가 되는 길은 두 개뿐**이다 — 웹스토어, 또는 관리형 크롬이
  자체 호스팅 CRX를 강제 설치하는 것. 개발자 모드로 로드한 폴더는 크롬이
  절대 갱신하지 않는다(디스크의 파일이 곧 확장이다). 그래서 `update.js`는
  가능한 절반(새 버전 알림 + ZIP 링크)만 하고, 진짜 자동화는 스토어 쪽이다.
  `.github/workflows/release.yml`이 푸시마다 `pack.sh` → 업로드 → 게시까지
  수행한다(비밀값 4개 미설정 시 ZIP만 만들고 조용히 통과). 서드파티 액션을
  쓰지 않는다 — 게시 자격 증명은 팀 전원의 브라우저에 코드를 넣는 권한이라
  남의 공급망을 우리 공급망으로 만들 이유가 없다. 절차는 `STORE.md` 7장.
