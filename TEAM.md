# Market Lens — 팀원용 안내

브라우징하다 발견한 카테고리 페이지를 리스트로 모으고, 그 리스트를 한 번에 스캔해
**브랜드 · 카테고리 · 상품명 · 썸네일 · URL · 원단 · 혼용률** 엑셀을 만드는 도구입니다.

설치는 **한 번, 약 3분**입니다. 그 뒤로는 업데이트도 알아서 됩니다.

---

## 1. 설치 (한 번만)

1. **[market-lens.zip 받기](https://github.com/choosingceo-taek/Shop-Category-Collector/releases/latest/download/market-lens.zip)**
2. **`문서\Market Lens\`** 같은, 앞으로 옮기지 않을 자리에 폴더를 하나 만들고
   **거기에 압축을 풉니다.** 그 폴더가 곧 확장입니다 — 안쪽 폴더를 더 찾을 필요
   없습니다. (바탕화면 임시 폴더는 피하세요. 이유는 아래 *데이터*)
3. 주소창에 **`chrome://extensions`** (엣지는 **`edge://extensions`**)
   → **개발자 모드** 켜기
4. **압축해제된 확장 프로그램을 로드** (엣지: **압축을 푼 확장 로드**) → 2번의 폴더 선택
5. 툴바 퍼즐 아이콘 → **Market Lens** 옆 압정을 눌러 고정

> **크롬과 엣지 둘 다 됩니다.** 엣지도 크롬과 같은 엔진이라 스캔·엑셀·LAB·자동
> 업데이트가 전부 같습니다. 다른 점은 **여는 모양 하나**입니다 — 사이드 패널을
> 지원하면 크롬과 똑같이 옆에 붙어 열리고, 지원하지 않으면 **같은 화면이 탭으로**
> 열립니다. 기능은 동일합니다.

### 설치 직후 한 번만 해두면 좋은 것

Market Lens를 열고 **오른쪽 위 버전 번호**(`v3.56.0`)를 누르면 상자가 열립니다.

- **📂 Choose my Market Lens folder** → 2번의 그 폴더를 지정
  → 이걸 해두면 **새 버전이 나올 때 패널만 열어도 알아서 설치**됩니다.
- **🔔 Tell me when a new version is out** → 크롬이 새 버전을 알려줍니다.

---

## 2. 쓰는 법

### ① 담기 — 쇼핑몰 페이지에서

보고 싶은 카테고리 페이지(예: 어떤 브랜드의 New In)를 열면 **오른쪽 아래에 동그란
버튼**이 뜹니다. 누르면 브랜드와 카테고리가 이미 채워진 카드가 나오고, **그 자리에서
고칠 수 있습니다.** 넣을 리스트를 누르면 끝입니다.

> 카테고리 이름은 **직접 적은 것이 이깁니다.** 몰이 뭐라고 부르든, 여기 적은 이름이
> 엑셀과 LAB에서 묶는 기준이 됩니다. `New In`으로 적어 두면 분석이 그것만 읽습니다.

버튼이 안 보이는 몰이라면 패널의 리스트를 **우클릭 → Import**로 주소 목록 파일
(.xlsx / .csv / .txt)을 넣어도 됩니다.

### ② 스캔 — 패널에서

툴바 아이콘을 눌러 패널을 엽니다.

- 패널의 두 탭은 **RADAR**(담고 스캔하는 곳)와 **DROPS**(모은 상품 사진 벽)입니다.
- RADAR 위쪽 **탭이 리스트**입니다(FABRIC / ACTIVE / …). 탭을 누르면 그 아래가 그 리스트의 사이트 목록입니다.
- 아래 **▶ SCAN ALL**을 누르면 리스트를 하나씩 훑습니다. 물어보면 확인만 하면 됩니다.
- **한 사이트만 다시 긁고 싶으면** 그 줄에 마우스를 올리면 나오는 **↻**를 누르세요.
  그 사이트 하나만 스캔하고, 결과는 원래 리스트에 그대로 들어갑니다.
- 도는 동안 **⏸ PAUSE / ■ STOP**을 쓸 수 있습니다. 중간에 멈춰도 그때까지 모은 건 남습니다.
- **지난주에 이미 읽은 상품은 상품 페이지를 다시 열지 않습니다** — 그만큼 빨라집니다.
  단 **원단과 사진이 둘 다 있는 행만** 그렇게 하고, 하나라도 비어 있으면 다시 읽습니다
  (다 가져오는 것이 시간보다 우선입니다). 가격은 언제나 오늘 값입니다.
- 끝나면 **⬇ EXCEL**로 파일을 받습니다. (누르기 전까지는 파일을 만들지 않습니다 — 사진을 전부 내려받는 일이라 시간이 듭니다.)

리스트를 **우클릭**하면 이름 변경 · 자동 스캔 예약 · txt/xlsx 내보내기 · Import ·
삭제가 나옵니다.

> **주소를 리스트에서 빼면 그 주소가 모아 온 상품도 같이 나갑니다.** 줄 오른쪽
> **✕**를 누르면 **몇 개가 함께 없어지는지 먼저 물어봅니다** — 예를 누르면
> DROPS에서도 LAB에서도 다음 엑셀에서도 사라집니다. **다른 주소도 같이 모으고
> 있던 상품은 남습니다**(그 주소는 아직 그걸 보고 있으니까요).
> 다만 **이미 지나간 주의 요약 숫자는 줄지 않습니다** — 주마다 집계를 따로 얼려
> 두기 때문입니다(그래서 상품을 정리해도 작년 추이가 남습니다).

### ③ 보기 — LAB

패널 위쪽 검은 **LAB** 띠를 누르면 새 탭에서 열립니다.

- **HOME 위쪽 INTERVAL은 네 가지**입니다 — **Monthly · Biweekly · Weekly · Daily**.
  같은 상품을 어느 간격으로 묶어 볼지 고르는 것이고, 아래 그래프가 전부 따라갑니다.
  드롭을 2주 주기로 내는 몰이 많아 **Biweekly**가 그 리듬에 맞고, **Daily**는 한 주
  안에서 움직임을 볼 때 씁니다. 2주 구간은 **경계가 고정**이라 오늘이 며칠이든 같은
  상품은 늘 같은 구간에 들어갑니다 — 보는 날에 따라 칸이 바뀌면 비교가 안 되니까요.
  Daily로 6개월을 보면 막대가 180개라 **막대 위 숫자는 생략**됩니다(겹쳐서 못 읽습니다).
  정확한 수치는 아래 기록에 그대로 있습니다.
- **HOME** — 주차별로 **원단 · 색상 · 핏**이 어떻게 움직였는지. 카드는
  **순위 · 키워드 · 실제 아이템 수 · 주간 꺾은선**입니다(선은 한 축 안에서
  **같은 눈금**이라 카드끼리 높이를 비교할 수 있습니다).
  - 카드에 **`#3` 뱃지**가 붙으면, **직접 가져오신 구글 트렌드 CSV** 안에서 그 검색어가
    최근 관심도 **상위 15위** 안에 있다는 뜻입니다(LAB 맨 아래 Data → `Add Google
    Trends (.csv)`). 구글은 공개 API가 없어서 **가져온 파일이 유일한 근거**이고,
    파일이 없으면 뱃지도 나오지 않습니다.
  (탭 이름이 LAB → **HOME**으로 바뀌었습니다. 왼쪽 위 LAB 표시는 그대로입니다.)
  - **색상은 12개**(Beige · Black · Blue · Brown · Green · Grey · Orange · Pink ·
    Purple · Red · White · Yellow)로 묶어서 봅니다. 몰이 `Deep Sea Navy`라고 써 둔
    것도 **Blue 한 칸**으로 갑니다 — 안 그러면 한 번씩만 나오는 이름이 200개가 됩니다.
  - **원단은 15개**(Polyester · Cotton · Elastane/Spandex · Nylon · Viscose ·
    Polyamide · Silk · Linen · Acrylic · Rayon · Wool · Tencel · Polyurethane ·
    Cupro · Acetate). `Organic Cotton`도 `Recycled Cotton`도 **Cotton 한 줄**입니다.
    이 15개에 없는 섬유(모달·캐시미어 등)는 **엑셀과 상품 목록에는 그대로 있고**
    이 축에만 안 나옵니다.
- **New In** — **주 단위(월~일)**로 봅니다. 모은 주가 **최신순으로 전부** 나오고,
  각 더미 위에 그 주가 덮는 날짜(`Aug 17 – Aug 23`)가 적힙니다. 고르는 컨트롤은
  없앴습니다 — 고를 게 없으니 **숨는 주도 없습니다.** 월요일에 스캔하고 수요일에 더
  스캔해도 **그 주 하나로 합쳐집니다**(같은 상품은 한 번만 — 주소가 같으면 같은 행을
  갱신합니다).
- **New In / CLOTHING** — 모은 상품을 브라우징. **몰이 진열한 순서 그대로**
  보여줍니다(브랜드 → 페이지 → 몰이 놓은 자리). 그 순서가 곧 MD의 랭킹입니다.
  왼쪽 필터는 **BRAND · CATEGORY · FABRIC · COLOUR** 네 가지이고, FABRIC은
  **위의 15개 섬유**로만 나옵니다. **New In과 CLOTHING은 필터를 각자 기억합니다.**
  (위쪽 검색바는 없앴습니다 — 왼쪽 필터가 같은 질문에 개수까지 붙여서 답합니다.)
- 상품 사진 벽은 **사이드 패널의 DROPS 탭**에 있습니다(리스트 옆이 제자리입니다).
- LAB 맨 아래 **Data** — 백업 / 다른 사람 백업 합치기 / 공간 정리
- **HTML로 내보내기** — 인터넷 없이도 열리는 대시보드 한 장(사진까지 파일 안에 들어갑니다)

---

## 3. 업데이트

설치할 때 폴더를 지정해 두셨다면, 새 버전이 나오고 **Market Lens를 여는 순간**
업데이트 상자가 열립니다.

- 그대로 **설치되고 재시작**되거나,
- **⚡ Update now 한 번**을 눌러 달라고 합니다. 크롬이 **브라우저를 껐다 켠 뒤에는
  폴더를 한 번 다시 확인**받기 때문입니다(크롬 정책이라 없앨 수 없습니다). 누르면
  폴더 확인 창이 뜨고, 허용하면 거기서 끝까지 갑니다 — **폴더를 다시 찾을 필요는
  없습니다.**

> `📂 Choose my Market Lens folder`라고 **폴더를 처음부터 고르라고 나오면** 아직
> 한 번도 지정하지 않으신 것입니다. 그때만 폴더를 찾아 주시면 됩니다.

- **스캔 중에는 절대 재시작하지 않습니다.**
- 버전 칩이 `v3.56.0 ?`(점선)이면 GitHub에 못 닿은 것입니다 — 새 버전이 있는지
  모른다는 뜻이지, 최신이라는 뜻이 아닙니다.

---

## 4. 데이터 — 어디에 있고 언제 없어지나

모은 상품은 **각자 크롬 안에만** 있습니다. 서버로 아무것도 보내지 않습니다.

| | |
| --- | --- |
| 인터넷 사용 기록 삭제 | **안 없어집니다** (전체 기간·모든 항목으로 실제 지워서 확인) |
| 쇼핑몰 쿠키/사이트 데이터 삭제 | **안 없어집니다** |
| 버전 업데이트 | **안 없어집니다** (같은 폴더에 덮어쓰기이므로) |
| **확장 삭제 후 재설치** | 없어집니다 |
| **폴더를 다른 자리로 옮겨서 다시 로드** | 없어집니다 ← 가장 흔한 사고 |

크롬은 **폴더 경로로 확장을 구분**합니다. 폴더를 옮기면 크롬 눈에는 다른 확장이고,
카탈로그가 빈 채로 시작합니다. 그래서 설치할 때 **옮기지 않을 자리**에 두시라고
한 것입니다.

**월 1회쯤 LAB 맨 아래 Data → `Back up (.json)`** 을 받아 두시면 노트북을 바꿔도
`Merge in a backup…`으로 그대로 옮길 수 있습니다. 팀원끼리 나눠 스캔한 걸 한 곳에
합칠 때도 같은 버튼을 씁니다.

> 운영 권고: **같은 몰을 여러 명이 중복으로 스캔하지 마세요.** 몰 입장에서는 N배
> 트래픽이고 결과는 같습니다. 리스트를 사람별로 나누고, 주 1회 한 명이 합치는 것이
> 가장 깔끔합니다.

---

## 5. 잘 안 될 때

**동그란 버튼이 안 보여요**
그 몰에 아직 접근 권한이 없는 경우입니다. 패널에 나타나는 **＋ Add this shop**을
한 번 누르면 크롬이 물어보고, 허용하면 그 뒤로는 항상 보입니다.

**스캔했는데 상품이 0개예요**
LAB 위쪽 띠가 이유를 사람 말로 적어 줍니다 — 몰이 안 준 것인지, 우리가 못 읽은
것인지, 잠깐 혼잡했던 것(다시 스캔하면 됨)인지 구분해서 말합니다. 그 문장을 그대로
보내 주시면 됩니다.

**원단 열이 비어 있어요**
같은 자리에 이유가 적힙니다. 몰이 상품 페이지에 조성을 안 적어 둔 경우가 가장
많고, 그건 고칠 수 있는 종류가 아닙니다.

**Windows Security가 "위협을 발견했습니다" / 다운로드가 "바이러스 발견"으로 막혀요**

**두 가지 원인이 있었고 둘 다 없앴습니다(v3.54.0).**

1. 예전(2026-08 이전) 방식으로 설치한 분들의 폴더에 **`update.bat`**이라는 옛날 자동
   업데이트 스크립트가 남아 있었습니다. 인터넷에서 파일을 받아 폴더에 덮어쓰는
   스크립트라 **생김새가 악성 코드와 같아서** Defender가 표시합니다(실행하지 않아도,
   폴더가 다시 검사될 때마다 — 그래서 업데이트할 때마다 떴습니다).
   → 이제 **업데이트가 그 파일을 폴더에서 지웁니다.**
2. 그리고 v3.53.0에서 **그 원인을 설명하는 주석에 문제의 명령줄을 그대로 적어 두는
   바람에**, 이번에는 **ZIP 안에 그 문자열이 들어가서** 다운로드가 막혔습니다.
   검사기는 뜻이 아니라 **글자**를 읽습니다. → 문장으로만 설명하도록 고쳤고,
   **배포 파일에 그런 문구가 들어가면 시험이 실패하도록** 막아 뒀습니다.

**지금 바로 되는 방법** — ZIP을 직접 받지 마세요. **패널 오른쪽 위 버전 번호를 누르고
⚡ Update now**를 쓰시면 확장이 직접 받아서 설치합니다. 크롬 다운로드를 거치지 않으므로
다운로드 검사에 걸리지 않습니다.

- 이미 뜬 알림은 **Windows 보안 → 보호 기록**에서 **제거**로 정리하시면 됩니다.
  Market Lens 폴더를 예외로 등록하실 필요는 없습니다.
- **그래도 다시 뜨면** 보호 기록에 적힌 **위협 이름과 파일 경로**를 보내 주세요.
  그 두 가지가 어느 파일인지 정확히 말해 줍니다.

**그래도 이상하면** — LAB 위쪽 밴드의 문장이나 화면 사진을 보내 주세요. 그 문장이
어디를 봐야 하는지 알려 줍니다.
