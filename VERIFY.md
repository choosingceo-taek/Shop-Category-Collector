# VERIFY — 유지보수 가이드

로직(추출 스코핑, 페이지네이션, 중복 제거, 상세 추출, 엑셀 생성, 출처 규칙,
범용 DOM 휴리스틱 스캐너)은 47개 케이스로 회귀 테스트되어 있습니다
(`node_modules/jsdom` 기반 픽스처 포함). 남는 리스크: **월마트가 페이지 구조를
바꾸는 경우**, **범용 모드가 특정 사이트의 카드 마크업에서 오탐/누락하는 경우**.

## Walmart 수집이 깨졌을 때 (0개 수집, 개수 불일치 등)

1. 문제가 나는 월마트 페이지에서 **F12 → Console**
2. `diagnose-console.js` 파일 내용 전체를 붙여넣고 Enter
3. 출력 전체(자동으로 클립보드에 복사됨)를 개발자/Claude에게 전달

출력에는 실제 데이터 blob 개수, 상품 배열 위치, 샘플 필드 값, 페이지네이션
정보가 담겨 있어 **추측 없이** `sites.js`의 월마트 어댑터를 수정할 수 있습니다.

전부 `sites.js`의 walmart 어댑터 안입니다. `content.js`(엔진)은 건드릴 일 없음.

| 증상 | 함수 |
|---|---|
| 목록 0개 / 개수 불일치 | `mainContainer` / `stacksItems` / `scrapeList` |
| 페이지를 안 넘어감 / 너무 넘어감 | `totalPages` / `resultCount` / `nextPageUrl` |
| 원단 조성 누락 | `findComposition` (스펙 쌍 + "Material: ..." 불릿 + FIBER 검증) |
| 색상 누락 | `extractColorways` (variantCriteria/variantList) |
| 디자인 특징 누락 | `collectSpecs` + `pickDesign` (DESIGN_LABELS 목록) |

## 범용(generic) 모드가 잘 안 잡힐 때

범용 어댑터(`sites.js`의 `generic`)는 "가격 텍스트 → 이미지+링크가 있는 가까운
조상(타일) → 같은 모양이 3개 이상 반복되는지 확인" 순서로 상품 카드를 찾습니다.

| 증상 | 원인/조치 |
|---|---|
| 0개 수집 | 그 사이트가 가격을 이미지(price-as-image)로 렌더링하거나, 카드 반복
구조가 특이할 수 있음. `generic._tileAncestor`/`_signature`로 콘솔에서 직접
디버깅 가능 |
| 상품명이 이상함 (가격/설명 문구가 섞임) | `bestName()`의 우선순위(제목 클래스 →
alt → title/aria-label → 최장 텍스트)를 그 사이트 마크업에 맞게 조정 |
| 홍보 배너/무관한 가격이 섞임 | `signature()`의 반복 판정 임계값(현재 3개)을
그 사이트에 맞게 조정, 또는 그 사이트만을 위한 전용 어댑터로 승격 |
| 페이지가 1개인데 여러 페이지처럼 돎 | `nextPageUrl`이 `?page=N`을 시도하고
있음 — 그 사이트가 다른 파라미터를 쓰면 전용 어댑터가 필요 (2개 연속 신규
0개 시 자동 정지되므로 무한 루프는 안 남) |

특정 사이트에서 반복적으로 안 맞으면, 그건 범용 모드의 한계 신호입니다 —
README의 "새 사이트 추가하기 B" 방법으로 전용 어댑터를 만드는 게 정답입니다.

## 지켜야 할 규칙 (zero-hallucination)

값은 **페이지에 실제로 있는 텍스트만** 기입합니다. 코드가 가격·원단·색상을
만들어내면 안 됩니다. 추론이 필요하면 "재확인 필요", 못 찾으면 "정보 확인"
(+원인)으로 빨갛게 표시하는 것이 계약입니다 — `excel.js`가 강제하고, 범용
어댑터는 브랜드/카테고리/색상/원단/디자인을 아예 시도하지 않고 빈 값으로
넘겨서 이 규칙을 자동으로 지킵니다.

## 새 사이트 추가

README.md의 "새 사이트 추가하기" 참고. 기본 정보만 필요하면 `manifest.json`
패턴 추가만으로 끝(A), 정밀 추출이 필요하면 전용 어댑터 작성(B).
무한 스크롤 사이트는 엔진에 스크롤 모드 추가가 필요합니다(1회 작업, 이후 재사용).
