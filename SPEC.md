# SPEC — 로드맵 & 미구현 항목

프로젝트 비전·원칙은 `CLAUDE.md` 참조. 이 문서는 **아직 안 된 것과 그 순서**만 다룬다.

## 최종 비전 (2026-08 확정) — 수집 → 카탈로그 → 프로젝트 → 리포트

```
[수집]     확장이 실제 브라우저에서 상품을 정확히 읽는다
   ↓
[카탈로그] 브랜드·카테고리·상품이 한 곳에 쌓인다 (사이트를 찾아다니지 않는다)
   ↓
[프로젝트] 원하는 브랜드/카테고리/상품만 골라 폴더로 정리한다
   ↓
[리포트]   그 폴더의 상품만으로 결과물. 팀별로 템플릿 선택
```

**왜 확장이어야 하는가**: LLM에 URL을 넘겨 상품을 읽게 하면 봇 차단·JS 렌더링·
로그인 상태 때문에 되는 사이트와 안 되는 사이트가 갈린다. 확장은 사용자의 실제
로그인된 브라우저에서 돌기 때문에 이 문제가 없다. **한 번 카탈로그에 들어오면
아무도 다시 URL 접근과 싸우지 않는다** — 이것이 이 도구의 대체 불가능한 가치다.

**규모/배포**: 사용자 약 60명(디자인팀). 개인별 로컬 카탈로그로 시작한다
(서버·계정 없음). 팀이 같은 카탈로그를 공유하는 것은 실제 필요가 확인된 뒤.
60명에게 ZIP을 수동 배포하는 것은 불가능하므로 **웹스토어 Unlisted 배포가 사실상
필수**다.

**카탈로그가 정확도 요구를 높인다**: 예전엔 스캔이 틀리면 그 xlsx만 버리면 됐지만,
카탈로그는 데이터가 영구히 쌓인다. 잘못 수집된 상품은 계속 남아 모든 프로젝트와
리포트에 반복 반영된다. 그래서 **수집 정확도 검증이 카탈로그 구축의 선행 조건**이다.

## 현재까지 된 것 (요약)

- 어댑터 레지스트리 `[walmart, target, cottonon, zara, cos, massimodutti, shopify, generic]`, 플랫폼 감지형(shopify)
- cos/massimodutti는 `houseBrandAdapter` 팩토리(zara 모델 일반화)로 추가 — 단일 브랜드
  + 무한 스크롤 그리드. 사이트별 CSS 하드코딩 없이 generic 스크레이프 + JSON-LD 상세
- 전 페이지 자동 순회(probe 방식), 상세 수집(원단·색상·사이즈·브랜드·정가)
- 원단 조성 파서: 섬유 앵커 방식으로 "100% Organic cotton"/"80% Recycled polyester"
  같은 수식어-접두 표기까지 인식(할인 문구 "50% off"는 배제)
- 12컬럼 xlsx(썸네일 임베드 + 출처 규칙), post-scan 필터, FAB + 팝업 UI
- Node+jsdom 12개 스위트 338+ 케이스

## Phase 0 — 수집 정확도 확정 (모든 것의 선행 조건)

카탈로그에 쌓기 전에 숫자가 맞는지 확인한다. 오염된 데이터는 영구히 남는다.

- [ ] Massimo Dutti `sleeveless-n1897`: 수집 개수가 실제 상품 수에 근접(가상화 대응)
- [ ] 같은 결과의 Color Count가 실제 색상 수(familyKey 수정)
- [ ] Walmart 필터 스캔에 필터 밖 품목(잡화)이 섞이지 않음(결과수 상한)

## Phase A — 카탈로그 (다음 작업)

수집 결과가 파일로만 나가는 지금 구조에, **누적 저장소**를 더한다.

- [ ] 확장 내 **IndexedDB 저장소**(`store.js`) — 서비스워커/확장 페이지 공용.
      스토어: `products`(브랜드·카테고리·소스 인덱스), `scans`(실행 이력),
      `projects`. chrome.storage.local(10MB)로는 수천 상품을 감당 못 함.
- [ ] 스캔 완료 시 xlsx/JSON 저장과 **함께 카탈로그에 upsert**(같은 상품은 갱신).
- [ ] **카탈로그 탭**(전체 화면 확장 페이지) — 브랜드/카테고리/원단/가격대로
      검색·필터·열람. 사이드 패널은 수집용, 카탈로그 탭은 열람·정리용.

## Phase A2 — 스캔 목록(URL 리스트) ← 스토리라인의 시작점

사용자는 지금 "브랜드 + 카테고리 URL" 목록을 **텍스트 파일로 손수 관리**하고 있다
(2026-08-05 업로드된 Aritzia/Zara 목록이 그 증거). 그 목록이 도구 안으로 들어와야
한다. 이게 주간 반복 작업의 실체다 — 매주 같은 URL들을 다시 돈다.

- [ ] **URL 리스트 저장**: {브랜드, 카테고리명, URL}을 목록으로 등록·편집·삭제.
      현재 보고 있는 페이지를 한 번에 추가하는 버튼(FAB/패널)도 필요.
- [ ] **목적별 그룹**: 리스트를 여러 개 만들어 목적에 따라 나눈다
      ("26SS 상의 리서치", "경쟁사 원피스" 등).
- [ ] **일괄 스캔**: 리스트의 URL을 순서대로 자동 순회하며 각각 전체 페이지 수집.
      기존 job 엔진(한 카테고리의 페이지네이션) 위에 큐 계층을 얹는다. 진행 상황은
      "3/8번째 URL 수집 중"처럼 중계.
- [ ] 텍스트 붙여넣기로 목록 일괄 등록(지금 쓰는 txt를 그대로 옮길 수 있게).

## Phase A3 — 카탈로그에서 엑셀 추출

스토리라인의 종착점은 엑셀이다. 지금 xlsx는 **스캔 1회당 1개**만 나오는데,
여러 URL을 모은 결과를 하나로 뽑을 수단이 없다.

- [ ] 카탈로그에서 **현재 필터된 상품 전체를 xlsx로 내보내기**(썸네일 임베드 포함,
      기존 `excel.js`의 12컬럼 규칙 그대로). 프로젝트 폴더 단위 내보내기도 동일 경로.

## Phase B — 프로젝트 폴더

- [ ] 카탈로그에서 상품을 골라 **프로젝트에 담기**(다중 선택, 브랜드/카테고리 단위).
- [ ] 프로젝트 = 리포트의 입력 단위. 프로젝트 안에서만 통계가 계산된다.
- [ ] 손으로 담은 clip 항목은 프로젝트에 넣을 수 있으나 **트렌드 통계와는 분리**
      (편향 표본 — CLAUDE.md의 트렌드 왜곡 금지).

## Phase C — 리포트 + 팀별 템플릿

- [ ] 프로젝트 단위 리포트(색·원단·가격대·브랜드·디자인 디테일 분포). 순수 계산.
- [ ] **팀별 템플릿 선택** — 같은 데이터를 팀이 원하는 레이아웃으로 렌더.
      템플릿은 데이터가 아니라 표현만 바꾼다(숫자는 동일).
- [ ] 공유용 단일 HTML 내보내기(첨부 하나로 열림, 서버 불필요).

## Phase D — 팀 공유 (필요 확인 후)

- [ ] 같은 카탈로그를 여러 명이 사용. 계정·백엔드 필요 — 실제 요구가 생기면 착수.

## Phase 1 — 플랫폼 엔진 일반화 (배경 작업)

지금은 어댑터가 사이트별로 나뉘어 있다. 원칙("사이트가 아니라 플랫폼")에 맞춰
**플랫폼 엔진**으로 수렴시킨다.

- [ ] **SFCC 엔진**: `cottonon` 어댑터를 SFCC(Demandware) 범용 엔진으로 승격.
      감지 마커: `dwvar_`/`cgid=` URL, `demandware`/`dwstatic` 자산 경로.
      → 다른 SFCC 몰(수많은 패션 리테일러)이 코드 없이 라우팅됨.
- [ ] **엔진 = {shopify, sfcc, custom-spa(walmart류), generic}** 로 재정리.
      `sites.js`를 adapter→engine 2계층으로. walmart는 custom-spa 엔진의 config.
- [x] Target 추가(커스텀 SPA, ?Nao=24 오프셋). 상세 추출은 실제 페이지 진단으로 보정 예정.

## Phase 2 — 카테고리 taxonomy 인식

- [ ] 페이지의 네비게이션/브레드크럼/사이드바에서 **카테고리 트리 추출**
      (JSON-LD BreadcrumbList, `nav[aria-label]`, SFCC `cgid` 계층).
- [ ] 추출한 트리를 팝업에 보여주고 **사용자가 정리**(포함/제외/이름수정).
      원칙: 자동 감지는 출발점, 사용자가 확정.

## Phase 3 — 카테고리별 샘플링 (트렌드 왜곡 방지)

- [ ] 여러 카테고리를 큐에 넣고 **카테고리당 N개 샘플링**(상위 N / 균등).
      pre-scan 단계에선 범위 필터만(카테고리·깊이·정렬), 속성 필터는 post-scan.
- [ ] 인기 신호 컬럼: **Rank(정렬 순서), Rating, Reviews, Best-seller 뱃지**.
      "잘 팔리는 제품 → 그 원단" 리서치 흐름 완성. (사이트 노출분만, zero-halluc.)

## Phase 4 — 스캔 프로필 저장/재사용

- [ ] per-site **스캔 프로필**(카테고리 목록·샘플 수·필터·상세수집 여부)을
      `chrome.storage`에 저장 → 시즌마다 재사용. 프로필 UI(선택/편집/삭제).

## Phase 5 — 비주얼 레포트 렌더러

- [ ] 수집 데이터로 **정량 레포트를 순수 계산으로** 생성(색 분포·가격대·원단
      비중·브랜드 점유·키워드 빈도). 차트는 확장 내 렌더(LLM 없이).
- [ ] 서술형 해석은 xlsx를 Claude에 핸드오프하는 별도 레이어(확장 안엔 LLM 없음).

## 상시 백로그

- [ ] 무한 스크롤 사이트 대응(스크롤-수집 모드). 현재는 `?page=N` 우선.
- [ ] optional_permissions로 사이트 추가 시 재심사 없이 권한 요청.
- [ ] 스토어 제출용 클린 패키지 스크립트(테스트/진단 파일 제외).

## 유지보수 메모

- 라이브 사이트 접근이 막힌 개발 환경 → 실제 구조는 사용자가
  `diagnose-console.js`/`diagnose-generic.js` 콘솔 출력으로 확인해 어댑터를 맞춘다.
- 모든 변경은 `scratchpad/*.js` Node 스위트로 회귀 확인 후 커밋.
